import CoreLogger from './core_logger';
import { CoreLoggerOptions } from '../typings';
export default class ConsoleLogger extends CoreLogger {
    options: CoreLoggerOptions;
    constructor(options?: CoreLoggerOptions);
}
