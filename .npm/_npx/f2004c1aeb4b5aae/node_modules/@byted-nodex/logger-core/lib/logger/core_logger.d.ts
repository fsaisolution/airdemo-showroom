import Transport from '../transport/transport';
import { BaseLogData, CoreLoggerOptions, Level, LogData } from '../typings';
export default class CoreLogger {
    static readonly version: string;
    private readonly version;
    readonly levels: {
        trace: number;
        debug: number;
        info: number;
        warn: number;
        error: number;
        fatal: number;
    };
    readonly name?: string;
    readonly level: Level;
    private transports;
    formatData: (data: LogData) => string;
    transformData: (data: LogData) => LogData;
    callSiteFrameIndex: number;
    private flushInterval;
    private flushTimer;
    constructor(options?: CoreLoggerOptions);
    addTransport(name: string, transport: Transport): CoreLogger;
    addTransport(transports: Transport[]): CoreLogger;
    removeTransport(name: string): void;
    getTransport(name: string): Transport | undefined;
    disableTransport(name: string): void;
    enableTransport(name: string): void;
    reloadTransport(name: string): void;
    reloadAllTransports(): void;
    closeTransport(name: string): void;
    closeAllTransports(): void;
    /**
     * Send log to all transports
     */
    log(data: BaseLogData): void;
    flush(): void;
    flushSync(): void;
    close(): void;
    trace(msg: string | Error, ...splats: any[]): void;
    debug(msg: string | Error, ...splats: any[]): void;
    info(msg: string | Error, ...splats: any[]): void;
    warn(msg: string | Error, ...splats: any[]): void;
    error(msg: string | Error, ...splats: any[]): void;
    fatal(msg: string | Error, ...splats: any[]): void;
}
