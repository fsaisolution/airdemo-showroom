"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.fnv32aTags = exports.fnv32a = exports.toBufferLE = exports.fnv1a = void 0;
const offset32 = 2166136261; // The prime, 32 bit offset_basis = 2,166,136,261 = 0x811C9DC5.
// Hash Algorithm: fnv-1a
function fnv1a(data) {
    let hash = offset32;
    for (let i = 0; i < data.length; i++) {
        hash = hash ^ data.charCodeAt(i);
        hash += (hash << 1) + (hash << 4) + (hash << 7) + (hash << 8) + (hash << 24);
    }
    return hash >>> 0; // unsigned 32 bit integer.
}
exports.fnv1a = fnv1a;
// To be compatible with: https://tools.ietf.org/html/draft-eastlake-fnv-12#page-6
function toBufferLE(hash) {
    if (typeof hash != 'number') {
        throw new Error('fnv32.toBufferLE input must be a Number.');
    }
    const buf = Buffer.allocUnsafe(4);
    buf[0] = hash & 0xff;
    buf[1] = (hash >>> 8) & 0xff;
    buf[2] = (hash >>> 16) & 0xff;
    buf[3] = (hash >>> 24) & 0xff;
    return buf; // 32 bit little endian Buffer.
}
exports.toBufferLE = toBufferLE;
function fnv32a(...ss) {
    let hash = offset32;
    for (const s of ss) {
        for (let i = 0; i < s.length; i++) {
            hash = hash ^ s.charCodeAt(i);
            hash += (hash << 1) + (hash << 4) + (hash << 7) + (hash << 8) + (hash << 24);
        }
    }
    return hash >>> 0;
}
exports.fnv32a = fnv32a;
function fnv32aTags(tags) {
    if (tags.length === 0) {
        return 0;
    }
    let hash = offset32;
    for (const tag of tags) {
        for (let i = 0; i < tag.key.length; i++) {
            hash = hash ^ tag.key.charCodeAt(i);
            hash += (hash << 1) + (hash << 4) + (hash << 7) + (hash << 8) + (hash << 24);
        }
        for (let i = 0; i < tag.value.length; i++) {
            hash = hash ^ tag.value.charCodeAt(i);
            hash += (hash << 1) + (hash << 4) + (hash << 7) + (hash << 8) + (hash << 24);
        }
    }
    return hash >>> 0;
}
exports.fnv32aTags = fnv32aTags;
