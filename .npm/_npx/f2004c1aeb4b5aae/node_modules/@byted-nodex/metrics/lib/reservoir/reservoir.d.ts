import { Value } from '../measure';
import { MetricType } from '@byted-nodex/metrics-codec';
export interface ReversoirValue {
    suffix: string;
    type: MetricType;
    values: number[];
    lastValues?: number[];
}
export declare abstract class Reservoir {
    next?: Reservoir;
    value: Value;
    lastActiveTime: number;
    /** 空闲周期，如果该次上报间隔中无打点，该值 +1 */
    protected idle: number;
    get type(): MetricType;
    get policy(): import("../measure").ValuePolicy;
    get suffix(): string;
    get active(): boolean;
    constructor(val: Value);
    merge(val: Value): boolean;
    marshal(): ReversoirValue[] | null;
    skip(): void;
    abstract init(): void;
    abstract reset(): void;
    protected abstract values(): ReversoirValue[] | null;
    protected abstract realMerge(val: Value): boolean;
}
