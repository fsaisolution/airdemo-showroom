import { Value } from '../measure';
import { Reservoir, ReversoirValue } from './reservoir';
import { AnalyseCollector, GCOption } from '../type';
export declare class Queue {
    head?: Reservoir;
    active?: boolean;
    size: number;
    /** 计算指标 */
    marshall(cb: (vs: ReversoirValue[], node: Reservoir) => void): void;
    /** 分析缓存数据，用于 dump 结果排查用 */
    analyse(info: AnalyseCollector): void;
    mergeOrInsert(value: Value): void;
    gc({ expiredDuration, ignoreMetrics }: GCOption): void;
}
export { Reservoir };
