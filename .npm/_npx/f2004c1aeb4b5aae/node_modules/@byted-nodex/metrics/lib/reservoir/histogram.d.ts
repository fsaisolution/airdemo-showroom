/**
 * Histogram
 * 新打点类型，根据区间进行统计
 */
import { Value } from '../measure';
import { Reservoir } from './reservoir';
interface Bucket {
    left: number;
    count: number;
}
export declare class Histogram extends Reservoir {
    sum: number;
    count: number;
    buckets: Bucket[];
    realMerge(val: Value): boolean;
    values(): {
        suffix: string;
        type: import("@byted-nodex/metrics-codec").MetricType;
        values: number[];
    }[] | null;
    init(): void;
    reset(): void;
}
export {};
