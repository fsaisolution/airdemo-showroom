"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DeltaCounter = void 0;
/**
 * DeltaCounter
 */
const addition_1 = require("./addition");
class DeltaCounter extends addition_1.Addition {
    values() {
        const counter = this.value.value;
        const delta = counter - this.lastValue; // 周期差值
        const rate = delta / this.policy.interval; // 周期变化比例
        const vs = [
            {
                suffix: this.value.suffix,
                type: this.value.type,
                values: [delta, rate, counter],
                lastValues: this.setLastValue ? [0, 0, this.lastValue] : undefined,
            },
        ];
        this.lastValue = counter;
        this.setLastValue = false;
        return vs;
    }
}
exports.DeltaCounter = DeltaCounter;
