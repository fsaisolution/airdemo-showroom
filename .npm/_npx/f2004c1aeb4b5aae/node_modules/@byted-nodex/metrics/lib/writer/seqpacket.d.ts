import { Writer, WriterOptions, WriterPool } from './';
export interface SeqpacketWriterOptions extends WriterOptions {
    socketPath: string;
}
/**
 * SeqpacketSender keeps connected SeqpacketSockets.
 */
export declare class SeqpacketWriter implements Writer {
    private socketPath;
    private socket;
    private Socket;
    ready: boolean;
    closed: boolean;
    private logger;
    constructor(pool: WriterPool, options: SeqpacketWriterOptions);
    close(): Promise<void>;
    init(): Promise<void>;
    write(buf: Buffer): Promise<void>;
    connect(): Promise<void>;
}
