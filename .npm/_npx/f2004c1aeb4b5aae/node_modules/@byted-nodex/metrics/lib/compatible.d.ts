import { Client, ClientOptions } from './client';
type ValueType = string | number;
type EmitFn<T = ValueType> = (
/** metric name */
name: string, 
/** metric value */
value: T, 
/** metric name prefix, default to use defaultPrefix */
prefix?: string, 
/** tags */
tags?: object, tagKeys?: string[]) => void;
export declare class CompatClient extends Client {
    constructor(options?: ClientOptions);
    private emitMetric;
    emitCounter: EmitFn;
    emitTimer: EmitFn;
    emitStore: EmitFn;
    emitRateCounter: EmitFn;
    emitMeter: EmitFn;
}
export {};
