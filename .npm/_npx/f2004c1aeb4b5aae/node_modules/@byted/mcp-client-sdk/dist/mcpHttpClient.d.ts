import { AfterCallHook, BeforeCallHook, BytedMcpClientContext, HttpConnection, Tool } from './types.js';
import { AI_PAAS_REGION } from '@byted/ai-paas-env';
export declare class McpHttpClient {
    readonly serverName: string;
    private readonly timeout;
    private httpEndpoint?;
    private readonly psm?;
    private readonly isMeshMode;
    private readonly headers?;
    private readonly params?;
    private beforeCallHooks;
    private afterCallHooks;
    constructor({ serverName, connection, }: {
        serverName: string;
        connection: HttpConnection;
    });
    initUrlWithPsm({ psm, env, region, ctx, }: {
        psm: string;
        env?: string;
        region: AI_PAAS_REGION;
        ctx: BytedMcpClientContext;
    }): Promise<void>;
    listTools({ ctx, }: {
        ctx: BytedMcpClientContext;
    }): Promise<Tool[]>;
    registerBeforeCallHook({ beforeCallHook, ctx, }: {
        beforeCallHook: BeforeCallHook;
        ctx: BytedMcpClientContext;
    }): void;
    clearBeforeCallHook({ ctx }: {
        ctx: BytedMcpClientContext;
    }): void;
    clearAfterCallHook({ ctx }: {
        ctx: BytedMcpClientContext;
    }): void;
    close({ ctx }: {
        ctx: BytedMcpClientContext;
    }): void;
    registerAfterCallHook({ afterCallHook, ctx, }: {
        afterCallHook: AfterCallHook;
        ctx: BytedMcpClientContext;
    }): void;
    callTool(name: string, args: unknown): Promise<unknown>;
}
//# sourceMappingURL=mcpHttpClient.d.ts.map