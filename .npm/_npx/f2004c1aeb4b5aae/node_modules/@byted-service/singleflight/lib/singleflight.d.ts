import { SingleflightOptions } from './types';
export declare class Singleflight<T = any> {
    private keyMap;
    private options;
    constructor(options?: SingleflightOptions<T>);
    do(key: string, fn: () => Promise<T>): Promise<T>;
}
