export declare class PendingPromise<T = void> {
    promise: Promise<PendingPromise<T>>;
    type: string;
    timeout?: number | null;
    isFinaled: boolean;
    value: T;
    reason: Error;
    possibleTimeoutCause: Error | null;
    private _resolve;
    private _reject;
    private timeoutId;
    constructor(type: string, timeout?: number | null);
    resolve(value: T): this;
    reject(reason: Error): this;
}
