import { TCPPoolOptions } from './tcp';
import { UDPPoolOptions } from './udp';
import { DynamicPoolOptions } from './dynamic';
export interface Pool<T> {
    id?: string;
    acquire(...args: any[]): Promise<T>;
    release(resource: T): boolean;
    destroy(): Promise<any>;
}
export interface ILogger {
    fatal(message: string, ...splat: any[]): void;
    error(message: string, ...splat: any[]): void;
    warn(message: string, ...splat: any[]): void;
    info(message: string, ...splat: any[]): void;
    debug(message: string, ...splat: any[]): void;
    trace(message: string, ...splat: any[]): void;
}
export type Unpromisify<T> = T extends Promise<infer R> ? R : T;
/** @deprecated use TCPPoolOptions instead */
export interface TCPOptions extends TCPPoolOptions {
}
/** @deprecated use UDPPoolOptions instead */
export interface UDPOptions extends UDPPoolOptions {
}
/** @deprecated use DynamicPoolOptions instead */
export interface DynamicOptions extends DynamicPoolOptions {
}
