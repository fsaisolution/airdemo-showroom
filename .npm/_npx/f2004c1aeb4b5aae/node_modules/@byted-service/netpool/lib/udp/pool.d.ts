import { UDPOperationOptions, UDPSocket } from './operation';
import { PoolOptions } from '../internal';
import { Pool } from '../typings';
export interface UDPPoolOptions extends UDPOperationOptions, Omit<PoolOptions<UDPSocket>, 'operation'> {
}
/**
 * UDP connection pool
 */
export declare class UDPPool implements Pool<UDPSocket> {
    id: string;
    acquire: (options?: null | number) => Promise<UDPSocket>;
    release: (socket: UDPSocket) => boolean;
    destroy: () => Promise<void>;
    private operation;
    private pool;
    constructor(options: UDPPoolOptions);
}
