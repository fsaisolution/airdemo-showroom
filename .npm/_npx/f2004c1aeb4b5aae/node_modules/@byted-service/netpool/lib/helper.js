"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.genIdCreator = void 0;
const genIdCreator = (prefix, delimiter = '-') => {
    let cache = Date.now();
    let i = 0;
    return (suffix) => {
        const now = Date.now();
        if (now !== cache) {
            i = 0;
            cache = now;
        }
        return [prefix, now, ++i, suffix].filter(Boolean).join(delimiter);
    };
};
exports.genIdCreator = genIdCreator;
