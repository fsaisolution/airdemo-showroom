import { TCPOperationOptions, TCPSocket } from './operation';
import { PoolOptions } from '../internal';
import { Pool } from '../typings';
export interface TCPPoolOptions extends TCPOperationOptions, Omit<PoolOptions<TCPSocket>, 'operation'> {
}
/**
 * TCP connection pool ( duplex )
 */
export declare class TCPPool implements Pool<TCPSocket> {
    id: string;
    acquire: (options?: null | number) => Promise<TCPSocket>;
    release: (socket: TCPSocket) => boolean;
    destroy: () => Promise<void>;
    private operation;
    private pool;
    constructor(options: TCPPoolOptions);
}
