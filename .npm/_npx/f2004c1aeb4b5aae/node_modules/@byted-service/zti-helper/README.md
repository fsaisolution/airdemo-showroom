# zti-jet-helper Node.js 版本

[![bnpm](https://web-bnpm.byted.org/badge/v/@byted-service/zti-helper.svg?style=flat-square)](https://web-bnpm.byted.org/package/@byted-service/zti-helper) [![download](https://2y8ngapf.fn.bytedance.net/d?p=@byted-service/zti-helper)](https://web-bnpm.byted.org/package/@byted-service/zti-helper)

[简体中文](https://code.byted.org/nodejs/byted-service/blob/master/packages/zti-helper/README.md) | [CHANGELOG](https://code.byted.org/nodejs/byted-service/blob/master/packages/zti-helper/CHANGELOG.md)

## 安装

```bash
npm install @byted-service/zti-helper --registry=http://bnpm.byted.org
```

## 快速入门

```ts
// demo.ts
import { ZtiJWTHelper } from '@byted-service/zti-helper';

const test = async () => {
    const zti = new ZtiJWTHelper();

    try {
        const token = await zti.getJwtSVID();
        const decodeRes1 = await zti.decodeGDPRorJwtSVID(token);
        // const decodeRes2 = await zti.decodeJwtSVID(token);

        console.log('token', token);
        console.log('decodeRes1', decodeRes1);
        // console.log('decodeRes2', decodeRes2);
        // 本地开发需要主要会抛出 error
    } catch (error) {
        console.error(error);
    }

    zti.close();
};

test().then(() => {});
// 本地运行 doas -p P.S.M ts-node demo.ts
```

## API

### `constructor(options: { logger?: Logger } = {}): Promise<ZtiJWTHelper>`

创建实例，同时从 zti-agent 或者本地文件获取一次 token 用来初始化，实例也会定时更新缓存。

### `getJwtSVID(): Promise<string>`

获取 token。

### `decodeGDPRorJwtSVID(tokenString: string): LegacyIdentity`

解析 zti token 或者 gdpr token。

### `decodeJwtSVID(tokenString: string): ZeroTrustIdentity`

解析 zti token。

## 相关资料文档

### [什么是零信任系统? What is ZTI?](https://bytedance.feishu.cn/docs/doccnRNDpYqGl0VZRp4AFMmiDng)

### [GDPR Token->ZTI Token 升级指南](https://bytedance.feishu.cn/docs/doccnjhV6ODFFADkBQi3UsWYGWe)

### [用户手册合集 ZTI User Documentation](https://bytedance.feishu.cn/docs/doccn58hQLoZ5UjcB13M0Tmhmsg)
