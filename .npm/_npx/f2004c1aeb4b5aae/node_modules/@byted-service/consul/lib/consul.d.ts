import { ConsulOptions, LookupOptions, Endpoint } from './typings';
export declare class Consul {
    address: string;
    private cache;
    private cacheTime;
    private logger;
    private enableWarning;
    private request;
    constructor(options?: ConsulOptions);
    lookup(psm: string, options?: LookupOptions): Promise<Endpoint[]>;
}
