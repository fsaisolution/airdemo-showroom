import { Response } from 'node-fetch';
import { Consul } from '@byted-service/consul';
import { NodeData, NodesData } from './consts';
import Logger from '@byted-service/logger';
export default class BConfig {
    cluster: string;
    bconfigPSM: string;
    getTimeout: number;
    consul: Consul;
    logger: Logger;
    constructor(logger: Logger, cluster?: string, getTimeout?: number, bconfigPSM?: string);
    /**
     * 从服务端获取单个数据
     * @param key
     * @param versionCode
     */
    getRemote(key: string, versionCode: string): Promise<NodeData>;
    multiGetRemote(nodes: Array<{
        node: string;
        v: string;
    }>): Promise<NodesData[]>;
    getFromServer(key: string, versionCode: string): Promise<NodeData>;
    decodeNodeResp(res: Response): Promise<NodeData>;
    decodeNodesResp(res: Response): Promise<NodesData[]>;
    emit(t0: number, err: Error | null, api: string): Promise<void>;
    private lookupBConfigProxy;
}
