import CacheNode from './cacheNode';
import MetricsEmiter from './emiter';
import Logger from '@byted-service/logger';
export interface TccGeneralOptions {
    /**
     * serviceName
     */
    serviceName: string;
    /**
     * cluster 配置项，默认为 `default`
     */
    cluster?: string;
    /**
     * 缓存时间，单位为 ms，最少为 3000ms，默认为 6000ms
     */
    cacheTime?: number;
    /**
     * 缓存击穿时，从远端获取的超时时间，单位为ms，默认为 200ms
     */
    getTimeout?: number;
    /**
     * 禁止输出 log
     */
    disableLog?: boolean;
}
export default class TccGeneral {
    /**
     * 创建实例
     * @param options
     */
    static create(options: TccGeneralOptions): TccGeneral;
    protected serviceName: string;
    protected cluster: string;
    protected cacheTime: number;
    protected getTimeout: number;
    protected cache: CacheNode;
    protected metricsEmiter: MetricsEmiter;
    protected logger: Logger;
    protected constructor(options: TccGeneralOptions);
    /**
     * 获取普通配置节点
     * @param nodeName
     */
    get(nodeName: string): Promise<{
        data: any;
        versionCode: string;
    }>;
    /**
     * 获取组配置节点
     * @param nodeName
     * @param key
     */
    getGroupKey(nodeName: string, key: string): Promise<{
        data: any;
        versionCode: string;
    }>;
    protected getWithCache(nodeName: string, key: string, mode: number): Promise<{
        data: any;
        versionCode: string;
    }>;
}
