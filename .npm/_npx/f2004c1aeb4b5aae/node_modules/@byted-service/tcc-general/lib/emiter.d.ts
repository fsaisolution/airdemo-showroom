import { GetType } from './consts';
import TCCError from './error';
interface EmiterItem {
    succ: number;
    err: number;
    notFound: number;
}
declare enum EmitStatus {
    statusSucc = "success",
    statusErr = "failed",
    statusNotFound = "notFound"
}
export default class MetricsEmiter {
    serviceName: string;
    cluster: string;
    notFoundItem: EmiterItem;
    cache: Map<string, EmiterItem>;
    constructor(serviceName: string, cluster: string);
    emit(err: TCCError | null, configKey: string, getType: GetType): Promise<void>;
    emitCache(): Promise<void>;
    doEmit({ status, nodeName, getType, count, }: {
        status: EmitStatus;
        nodeName: string;
        getType: GetType;
        count: number;
    }): Promise<void>;
}
export {};
