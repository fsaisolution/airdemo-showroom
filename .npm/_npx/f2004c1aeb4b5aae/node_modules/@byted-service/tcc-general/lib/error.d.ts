export declare enum TccErrorCode {
    errKeyNotFound = 100,
    errRemoteFailed = 500,
    errNotModified = 304,
    errOther = 0,
    errInternal = 1,
    errInvalidNodeMode = 2,
    errGetException = 3
}
export default class TCCError extends Error {
    code: number;
    constructor(code: TccErrorCode, msg?: string);
}
