import { NodeOptions, NodeData } from './consts';
import TCCError from './error';
export default class Node {
    nodeName: string;
    data: NodeData | undefined;
    lastErr: TCCError | undefined;
    cacheTime: number;
    expiredAt: number;
    lastAccessAt: number;
    constructor(options: NodeOptions);
    getValue(key: string, mode: number): {
        data: any;
        versionCode: string;
    };
    getVersionCode(): string;
    getCacheTime(): number;
    expired(): boolean;
    renew(): void;
    needRefresh(refreshInterval: number): boolean;
}
