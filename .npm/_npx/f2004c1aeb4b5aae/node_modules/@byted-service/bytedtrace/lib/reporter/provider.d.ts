/// <reference types="node" />
/**
 * 数据上报到 agent
 */
import * as gloablTypes from '../types';
import { AgentClient } from './client';
import { StatusCounter } from '../status';
import { Logger } from '@byted-service/logger';
export declare class AgentProvider {
    queue: Buffer[];
    queueLen: number;
    statusCounter: StatusCounter;
    status: gloablTypes.AgentProviderStatus;
    retryStatus: gloablTypes.AgentProviderStatus;
    sender: AgentClient;
    constructor(options: {
        queueLen: number;
        statusCounter: StatusCounter;
        logger: Logger;
    });
    write(data: Buffer): void;
    run(): Promise<void>;
    flush(data: Buffer, needRetry: boolean): Promise<void>;
    close(): void;
}
