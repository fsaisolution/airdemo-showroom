"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.AccumulateIdGenerator = void 0;
class AccumulateIdGenerator {
    constructor(options) {
        this.randomNumber = options.randomNumber;
        this.seed = options.randomNumber();
        this.maxId = options.maxId;
        this.delta = options.delta;
    }
    genId() {
        const id = this.addAndGet();
        if (id >= this.maxId) {
            this.seed = this.randomNumber();
            return this.genId();
        }
        return id;
    }
    addAndGet() {
        this.seed += this.delta;
        return this.seed;
    }
}
exports.AccumulateIdGenerator = AccumulateIdGenerator;
