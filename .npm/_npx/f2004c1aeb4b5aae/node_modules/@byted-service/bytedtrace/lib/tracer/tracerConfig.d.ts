import * as configProvider from '../confProvider';
import * as types from '../configsClient/types';
interface ConfigOptions {
    /**
     * 全局总开关
     */
    enableTrace: boolean;
    /**
     * PostTrace是否生效
     */
    enablePostTrace: boolean;
    /**
     * 根据header进行染色是否生效
     */
    enableDyedHeader: boolean;
    /**
     * 是否发起主动采样
     */
    enableSampling: boolean;
    /**
     * 是否服务内全量上报span
     */
    enableReportTotalSpan: boolean;
    /**
     * ServerSpan和ClientSpan metrics的级别
     * 0: 全量tag模式
     * 1: 精简tag模式
     * 2: 关闭模式
     */
    spanMetricsLevel: number;
    /**
     * 其他的metrics控制
     * 0: 打开模式
     * 1: 关闭模式
     */
    customMetricsLevel: number;
    /**
     * 精确到component粒度的轻量模式控制
     */
    lightModeByComponent: ControlForComponent;
    /**
     * 精确到component粒度的旧版指标控制
     */
    disableThriftStyleMetricsByComponent: ControlForComponent;
}
export declare class ControlForComponent {
    base: Record<string, boolean>;
    global: Record<string, boolean>;
    constructor(base: any, global: any);
    getByKey(key: string): boolean;
}
export declare class TracerConfig {
    config: ConfigOptions;
    configurator: types.IConfigurator;
    constructor(configurator: types.IConfigurator);
    /**
     * 先拉取 tcc 配置然后初始化
     */
    syncConfig(): Promise<void>;
    /**
     * 初始化 tracer 时，先试用默认配置，然后从 tcc 拉取配置
     */
    asyncConfig(): void;
    init(cf: configProvider.GlobalTracerConfig): void;
    enablePostTrace(): boolean;
    enableDyedHeader(): boolean;
    enableSampling(): boolean;
    enableReportTotalSpan(): boolean;
    enableTrace(): boolean;
    getSpanMetricsLevel(): number;
    isLightModeEnable(component: string): boolean;
    isThriftStyleMetricsDisable(component: string): boolean;
    isSimplifySpanMetrics(): boolean;
    isMetricsEnable(custom: boolean): boolean;
}
export {};
