import { Logger } from '@byted-service/logger';
export declare class AsyncCache {
    private cache;
    private fetcher;
    private isSame;
    private changeHandler;
    private logger;
    constructor(options: {
        fetcher: () => Promise<any>;
        isSame: (oldValue: any, newValue: any) => boolean;
        changeHandler: (newValue: any) => void;
        logger: Logger;
    });
    setDefault(key: string, val: any): void;
    get(key: string, force?: boolean): Promise<any>;
    getOrSet(key: string, def: any): Promise<any>;
    dump(): {};
    refresh(): Promise<void>;
}
