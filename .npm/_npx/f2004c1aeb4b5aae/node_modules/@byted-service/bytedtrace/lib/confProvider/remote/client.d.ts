import tcc from '@byted-service/tcc-general';
export declare class TCCClient {
    static tccServicePSM: string;
    static tccServiceCluster: string;
    static tccServiceFramework: string;
    static defaultClient: TCCClient;
    /**
     * 在同一个进程中 psm cluster framework 应该是一致的
     * init default wrapped client with meta info.
     * @param psm
     * @param cluster
     * @param framework
     */
    static init(psm: string, cluster: string, framework: string): void;
    static getDefaultClient(): Promise<TCCClient>;
    private static sf;
    private static newClient;
    tccNodeNameList: string[];
    tccClient: tcc;
    private constructor();
    getTccConfigByKey(key: string): Promise<any>;
    private getWithRetry;
}
