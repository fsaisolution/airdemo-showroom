import { ClientOptions } from '@byted-nodex/metrics';
type EmitFuntion = (name: string, value: any, prefix?: string, tags?: object, tagKeys?: string[]) => void;
export declare class ByteTraceMetrics {
    globalTags: object;
    emitCounter: EmitFuntion;
    emitStore: EmitFuntion;
    emitMeter: EmitFuntion;
    private client;
    private clientTimerMetric;
    private serverTimerMetric;
    private serverRecvSizeRateCounter;
    private serverSendSizeRateCounter;
    private clientRecvSizeRateCounter;
    private clientSendSizeRateCounter;
    private serverRateCounter;
    private clientRateCounter;
    constructor(options?: Partial<ClientOptions>, globalTags?: object);
    emitTimer(name: string, value: any, prefix?: string, tags?: object, tagKeys?: string[]): void;
    emitRateCounter(name: string, value: any, prefix?: string, tags?: object, tagKeys?: string[]): void;
    private _emitTimer;
    private _emitRateCounter;
}
export {};
