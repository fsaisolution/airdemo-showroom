/// <reference types="node" />
import { SamplerConfig } from '../confProvider';
import { StatusCounter } from '../status';
export declare const minExpireTimeSecond = 60;
export declare class LowerBoundSampler {
    methodSet: Set<string>;
    maxMethodCount: number;
    expireSecond: number;
    minExpireTimeSecond: number;
    statCounter: StatusCounter;
    intervalId: NodeJS.Timeout;
    random: any;
    constructor(minExpireTimeSecond: number, statCounter: StatusCounter);
    sampled(method: string): boolean;
    start(): void;
    close(): void;
    syncConfig(config: SamplerConfig): void;
    private genExpireTime;
    private setExpireTime;
    private resetTimer;
}
export declare class NoopLowerBoundSampler {
    sampled(): boolean;
    start(): void;
    close(): void;
    syncConfig(): void;
}
