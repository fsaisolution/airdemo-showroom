/// <reference types="node" />
import * as types from './types';
import { WindowCounter } from './counter';
export declare class QpsCatcher {
    /**
     * 最大窗口数，大于此数将不会继续生成窗口
     */
    maxOperationNum: number;
    /**
     * 窗口的时间跨度ms
     */
    windowDuringMs: number;
    /**
     * 窗口存储桶的数量
     */
    windowNum: number;
    /**
     * 当前窗口可以存放的数量
     */
    windowCounterNum: number;
    /**
     * 过期时间
     */
    windowExpireTimeMs: number;
    /**
     * 存储窗口
     */
    windowCounters: Map<string, WindowCounter>;
    ticker: Ticker;
    /**
     * 窗口总的时间跨度,秒
     */
    windowsTimeSpan: number;
    isNoop: boolean;
    constructor(isNoop?: boolean, options?: types.QpsCatcherOption);
    inc(operationName: string, delta: number): void;
    incAndGetQps(operationName: string, delta: number): number;
    getQps(operationName: string): number;
    tickHandler(): void;
    tick(): void;
    close(): void;
    private getWindowCounter;
    private applyOne;
}
declare class Ticker {
    duration: number;
    callback: any;
    id: NodeJS.Timeout | undefined;
    constructor(duration: number, callback: any);
    tick(): void;
    stop(): void;
}
export {};
