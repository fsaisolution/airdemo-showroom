export declare class WindowCounter {
    /**
     * bucket可存储的数量
     */
    bucketNum: number;
    /**
     * 在窗口中的bucket的数量
     */
    inWindow: number;
    /**
     * 在时间窗口内的计数值
     */
    buckets: number[];
    count: number;
    oldIndex: number;
    /**
     * 当前存储桶在存储桶数组中的下标
     */
    nowIndex: number;
    tokenNum: number;
    maxTokenNum: number;
    constructor(bucketNum: number, maxTokenNum: number);
    /**
     * 打一次点
     * @param n
     */
    add(n: number): void;
    /**
     * 获取打点量
     * @returns
     */
    get(): number;
    /**
     * 首先获取打点量再打点
     * @param n
     * @returns
     */
    getAndAdd(n: number): number;
    zero(): void;
    tick(): void;
    incNowIndex(): void;
    charge(): void;
    cut(): void;
    /**
     * 返回是否过期
     * @returns
     */
    shouldExpired(): boolean;
}
