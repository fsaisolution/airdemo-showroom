import * as confProvider from '../confProvider';
export interface IConfigurator {
    pullGlobalTracerConfig(force?: boolean): Promise<confProvider.GlobalTracerConfig>;
    pullBreakerConfig(force?: boolean): Promise<confProvider.CBConfig>;
    pullSamplerConfig(force?: boolean): Promise<confProvider.SamplerConfig>;
    register(key: string, f: (config: any) => any): any;
    syncInit(): Promise<void>;
    asyncInit(): void;
    close(): void;
}
export type voidFun = (config: any) => void;
export interface LimiterConfigUnit {
    /**
     * Span的类型: server/client/custom
     */
    spanType: string;
    /**
     *  Span的名字
     */
    spanName: string;
    /**
     * 每分钟最多命中次数
     */
    hitPerMinute: number;
}
