import * as types from '../configsClient/types';
export declare class BreakerConfigurator {
    configurator: types.IConfigurator;
    /**
     * 单个transaction中span个数上限
     */
    maxSpanCountInTxn: number;
    /**
     * 单个transaction/span/event/metrics中tagKv上限
     */
    maxTagKVCountInObj: number;
    /**
     * 单个span中event个数上限
     */
    maxEventCountInSpan: number;
    /**
     *  单个span中metrics个数上限
     */
    maxCustomMetricCountInSpan: number;
    /**
     * 单个tagKv中value的长度上限
     */
    maxTagValueLen: number;
    /**
     * 单个event中data的长度上限
     */
    maxEventDataLen: number;
    /**
     * 单个transaction序列化后大小上限(byte)
     */
    maxTxnDataLen: number;
    /**
     * Txn各接口采样速率熔断值 float64
     */
    maxTxnSamplingRatePerOperation: number;
    /**
     * PostTrace各接口采样速率熔断值 float64
     */
    maxPostTraceSamplingRatePerOperation: number;
    /**
     * Span层面频控细分配置
     */
    postTraceLimitConfigs: types.LimiterConfigUnit[];
    /**
     * Dyeing 各接口采样速率熔断值 float64
     */
    maxDyeingSamplingRatePerOperation: number;
    constructor(configurator: types.IConfigurator);
    syncConfig(): Promise<void>;
    asyncConfig(): void;
    private init;
}
