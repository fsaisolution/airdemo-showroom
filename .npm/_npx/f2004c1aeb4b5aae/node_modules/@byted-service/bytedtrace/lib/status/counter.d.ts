import * as sampler from '../sampler';
import { Logger } from '@byted-service/logger';
import { ByteTraceMetrics } from '../metrics';
export declare class StatusCounter {
    enableCountingRate: boolean;
    counter: sampler.QpsCatcher;
    logger: Logger;
    metrics: ByteTraceMetrics;
    limiterPack: sampler.LimiterPack;
    debugMode: boolean;
    constructor(logger: Logger, metricsClient: ByteTraceMetrics, enableCountingRate: boolean);
    /**
     * SDK 内部发生错误时进行 metrics 打点
     *
     * Tracer internal error metrics.
     * 'forceLog': if true, this log will be emitted whether is in debug mode.
     *  Format: bytedtrace.sdk.internal.error.rate{type=*}.
     * @param forceLog
     * @param location
     * @param formate
     * @param logs
     */
    recordWarningWithRateLimit(forceLog: boolean, location: string, formate: string, ...logs: any[]): void;
    inc(name: string, delta: number, enableEmitMetrics: boolean): Promise<void>;
    getRate(name: string): number;
    close(): void;
}
