import * as gTypes from '../types';
import * as gConsts from '../consts';
import * as bytedtracePb from '../gen/bytedtrace_pb';
import { Breaker } from '../breaker/breaker';
interface Options {
    tags: gTypes.TagKVs;
    eventType: string;
    name: string;
    logLevel: gConsts.LogLevels;
    content?: string;
}
export declare class TraceEvent {
    /**
     * event 类型
     */
    private eventType;
    /**
     * event 名字
     */
    private name;
    /**
     * tags 信息
     */
    private tags;
    /**
     * event 发生时间
     */
    private startTime;
    /**
     * envet 内容
     */
    private content;
    /**
     * 是否打印 log
     */
    private emitLog;
    /**
     * 是否进行 metrics 打点
     */
    private emitMetrics;
    /**
     * 输出日志的级别
     */
    private logLevel;
    /**
     * 记录调用层级，给 log 定位
     */
    private callDepth;
    constructor(options: Options);
    /**
     * 返回 event 发生时间
     * @returns
     */
    getStartTime(): number;
    /**
     * 返回 event 描述内容
     * @returns
     */
    getContent(): string;
    /**
     * 返回 event 类型
     * @returns
     */
    getEventType(): string;
    /**
     * 返回 tags 内容，这里返回新的对象
     * @returns
     */
    getTags(): gTypes.TagKVs;
    /**
     * 返回 log level
     * @returns
     */
    getLogLevel(): gConsts.LogLevels;
    /**
     * 返回 event 名字
     * @returns
     */
    getName(): string;
    /**
     * 返回是否打印 log 的判断标志值
     * @returns
     */
    getEmitLog(): boolean;
    /**
     * 设置一对 key-value 到 tags 中
     * @param key
     * @param value
     * @returns
     */
    setTag(key: string, value: any): this;
    /**
     * 设置多个 key-value 到 tags 中
     * @param tags
     * @returns
     */
    setTags(tags: gTypes.TagKVs): this;
    /**
     * 设置是否打印 log
     * @param e
     * @returns
     */
    setEmitLog(e: boolean): this;
    /**
     * 增加 callDepth
     * @param depth
     * @returns
     */
    incCallDepthForLog(depth: number): this;
    /**
     * 返回 CallDepth 值
     * @returns
     */
    getCallDepth(): number;
    /**
     * 设置是否进行 metrics 打点
     * @param e
     * @returns
     */
    setEmitMetrics(e: boolean): this;
    /**
     * 获取是否进行 metrics 打点的状态值
     * @returns
     */
    getEmitMetrics(): boolean;
    /**
     * 设置描述内容
     * @param content
     * @returns
     */
    setContent(content: string): this;
    /**
     * 设置发生时间戳
     * @param time
     * @returns
     */
    setTimestamp(time: number): this;
    tranToPb(breaker: Breaker): bytedtracePb.EventProto;
}
export {};
