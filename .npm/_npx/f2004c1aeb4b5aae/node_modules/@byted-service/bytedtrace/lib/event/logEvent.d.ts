import { TraceEvent } from './event';
export declare class LogEvent {
    /**
     * 返回输出 log level 为 info 的 TraceEvent 实例
     * @param name
     * @returns
     */
    static createInfoLogEvent(name: string): TraceEvent;
    /**
     * 返回输出 log level 为 warn 的 TraceEvent 实例
     * @param name
     * @returns
     */
    static createWarnLogEvent(name: string): TraceEvent;
    /**
     * 返回输出 log level 为 error 的 TraceEvent 实例
     * @param name
     * @returns
     */
    static createErrorLogEvent(name: string): TraceEvent;
    /**
     * 返回输出 log level 为 fatal 的 TraceEvent 实例
     * @param name
     * @returns
     */
    static createFatalLogEvent(name: string): TraceEvent;
    /**
     * 返回输出 log level 为 LogLevels.LEVEL_NO_DEFINE 的 TraceEvent 实例
     * @param eventType
     * @param name
     * @returns
     */
    static createLogEvent(eventType: string, name: string): TraceEvent;
    private static _createLogEvent;
}
