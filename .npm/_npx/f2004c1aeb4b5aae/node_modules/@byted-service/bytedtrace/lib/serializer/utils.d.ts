/**
 * TODO 这里可能会非常频繁的创建 buffer 需要考虑池化
 * 调用 serialize 的方法时单线程操作，不存在异步，
 * 所以提前申请 buffer 用完 clear 应该也是可行的
 */
/// <reference types="node" />
/**
 * 创建 8 byte 的 buffer
 * @returns
 */
export declare const getBuffer8Byte: () => Buffer;
/**
 * 创建 4 byte 的 buffer
 * @returns
 */
export declare const getBuffer4Byte: () => Buffer;
export declare const int64ToBytes: (long: string | number, bigEndian: boolean) => Buffer;
export declare const uint64ToBytes: (long: string, bigEndian: boolean) => Buffer;
export declare const intToBytes: (value: number, bigEndian: boolean) => Buffer;
/**
 * int32 转成 uint32 后再转成 buffer
 * @param value
 * @param bigEndian
 * @returns
 */
export declare const int32ToBytes: (value: number, bigEndian: boolean) => Buffer;
/**
 * uint32 转成 buffer
 * @param value
 * @param bigEndian
 * @returns
 */
export declare const uint32ToBytes: (value: number, bigEndian: boolean) => Buffer;
/**
 * float64 转成 buffer
 * @param value
 * @param bigEndian
 * @returns
 */
export declare const float64ToBytes: (value: number, bigEndian: boolean) => Buffer;
/**
 * 从 start(包括start) 开始填充0
 * @param buffer
 * @param start
 * @returns
 */
export declare const fillBufferWith0: (buffer: Buffer, start: number) => Buffer;
/**
 * 转成十进制字符串
 * @param buff
 * @returns
 */
export declare const bufferToInt: (buff: Buffer) => string;
/**
 * 将 buffer append 到 target 中，从 start 这个点开始
 * @param target
 * @param buffer
 * @param start 起点
 */
export declare const appendToTarget: (target: Buffer, buffer: Buffer, start?: number) => void;
