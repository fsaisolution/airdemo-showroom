import { URL } from 'url';
import { FetchOptions, ProxyAgent } from './typings';
export declare const proxyAgent: (url: URL, options: FetchOptions) => ProxyAgent | undefined;
/**
 * 只兼容以下集中格式
 * - /path/to/xx.sock
 * - 127.0.0.1:3306
 * - [::]:3306
 */
export declare const parseAddr: (addr: string) => {
    host: string;
    port: string;
    socketPath: string;
};
export declare const encodeUnicodeText: (text: string) => string;
