import { LogagentV2, LogAgentOptionsType } from '@byted-service/logagent';
import { InputData, LoggerOptionsType } from '../typings';
declare module '../typings' {
    interface LoggerOptionsType {
        /**
         * `logagent` 构造参数
         */
        agent?: LogAgentOptionsType;
        /**
         * `logagent` 日志类型标示，`_rpc` 代表 `rpc` 日志，默认为 `options.psm`
         */
        taskName?: string;
    }
}
export declare const createLogAgent: (options?: LoggerOptionsType) => {
    logAgent: LogagentV2;
    write(_: string, inputData: InputData): void;
    flush(): void;
};
