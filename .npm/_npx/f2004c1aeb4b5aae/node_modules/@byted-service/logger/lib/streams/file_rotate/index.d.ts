import { FileRotateStream, FileRotateStreamOptions } from './file_rotate_stream';
import { LoggerOptionsType } from '../../typings';
declare module '../../typings' {
    interface LoggerOptionsType extends Omit<FileRotateStreamOptions, 'file'> {
        /**
         * 日志文件，为数组时，将逐个尝试创建文件，但仅写入一个
         */
        file?: string | string[];
        /**
         * 日志存放目录，当 `env.isTest() || env.isProd()` 为 `true` 时 ( 本地开发环境为 `false` )，默认为 `/opt/tiger/toutiao/log/app`，否则默认为 `${process.cwd()}/log`
         */
        dir?: string;
        /**
         * 日志文件名称，默认为 `options.psm`
         */
        filename?: string;
        /**
         * 日志文件后缀，默认为 `.log`
         * - 如果是访问日志，则填写 `.access.log`
         * - 如果是 `rpc` 日志，则填写 `.call.log`
         */
        ext?: string;
        /**
         * 日志目录创建失败后的尝试目录，默认为 `` [`${process.cwd()}/log`, `${os.tmpdir()}/log`] ``，若全部失败，则创建日志实例失败
         */
        fallbackDirs?: string[];
        /**
         * 是否关闭日志目录创建失败时的输出，默认为 `false`
         */
        disableDirFallbackWarning?: boolean;
    }
}
export { FileRotateStream, FileRotateStreamOptions };
export declare const createFileRotate: (options?: LoggerOptionsType) => FileRotateStream;
