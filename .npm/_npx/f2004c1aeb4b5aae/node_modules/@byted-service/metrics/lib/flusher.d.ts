export interface Flushable {
    flush(): Promise<void>;
}
export declare class Flusher {
    private flushableMap;
    private intervals;
    register(key: number, flushable: Flushable): void;
    close(): void;
    private handle;
}
