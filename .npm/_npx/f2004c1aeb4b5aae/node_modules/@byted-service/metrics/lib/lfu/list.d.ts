import type { Node } from './node';
export declare class List {
    header: Node;
    length: number;
    constructor();
    insertHeader(node: Node): void;
    insertBefore(node: Node, cur: Node): void;
    insertAfter(node: Node, cur: Node): void;
    remove(node: Node): void;
}
