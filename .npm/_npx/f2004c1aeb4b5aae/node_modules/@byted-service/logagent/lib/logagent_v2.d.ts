/// <reference types="node" />
import { LogagentV2Options } from './typings';
/**
 * 目前还在试验阶段，外部请勿使用
 */
export declare class LogagentV2 {
    psm: string;
    length: number;
    private flushInterval;
    private maxLength;
    private logger;
    private metrics;
    private msgs;
    private sendMsg;
    constructor(options?: LogagentV2Options);
    send(msg: string | Buffer, tags?: {
        [key: string]: any;
    }, tagKeys?: string[]): void;
    flush(): void;
}
