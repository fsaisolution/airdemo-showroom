/**
 * 文档地址：https://docs.bytedance.net/doc/lQkZlmuWeu7fZKVXlG8cyh
 * 参考代码：https://code.byted.org/log_market/gosdk
 */
import { LogAgentMessageType, LogAgentOptionsType } from './typings';
export declare class LogAgent {
    private messages;
    private sendBuffer;
    private timer;
    private psm;
    private sendInterval;
    private maxLen;
    private packLen;
    private nextSendTime;
    private logger;
    private metrics;
    constructor(options?: LogAgentOptionsType);
    /**
     * 立即发送所有待发送的日志
     */
    flush(): void;
    /**
     * 检查是当前时间点是否应该发送待发送的日志；如果没有发现定时器的话，会创建一个定时器来发送日志
     */
    checkTimeout(): void;
    /**
     * 可以通过两种方式来调用接口，如果只需要指定taskName，则：
     *
     * ```js
     * logagent.send('MESSAGE_CONTENT_HERE')
     * ```
     *
     * 如果定义tag，可以传入一个LogAgentMessageType结构的数据：
     *
     * ```js
     * logagent.send({
     *   msg: 'MESSAGE_CONTENT_HERE',
     *   tags: {
     *     _level: 'info',
     *   }
     * })
     * ```
     *
     * @param taskName
     * @param msg
     */
    send(taskName: string | LogAgentMessageType, msg?: LogAgentMessageType): void;
    /**
     * @private
     * @param taskName
     * @param originMsg
     */
    private _encode;
}
