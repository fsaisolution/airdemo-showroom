import Metrics from '@byted-service/metrics';
import { LogagentV2 } from './logagent_v2';
export declare const wrapMetrics: (metrics: Metrics, logagent: LogagentV2) => {
    success(msgs: Uint8Array[]): void;
    encodeError(): void;
    packetTooLongError(): void;
    sendHttpError(msgs: Uint8Array[]): void;
    sendSocketError(msgs: Uint8Array[]): void;
};
