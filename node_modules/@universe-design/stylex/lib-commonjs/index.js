"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
var _exportNames = {};
exports.default = void 0;
var _stylexTypes = require("@universe-design/stylex-types");
Object.keys(_stylexTypes).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (Object.prototype.hasOwnProperty.call(_exportNames, key)) return;
  if (key in exports && exports[key] === _stylexTypes[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function () {
      return _stylexTypes[key];
    }
  });
});
var _merge = _interopRequireDefault(require("lodash/merge"));
var _flattenDeep = _interopRequireDefault(require("lodash/flattenDeep"));
var _tinyInvariant = _interopRequireDefault(require("tiny-invariant"));
const isTruthy = o => Boolean(o);
const _traverseCSSClassNameObject = (cssClassNameObject, cb) => {
  for (const v of Object.values(cssClassNameObject)) {
    if (typeof v === 'string') {
      cb(v);
      continue;
    }
    _traverseCSSClassNameObject(v, cb);
  }
};
const stylex = function () {
  for (var _len = arguments.length, instances = new Array(_len), _key = 0; _key < _len; _key++) {
    instances[_key] = arguments[_key];
  }
  const flatInstances = (0, _flattenDeep.default)(instances);
  const alias = flatInstances.filter(isTruthy).map(o => o[_stylexTypes.StylexInstancePropNameAlias]).filter(isTruthy).join(' ');
  const mergedCSSClassNameObject = stylex.compose(...flatInstances);
  const classNames = alias ? [alias] : [];
  _traverseCSSClassNameObject(mergedCSSClassNameObject[_stylexTypes.StylexInstancePropNameProps], className => {
    classNames.push(className);
  });
  return classNames.join(' ');
};
stylex.create = () => {
  process.env.NODE_ENV !== "production" ? (0, _tinyInvariant.default)(false, 'stylex.create calls should be compiled away') : (0, _tinyInvariant.default)(false);
};
stylex.compose = function () {
  const mergedCSSClassNameObject = {};
  for (var _len2 = arguments.length, instances = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
    instances[_key2] = arguments[_key2];
  }
  const flatInstances = (0, _flattenDeep.default)(instances);
  for (const instance of flatInstances) {
    if (!instance) {
      continue;
    }
    (0, _merge.default)(mergedCSSClassNameObject, instance[_stylexTypes.StylexInstancePropNameProps]);
  }
  return {
    props: mergedCSSClassNameObject
  };
};
var _default = exports.default = stylex;
//# sourceMappingURL=index.js.map
