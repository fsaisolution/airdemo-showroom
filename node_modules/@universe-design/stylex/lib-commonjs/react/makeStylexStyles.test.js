"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault").default;
var _u10akqe = _interopRequireDefault(require("./../.stylex/u10akqe5"));
var _reactHooks = require("@testing-library/react-hooks");
var _stylex = _interopRequireDefault(require("@universe-design/stylex"));
var _makeStylexStyles = require("./makeStylexStyles");
//.ue3e8s9{color:red;}
describe('makeStylexStyles', () => {
  test('styles should be correct', () => {
    const useStyles = (0, _makeStylexStyles.makeStylexStyles)(_ref => {
      let {
        prefix
      } = _ref;
      return {
        base: {
          "props": {
            "sj55zd": _u10akqe.default
          },
          "alias": prefix
        }
      };
    });
    const wrapper = (0, _reactHooks.renderHook)(useStyles, {
      initialProps: {
        prefix: 'ud'
      }
    });
    expect(wrapper.result.current).toStrictEqual({
      base: {
        alias: 'ud',
        props: {
          sj55zd: 'ue3e8s9'
        }
      }
    });
    wrapper.unmount();
  });
  test('styles should not change with same prefix', () => {
    const useStyles = (0, _makeStylexStyles.makeStylexStyles)(_ref2 => {
      let {
        prefix
      } = _ref2;
      return {
        base: {
          "props": {
            "sj55zd": _u10akqe.default
          },
          "alias": prefix
        }
      };
    });
    const wrapper = (0, _reactHooks.renderHook)(useStyles, {
      initialProps: {
        prefix: 'ud'
      }
    });
    wrapper.rerender();
    expect(wrapper.result.all[0] === wrapper.result.all[1]).toBe(true);
    wrapper.unmount();
  });
  test('styles should change with different prefix', () => {
    const useStyles = (0, _makeStylexStyles.makeStylexStyles)(_ref3 => {
      let {
        prefix
      } = _ref3;
      return {
        base: {
          "props": {
            "sj55zd": _u10akqe.default
          },
          "alias": prefix
        }
      };
    });
    const wrapper = (0, _reactHooks.renderHook)(useStyles, {
      initialProps: {
        prefix: 'ud'
      }
    });
    wrapper.rerender({
      prefix: 'udx'
    });
    expect(wrapper.result.current).toStrictEqual({
      base: {
        alias: 'udx',
        props: {
          sj55zd: 'ue3e8s9'
        }
      }
    });
    expect(wrapper.result.all[0] !== wrapper.result.all[1]).toBe(true);
    wrapper.unmount();
  });
});
//# sourceMappingURL=makeStylexStyles.test.js.map
