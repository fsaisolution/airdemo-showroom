"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.makeStylexStyles = void 0;
var _memoize = _interopRequireDefault(require("lodash/memoize"));
/**
 * 创建带有缓存的 stylex styles
 * @param creator - stylex styles 的创建函数
 * @returns React hook - useStylexStyles
 * @example
 * ```
 * const useStylexStyles = makeStylexStyles(({ prefix }) => ({
 *   base: stylex.create({
 *     color: 'red'
 *   }, prefix)
 * }));
 *
 * const C = () => {
 *   const cssScope = useCssScope('component');
 *   const styles = useStylexStyles({ prefix: cssScope() }};
 * }
 * ```
 */
const makeStylexStyles = creator => {
  const memoizedCreator = (0, _memoize.default)(creator, _ref => {
    let {
      prefix
    } = _ref;
    return `${prefix}`;
  });
  return _ref2 => {
    let {
      prefix
    } = _ref2;
    const styles = memoizedCreator({
      prefix
    });
    return styles;
  };
};
exports.makeStylexStyles = makeStylexStyles;
//# sourceMappingURL=makeStylexStyles.js.map
