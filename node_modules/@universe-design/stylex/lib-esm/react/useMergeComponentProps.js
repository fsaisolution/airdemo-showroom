import clsx from 'clsx';
import * as React from 'react';
/**
 * 合并用户传入的 props 和需要合并的 props
 */
export const useMergeComponentProps = (userProps, propsToMerge) => ({
  ...userProps,
  className: clsx(propsToMerge.className, userProps.className),
  style: React.useMemo(() => ({
    ...propsToMerge.style,
    ...userProps.style
  }), [propsToMerge.style, userProps.style]),
  sx: React.useMemo(() => [propsToMerge.sx, userProps.sx], [propsToMerge.sx, userProps.sx])
});
//# sourceMappingURL=useMergeComponentProps.js.map
