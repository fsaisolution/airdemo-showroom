type StylexStylesCreator<T> = ({ prefix }: {
    prefix: string;
}) => T;
type UseStylexStyles<T> = ({ prefix }: {
    prefix: string;
}) => T;
/**
 * 创建带有缓存的 stylex styles
 * @param creator - stylex styles 的创建函数
 * @returns React hook - useStylexStyles
 * @example
 * ```
 * const useStylexStyles = makeStylexStyles(({ prefix }) => ({
 *   base: stylex.create({
 *     color: 'red'
 *   }, prefix)
 * }));
 *
 * const C = () => {
 *   const cssScope = useCssScope('component');
 *   const styles = useStylexStyles({ prefix: cssScope() }};
 * }
 * ```
 */
export declare const makeStylexStyles: <T>(creator: StylexStylesCreator<T>) => UseStylexStyles<T>;
export {};
//# sourceMappingURL=makeStylexStyles.d.ts.map