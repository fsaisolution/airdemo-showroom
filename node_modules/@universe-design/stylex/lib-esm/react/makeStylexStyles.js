import memoize from 'lodash/memoize';
/**
 * 创建带有缓存的 stylex styles
 * @param creator - stylex styles 的创建函数
 * @returns React hook - useStylexStyles
 * @example
 * ```
 * const useStylexStyles = makeStylexStyles(({ prefix }) => ({
 *   base: stylex.create({
 *     color: 'red'
 *   }, prefix)
 * }));
 *
 * const C = () => {
 *   const cssScope = useCssScope('component');
 *   const styles = useStylexStyles({ prefix: cssScope() }};
 * }
 * ```
 */
export const makeStylexStyles = creator => {
  const memoizedCreator = memoize(creator, _ref => {
    let {
      prefix
    } = _ref;
    return `${prefix}`;
  });
  return _ref2 => {
    let {
      prefix
    } = _ref2;
    const styles = memoizedCreator({
      prefix
    });
    return styles;
  };
};
//# sourceMappingURL=makeStylexStyles.js.map
