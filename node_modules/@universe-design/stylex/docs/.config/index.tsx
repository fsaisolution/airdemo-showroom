/* eslint-disable no-console */
import * as React from 'react';
import * as ReactDOM from 'react-dom';
import { App } from '@universe-design/site-template';
import routes from 'site-cli-route-config';

ReactDOM.render(<App routes={routes} isHeaderHidden />, document.getElementById('root'));
