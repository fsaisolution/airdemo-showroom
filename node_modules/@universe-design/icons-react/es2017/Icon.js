import _extends from '@babel/runtime/helpers/extends';
import clsx from 'clsx';
import * as React from 'react';
import './icon.css';

let globalIdCounter = 0;
function useId() {
  const [id] = React.useState(() => {
    globalIdCounter++;
    return `u${globalIdCounter.toString(32)}`;
  });
  return id;
}

/**
 * @example
 *
 * ```
 *  import { CheckOutlined } from '@universe-design/icons-react'
 *  const prodIcon = <Icon icon={CheckOutlined} />
 * ```
 */
var Icon = /*#__PURE__*/React.forwardRef(function Icon(_ref, ref) {
  let {
    icon: SVGIcon,
    className,
    svgProps,
    ...props
  } = _ref;
  return /*#__PURE__*/React.createElement("span", _extends({
    className: clsx('universe-icon', className),
    ref: ref
  }, props), /*#__PURE__*/React.createElement(SVGIcon, _extends({
    prefixId: useId()
  }, svgProps)));
});

export { Icon as default };
