"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const react_1 = __importDefault(require("react"));
const react_2 = require("@testing-library/react");
const ThemeProvider_1 = require("../ThemeProvider");
const path_1 = __importDefault(require("path"));
const fs_1 = __importDefault(require("fs"));
const originalIconsDir = path_1.default.resolve(path_1.default.dirname(require.resolve('@universe-design/icons/package.json')), 'icons');
const LightProviders = ({ children }) => (react_1.default.createElement(ThemeProvider_1.ThemeProvider, { mode: "light" }, children));
const DarkProviders = ({ children }) => (react_1.default.createElement(ThemeProvider_1.ThemeProvider, { mode: "dark" }, children));
function getName(file) {
    const obj = path_1.default.parse(file);
    const fileName = obj.name.replace(/-dm$/, '');
    const parts = fileName.split(/[-_]/g);
    const theme = parts[parts.length - 1];
    const componentName = parts
        .map((word) => {
        if (word === 'icon') {
            return '';
        }
        return word[0].toUpperCase() + word.slice(1);
    })
        .join('');
    return { theme, componentName, fileName };
}
const entries = fs_1.default.readdirSync(originalIconsDir).map((file) => {
    const { componentName, ...rest } = getName(file);
    return [componentName, { componentName, file, ...rest }];
});
const entriesMap = Object.fromEntries(entries);
console.log(originalIconsDir);
function getPath(p = '') {
    return path_1.default.resolve(__dirname, '../icons', p);
}
// function readSourceSVG(file: string) {
//   return fs.readFileSync(path.resolve(originalIconsDir, file), {
//     encoding: 'utf-8',
//   })
// }
fs_1.default.readdirSync(getPath())
    .filter((file) => {
    const name = path_1.default.basename(file, '.tsx');
    const entry = entriesMap[name];
    return entry && entry.theme === 'colorful';
})
    .forEach((file) => {
    test(file, () => {
        // const name = path.basename(file, '.tsx')
        // const entry = entriesMap[name]
        // eslint-disable-next-line @typescript-eslint/no-require-imports
        const Component = require(getPath(file)).default;
        const { container } = (0, react_2.render)(react_1.default.createElement(Component, null), {
            wrapper: LightProviders,
        });
        expect(container.firstChild).toMatchSnapshot();
        //   expect(container.firstChild?.firstChild).toMatchInlineSnapshot(
        //     prettier.format(readSourceSVG(`${entry.fileName}.svg`), {
        //       parser: 'babel',
        //     })
        //   )
        const { container: containerDark } = (0, react_2.render)(react_1.default.createElement(Component, null), {
            wrapper: DarkProviders,
        });
        expect(containerDark.firstChild).toMatchSnapshot();
        //   expect(containerDark.firstChild?.firstChild).toMatchInlineSnapshot(
        //     prettier.format(readSourceSVG(`${entry.fileName}-dm.svg`), {
        //       parser: 'babel',
        //     })
        //   )
    });
});
