"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.use = void 0;
// Fork from https://github.com/pmndrs/jotai/blob/main/src/react/useAtomValue.ts

/**
 * React.use polyfill
 *
 * @public
 */
const use = promise => {
  if (promise.status === 'pending') {
    throw promise;
  } else if (promise.status === 'fulfilled') {
    return promise.value;
  } else if (promise.status === 'rejected') {
    throw promise.reason;
  } else {
    promise.status = 'pending';
    promise.then(v => {
      Object.assign(promise, {
        status: 'fulfilled',
        value: v
      });
    }, e => {
      Object.assign(promise, {
        status: 'rejected',
        reason: e
      });
    });
    throw promise;
  }
};
exports.use = use;
//# sourceMappingURL=use.js.map
