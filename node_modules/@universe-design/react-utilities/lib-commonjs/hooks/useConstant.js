"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useConstant = useConstant;
var _react = _interopRequireDefault(require("react"));
/**
 * The `useConstant` function is a custom React Hook that allows you to store a constant value that persists for the lifetime of the component.
 * This is useful when you need a constant value that does not change across re-renders, but is not part of the component's state or props.
 *
 * @public
 */
function useConstant(fn) {
  const ref = _react.default.useRef();
  if (!ref.current) {
    ref.current = {
      v: fn()
    };
  }
  return ref.current.v;
}
//# sourceMappingURL=useConstant.js.map
