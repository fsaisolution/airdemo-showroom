"use strict";

var _reactHooks = require("@testing-library/react-hooks");
var _useConstant = require("./useConstant");
test('should remain constant', () => {
  const {
    result,
    rerender
  } = (0, _reactHooks.renderHook)(() => (0, _useConstant.useConstant)(() => ({
    a: 1
  })));
  const pre = result.current;
  rerender();
  const {
    current
  } = result;
  expect(current).toBe(pre);
});
//# sourceMappingURL=useConstant.test.js.map
