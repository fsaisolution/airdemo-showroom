"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getSize = getSize;
/**
 * 通过 element 或 resize observer entry 获取节点的尺寸信息。该函数内部处理了兼容性问题，包括浏览器差异、节点差异等。
 *
 * @public
 */
function getSize(target, entry) {
  // 如果不是 svg 元素，并且提供了 entry，说明是 resize observer 回调
  // 优先级 borderBoxSize > getBoundingClientRect
  if (!(target instanceof SVGElement) && entry) {
    const rect = entry.target.getBoundingClientRect();
    const borderBoxSize = Array.isArray(entry.borderBoxSize) ? entry.borderBoxSize[0] : entry.borderBoxSize; // firefox 不是数组，safari 没有 borderBoxSize
    const {
      width,
      height
    } = rect;
    return {
      // 有一种 case 在动画过程中触发 resize 回调 getBoundingClientRect 取到的 width 和 height 不正确，但是 borderBoxSize 取到是正确的
      width: borderBoxSize ? borderBoxSize.inlineSize : width,
      height: borderBoxSize ? borderBoxSize.blockSize : height
    };
  }
  // 如果是 svg 元素，borderBoxSize 不准，所以用 getBoundingClientRect
  return target.getBoundingClientRect();
}
//# sourceMappingURL=getSize.js.map
