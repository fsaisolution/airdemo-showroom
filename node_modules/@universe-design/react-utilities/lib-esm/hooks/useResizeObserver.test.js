import { renderHook } from '@testing-library/react-hooks';
import { sleep, useResizeObserver } from '..';
describe('useResizeObserver', () => {
  test('ref node is not Element', async () => {
    const cb = jest.fn();
    const wrapper = renderHook(useResizeObserver, {
      initialProps: cb
    });
    wrapper.result.current.current = {};
    await sleep(1000);
    // 其实只是为了验证赋值不会挂掉，后面这个测试是附带的
    expect(cb).toHaveBeenCalledTimes(0);
  });
});
//# sourceMappingURL=useResizeObserver.test.js.map
