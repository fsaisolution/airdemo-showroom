import React from 'react';
import { renderHook } from '@testing-library/react-hooks';
import { render } from '@testing-library/react';
import { useCloneElementRef } from './useCloneElementRef';
import '@testing-library/jest-dom';
describe('useCloneElementRef', () => {
  test('Compose user ref', async () => {
    const userRef = jest.fn();
    const ref = jest.fn();
    const children = /*#__PURE__*/React.createElement("div", {
      ref: userRef
    });
    const wrapper = renderHook(() => useCloneElementRef({
      children,
      ref
    }));
    render(/*#__PURE__*/React.createElement(React.Fragment, null, wrapper.result.current));
    expect(userRef).toHaveBeenCalledTimes(1);
    expect(ref).toHaveBeenCalledTimes(1);
  });
  test.each([[null, ''], [undefined, ''], ['123', '123']])('Not valid element: %s', (children, text) => {
    const userRef = jest.fn();
    const ref = jest.fn();
    const wrapper = renderHook(() => useCloneElementRef({
      children,
      ref
    }));
    const wrapper2 = render(/*#__PURE__*/React.createElement(React.Fragment, null, wrapper.result.current));
    if (text) {
      expect(wrapper2.getByText(text)).toBeVisible();
    }
    expect(userRef).toHaveBeenCalledTimes(0);
    expect(ref).toHaveBeenCalledTimes(0);
  });
  test('Children has ref but not dom, domOnly true', async () => {
    const refValue = {};
    const Child = /*#__PURE__*/React.forwardRef((_, ref) => {
      React.useImperativeHandle(ref, () => refValue, []);
      return /*#__PURE__*/React.createElement("div", null);
    });
    const userRef = jest.fn();
    const ref = jest.fn();
    const children = /*#__PURE__*/React.createElement(Child, {
      ref: userRef
    });
    const wrapper = renderHook(() => useCloneElementRef({
      children,
      ref
    }));
    render(/*#__PURE__*/React.createElement(React.Fragment, null, wrapper.result.current));
    expect(userRef).toHaveBeenCalledTimes(1);
    expect(userRef.mock.calls[0][0]).toBe(refValue);
    expect(ref).toHaveBeenCalledTimes(1);
    expect(ref.mock.calls[0][0]).toBeInstanceOf(HTMLElement);
  });
  test('Children has ref but not dom, domOnly false', async () => {
    const refValue = {};
    const Child = /*#__PURE__*/React.forwardRef((_, ref) => {
      React.useImperativeHandle(ref, () => refValue, []);
      return /*#__PURE__*/React.createElement("div", null);
    });
    const userRef = jest.fn();
    const ref = jest.fn();
    const children = /*#__PURE__*/React.createElement(Child, {
      ref: userRef
    });
    const wrapper = renderHook(() => useCloneElementRef({
      children,
      ref
    }, {
      domOnly: false
    }));
    render(/*#__PURE__*/React.createElement(React.Fragment, null, wrapper.result.current));
    expect(userRef).toHaveBeenCalledTimes(1);
    expect(userRef).toHaveBeenCalledWith(refValue);
    expect(ref).toHaveBeenCalledTimes(1);
    expect(ref).toHaveBeenCalledWith(refValue);
  });
  test('Element does not support ref, domOnly true', async () => {
    const ref = jest.fn();
    const children = /*#__PURE__*/React.createElement(() => /*#__PURE__*/React.createElement("div", null, "123"), null);
    const wrapper = renderHook(() => useCloneElementRef({
      children,
      ref
    }));
    const wrapper2 = render(/*#__PURE__*/React.createElement(React.Fragment, null, wrapper.result.current));
    expect(wrapper2.getByText('123')).toBeVisible();
    expect(ref).toHaveBeenCalledTimes(1);
    expect(ref.mock.calls[0][0]).toBeInstanceOf(HTMLElement);
    wrapper.rerender(() => useCloneElementRef({
      children,
      ref
    }));
    wrapper2.rerender(/*#__PURE__*/React.createElement(React.Fragment, null, wrapper.result.current));
    expect(ref).toHaveBeenCalledTimes(1);
    expect(ref.mock.calls[0][0]).toBeInstanceOf(HTMLElement);
  });
  test('Element does not support ref, domOnly false', async () => {
    const userRef = jest.fn();
    const ref = jest.fn();
    const children = /*#__PURE__*/React.createElement(() => /*#__PURE__*/React.createElement("div", null, "123"), null);
    const wrapper = renderHook(() => useCloneElementRef({
      children,
      ref
    }, {
      domOnly: false
    }));
    const wrapper2 = render(/*#__PURE__*/React.createElement(React.Fragment, null, wrapper.result.current));
    expect(wrapper2.getByText('123')).toBeVisible();
    expect(userRef).toHaveBeenCalledTimes(0);
    expect(ref).toHaveBeenCalledTimes(0);
  });
  test('Element support ref, domOnly false, but not handle ref', async () => {
    const Child = /*#__PURE__*/React.forwardRef(() => /*#__PURE__*/React.createElement("div", null));
    const userRef = jest.fn();
    const ref = jest.fn();
    const children = /*#__PURE__*/React.createElement(Child, {
      ref: userRef
    });
    const wrapper = renderHook(() => useCloneElementRef({
      children,
      ref
    }, {
      domOnly: false
    }));
    render(/*#__PURE__*/React.createElement(React.Fragment, null, wrapper.result.current));
    expect(userRef).toHaveBeenCalledTimes(0);
    expect(ref).toHaveBeenCalledTimes(0);
  });
  test('Element support ref, domOnly true, but not handle ref', async () => {
    const Child = /*#__PURE__*/React.forwardRef(() => /*#__PURE__*/React.createElement("div", null));
    const userRef = jest.fn();
    const ref = jest.fn();
    const children = /*#__PURE__*/React.createElement(Child, {
      ref: userRef
    });
    const wrapper = renderHook(() => useCloneElementRef({
      children,
      ref
    }, {
      domOnly: true
    }));
    render(/*#__PURE__*/React.createElement(React.Fragment, null, wrapper.result.current));
    expect(userRef).toHaveBeenCalledTimes(0);
    expect(ref).toHaveBeenCalledTimes(1);
    expect(ref.mock.calls[0][0]).toBeInstanceOf(HTMLElement);
  });
  test('cleanup ref in unmounting', async () => {
    const Child = /*#__PURE__*/React.forwardRef((_, ref) => /*#__PURE__*/React.createElement("div", {
      ref: ref
    }));
    const ref = jest.fn();
    const hookWrapper = renderHook(() => useCloneElementRef({
      children: /*#__PURE__*/React.createElement(Child, null),
      ref
    }, {
      domOnly: true
    }));
    const wrapper = render(/*#__PURE__*/React.createElement(React.Fragment, null, hookWrapper.result.current));
    expect(ref).toHaveBeenCalledTimes(1);
    wrapper.unmount();
    expect(ref).toHaveBeenCalledTimes(2);
    expect(ref.mock.calls[0][0]).toBeInstanceOf(HTMLElement);
    expect(ref.mock.calls[1][0]).toBe(null);
  });
});
//# sourceMappingURL=useCloneElementRef.test.js.map
