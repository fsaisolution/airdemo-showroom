/**
 * 需求
 * 1. emotion global 那种插入一段内容到全局，可以重复加入
 * 2. 通过一个样式的 hash 作为 key，如果已经 inserted 后续的就不加入了
 */
/**
 * @public
 * useStyleSheetOnce
 * 全局插入一次 css 样式，适用于静态样式场景
 */
export declare function useStyleSheetOnce(styles: string, { disabled, atLeastOne }?: {
    disabled?: boolean;
    atLeastOne?: boolean;
}): void;
/**
 * @public
 */
export declare const StyleSheetOnce: ({ styles }: {
    styles: string;
}) => null;
//# sourceMappingURL=useStyleSheetOnce.d.ts.map