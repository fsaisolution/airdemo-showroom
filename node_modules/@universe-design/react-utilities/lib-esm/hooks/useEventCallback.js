import * as React from 'react';
import { useIsomorphicLayoutEffect as useLayoutEffect } from './useIsomorphicLayoutEffect';

/**
 * For event callback, 利用最新值模式，在并发场景下有可能有问题
 *
 * @public
 * @param fn - effect 函数
 */
export function useEventCallback(fn) {
  const ref = React.useRef(fn);

  // we copy a ref to the callback scoped to the current state/props on each render
  useLayoutEffect(() => {
    ref.current = fn;
  });
  return React.useCallback(function () {
    var _ref$current;
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    return (_ref$current = ref.current) === null || _ref$current === void 0 ? void 0 : _ref$current.apply(void 0, args);
  }, []);
}
//# sourceMappingURL=useEventCallback.js.map
