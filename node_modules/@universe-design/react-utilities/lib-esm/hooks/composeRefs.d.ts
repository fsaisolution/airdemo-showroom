import * as React from 'react';
/**
 * 可选 ref
 *
 * @public
 */
export type OptionalRef<T> = React.Ref<T> | undefined;
/**
 * 非空 ref
 *
 * @public
 */
export type NonNullRef<T> = NonNullable<React.Ref<T>>;
/**
 * 更新 ref 的值
 *
 * @public
 */
export declare function updateRef<T>(ref: NonNullRef<T>, instance: null | T): void;
/**
 * 判断是否支持 ref
 *
 * @public
 */
export declare function supportRef(node?: React.ReactNode): node is React.ReactElement;
/**
 * 组合多个 ref
 *
 * @public
 */
export declare function composeRefs<T>(...refs: [OptionalRef<T>, OptionalRef<T>, ...Array<OptionalRef<T>>]): React.Ref<T>;
/**
 * 合并多个 ref。注意这里传入的 refs 会作为 useMemo 的 deps，数量不能随 render 变化。
 *
 * @public
 * @param refs - 多个 refs
 * @returns 合并后的 ref
 */
export declare function useMergedRefs<T>(...refs: [OptionalRef<T>, OptionalRef<T>, ...Array<OptionalRef<T>>]): React.Ref<T>;
//# sourceMappingURL=composeRefs.d.ts.map