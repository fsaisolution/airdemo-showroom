import { useEffect, useRef } from 'react';
import { useDidMount } from './useDidMount';
import { useWillUnmount } from './useWillUnmount';
import { useIsomorphicLayoutEffect as useLayoutEffect } from './useIsomorphicLayoutEffect';
export function useDidUpdate(callback) {
  const hasMountedRef = useRef(false);
  useEffect(() => {
    if (hasMountedRef.current) {
      return callback();
    }
  }, [callback]);
  useDidMount(() => {
    hasMountedRef.current = true;
  });
  useWillUnmount(() => {
    hasMountedRef.current = false;
  });
}
export function useUpdateLayoutEffect(callback) {
  const hasMountedRef = useRef(false);
  useLayoutEffect(() => {
    if (hasMountedRef.current) {
      return callback();
    }
  }, [callback]);
  useDidMount(() => {
    hasMountedRef.current = true;
  });
  useWillUnmount(() => {
    hasMountedRef.current = false;
  });
}
//# sourceMappingURL=useDidUpdate.js.map
