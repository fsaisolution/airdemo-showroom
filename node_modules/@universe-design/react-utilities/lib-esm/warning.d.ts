/**
 * 打印一个警告。这个方法不会去重，只是单纯打印一个警告。
 *
 * @public
 * @param valid - 是否是合法行为，false 则会打印
 * @param message - 警告消息
 * @param args - 其他参数
 * @returns
 */
export declare function warn(valid: boolean, message: string, ...args: any[]): void;
/**
 * 重置警告 map。重置后可以打印之前打印过的警告。
 *
 * @public
 */
export declare function resetWarned(): void;
/**
 * 打印一个一次性警告。这个函数命名为 warning 可以配合 `babel-plugin-dev-expression` 插件进行转码加上 NODE_ENV 限制，最终 production 代码中没有任何的 warning 调用。
 *
 * @public
 * @param valid - 是否是合法行为，false 则会打印
 * @param message - 警告消息
 * @param args - 其他参数
 */
export declare function warning(valid: boolean, message: string, ...args: any[]): void;
/**
 * 格式化出一个 UD 开发警告
 *
 * @public
 * @param component - 组件名称
 * @param message - 警告信息
 * @returns
 *
 * @example
 *
 * ```typescript
 * warning(valid, devWarning('Component', 'Cannot use this prop.'))
 * ```
 */
export declare function devWarning(component: string, message: string): string;
/**
 * 格式化出一个 AntD 兼容性开发警告
 *
 * @public
 * @param component - 组件名称
 * @param propNames - 属性名
 * @returns
 */
export declare function compatibleDevWarning(component: string, propNames: string[]): string;
/**
 * 格式化出一个即将废弃警告
 *
 * @public
 * @param component - 组件名称
 * @param propName - 原属性名
 * @param replacePropName - 替换属性名
 * @returns
 */
export declare function deprecatedWarning(component: string, propName: string, replacePropName?: string | string[]): string;
//# sourceMappingURL=warning.d.ts.map