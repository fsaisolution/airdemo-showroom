/**
 * Create a generic typed descendant context
 *
 * @internal
 */
export const microDebounce = fn => {
  let scheduled = false;
  return function () {
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    if (scheduled) {
      return;
    }
    scheduled = true;
    Promise.resolve().then(() => {
      scheduled = false;
      fn(...args);
    });
  };
};
//# sourceMappingURL=microDebounce.js.map
