import * as React from 'react';
import { test, expect } from '@playwright/experimental-ct-react17';
import { ResizeObserver } from './ResizeObserver';
import '../../config/test.setup';
test('should work', async _ref => {
  let {
    mount
  } = _ref;
  const component = await mount(/*#__PURE__*/React.createElement(ResizeObserver, {
    onResize: () => {}
  }, /*#__PURE__*/React.createElement("div", {
    id: "block",
    style: {
      width: 200,
      height: 100
    }
  })));
  expect(await component.boundingBox()).toStrictEqual({
    height: 100,
    width: 200,
    x: 0,
    y: 0
  });
});
test('should work 2', async _ref2 => {
  let {
    mount
  } = _ref2;
  const component = await mount(/*#__PURE__*/React.createElement(ResizeObserver, {
    onResize: () => {}
  }, /*#__PURE__*/React.createElement("div", {
    id: "block",
    style: {
      width: 200,
      height: 100
    }
  })));
  expect(await component.boundingBox()).toStrictEqual({
    height: 100,
    width: 200,
    x: 0,
    y: 0
  });
});
//# sourceMappingURL=ResizeObserver.playwright.test.js.map
