"use strict";

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useScrollObserver = useScrollObserver;
var React = _interopRequireWildcard(require("react"));
var _reactUtilities = require("@universe-design/react-utilities");
/**
 * @description
 * 用于检测某个元素是否出现了滚动条
 */
function useScrollObserver() {
  const [scrolling, setScrolling] = React.useState(false);
  const isScrolling = React.useCallback(scrollElementRef => {
    const element = scrollElementRef.current;
    if (!element) {
      return;
    }
    if (element.scrollTop > 0) {
      setScrolling(true);
    } else {
      setScrolling(false);
    }
  }, []);
  const scrollElementRef = (0, _reactUtilities.useResizeObserver)(() => {
    isScrolling(scrollElementRef);
  });
  const handleScroll = React.useCallback(() => {
    isScrolling(scrollElementRef);
  }, [isScrolling, scrollElementRef]);
  React.useEffect(() => {
    const element = scrollElementRef.current;
    if (!element) {
      return;
    }
    element.addEventListener('scroll', handleScroll);
    return () => {
      element.removeEventListener('scroll', handleScroll);
    };
  }, [scrollElementRef, handleScroll]);
  return {
    scrolling,
    scrollElementRef
  };
}
//# sourceMappingURL=useScrollObserver.js.map
