import type { TransferGroupOption, TransferOption, TransferValue, UseTransferProps } from '../interface';
export declare const useTransferData: <V extends TransferValue, OP extends TransferOption<V>>({ options, searchValue, filterOption, group: groupProp, }: Required<Pick<UseTransferProps<V, OP>, "options" | "value" | "filterOption" | "group">> & {
    searchValue: string;
}) => {
    formattedOptions: OP[];
    optionsMap: Map<V, OP>;
    filteredOptions: OP[];
    groupWeakMap: WeakMap<OP, TransferGroupOption<V, OP>>;
};
