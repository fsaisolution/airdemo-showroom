import React from 'react';
import type { TagProps } from '../../tag';
interface CounterProps {
    showCount?: boolean;
    count?: number;
    maxLength?: number;
    disabled?: boolean;
    prefix?: string;
    tagSize?: TagProps['size'];
    style?: React.CSSProperties;
}
export default function Counter(props: CounterProps): React.JSX.Element | null;
export {};
