import type moment from 'moment';
import type { DatePickerLocale } from '../shared/locales';
export type MODE = 'date' | 'month' | 'year';
export type PickerType = 'date' | 'month' | 'year';
export type OnSelect = (date: moment.Moment) => void;
export type RangeValue = [moment.Moment?, moment.Moment?];
export type IFormat = string | ((value: moment.Moment) => string);
export type IParse = (value: string) => moment.Moment;
export type Locale = DatePickerLocale;
export type StartOfWeek = 0 | 1 | 2 | 3 | 4 | 5 | 6;
