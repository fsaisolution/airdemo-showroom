import type moment from 'moment';
import type { GenerateConfig } from '../../shared/components/picker/generate';
import type { SharedTimeProps } from '../../shared/components/picker/interface';
import type { IFormat, MODE, RangeValue } from '../interface';
export declare function elementsContains(elements: (HTMLElement | undefined | null)[], target: HTMLElement): boolean;
export declare function isInHoverRange(date: moment.Moment, mode: MODE, hoverDate?: moment.Moment, rangeValue?: RangeValue, index?: 0 | 1): boolean;
export declare function getTimeProps<DateType>(props: {
    format?: IFormat;
} & SharedTimeProps<DateType>): SharedTimeProps<DateType>;
export declare function getInputSize(format: IFormat, generateConfig: GenerateConfig): number;
/**
 * 获取单面板的开始时间
 */
export declare const getStartOfPanel: (_value: moment.Moment, mode: MODE) => moment.Moment;
/**
 * 判断是否在指定面板上
 */
export declare function isInRangeView(
/**
 * 要判断的值
 */
_value: moment.Moment, 
/**
 * 面板值
 */
_pickerValues: [moment.Moment, moment.Moment], 
/**
 * 面板 mode
 */
mode: MODE): boolean;
