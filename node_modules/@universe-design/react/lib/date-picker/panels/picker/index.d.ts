import moment from 'moment';
import React from 'react';
import type { IFormat, MODE } from '../../interface';
import type { PickerFooterProps } from './Footer';
import type { MonthYearSelectorProps } from '../selector/MonthYearSelector';
import type { DateCalendarUIProps } from '../DateCalendar/interface';
import '../../style';
/**
 * @privateRemarks rangedValue不会对视图定位产生影响，value会
 * @public
 * @docgen
 * 日期选择面板的属性
 */
export interface PickerPanelProps extends Pick<PickerFooterProps, 'onCancel' | 'onConfirm'>, Pick<MonthYearSelectorProps, 'onYearClick' | 'onMonthClick' | 'onCollapsed' | 'hiddenCollapse'>, Omit<DateCalendarUIProps, 'onPickerValueChange' | 'selectedDates' | 'selectedRange' | 'pickerValue' | 'cellRenderer' | 'previewDate'> {
    /**
     * 是否显示 footer
     * @defaultValue true
     */
    footer?: boolean;
    format?: IFormat;
    /**
     * 选中的值
     */
    value?: moment.Moment | null;
    /**
     * 当前视图日期发生变化时触发
     *
     * @privateRemarks
     * 1. 点击 Cell（Date、Month、Year）
     * 2. 点击 Prev Next 按钮（Date、Month、Year）
     * 3. Footer 中的『今天』
     */
    onViewDateChange?: DateCalendarUIProps['onPickerValueChange'];
    /**
     * 面板默认显示时间
     */
    defaultViewDate?: moment.Moment;
    /**
     * 自定义日期单元格的内容
     */
    dateRender?: (currentDate: moment.Moment, mode?: MODE, defaultNode?: React.ReactNode) => React.ReactNode;
}
export declare const isDateValueEqual: (a: moment.Moment | undefined, b: moment.Moment | undefined) => boolean;
/**
 * @deprecated
 * 推荐使用 `DateCalendar` 或 `DateCalendarUI` 组件
 */
export declare function PickerPanel(props: PickerPanelProps): React.JSX.Element;
export default PickerPanel;
