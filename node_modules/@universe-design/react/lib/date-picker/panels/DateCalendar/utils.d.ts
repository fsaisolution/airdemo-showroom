import type { MODE } from '../../interface';
export declare function isSelectCell({ selectedDates, selectedRange, date, mode, }: {
    mode: MODE;
    date: moment.Moment;
    selectedDates: moment.Moment[];
    selectedRange?: [moment.Moment, moment.Moment];
}): boolean;
