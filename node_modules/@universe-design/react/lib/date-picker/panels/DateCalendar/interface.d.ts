import type moment from 'moment';
import type { MODE, StartOfWeek } from '../../interface';
import type { PickerBodyProps } from '../picker/Body';
import type { MonthYearSelectorProps } from '../selector/MonthYearSelector';
import type { StylexProps } from '../../../util';
/**
 * @internal
 */
export interface InteractivePanelProps<DateType> {
    /**
     * 日期选中回调
     */
    onSelect?: (value: DateType) => void;
    /**
     * 选中的时间点
     */
    selectedDates?: DateType[];
    /**
     * 选中的时间段
     */
    selectedRange?: [DateType, DateType];
    /**
     * 日期选项鼠标移入事件
     */
    onDateMouseEnter?: (value: DateType) => void;
    /**
     * 日期选项鼠标移出事件
     */
    onDateMouseLeave?: (value: DateType) => void;
    /**
     * 面板鼠标移入事件
     * @internal
     */
    onContentMouseEnter?: () => void;
    /**
     * 面板鼠标移出事件
     * @internal
     */
    onContentMouseLeave?: () => void;
}
/**
 * @docgen
 * @public
 */
export interface DateCalendarUIProps<DateType = moment.Moment> extends Pick<MonthYearSelectorProps, 'onYearClick' | 'onMonthClick' | 'onCollapsed' | 'hiddenCollapse'>, Omit<PickerBodyProps, 'prefixCls' | 'mode' | 'pickerValue' | 'dateRender'>, InteractivePanelProps<DateType>, StylexProps {
    /**
     * className 属性透传
     */
    className?: string;
    /**
     * style 属性透传
     */
    style?: React.CSSProperties;
    /**
     * 面板默认显示时间
     */
    defaultPickerValue?: DateType;
    /**
     * 面板受控显示时间
     */
    pickerValue?: DateType;
    /**
     * 是否显示上一月（年）按钮
     * @defaultValue false
     */
    hiddenPrev?: boolean;
    /**
     * 是否显示下一月（年）按钮
     * @defaultValue false
     */
    hiddenNext?: boolean;
    /**
     * (EXPERIMENTAL) 自定义 Header 渲染
     *
     * @internal
     */
    headerRenderer?: (meta: {
        prevBtnNode: React.ReactElement;
        nextBtnNode: React.ReactElement;
        defaultNode: React.ReactElement;
    }) => React.ReactNode;
    /**
     * 当前视图日期发生变化时触发
     *
     * @privateRemarks
     * 1. 点击 Cell（Date、Month、Year）
     * 2. 点击 Prev Next 按钮（Date、Month、Year）
     * 3. Footer 中的『今天』
     */
    onPickerValueChange?: (pickerValue: DateType, meta: {
        mode: MODE;
    }) => void;
    /**
     * 日期面板的状态，可选年、月、日
     */
    mode?: MODE;
    /**
     * 日期选项是否展示预览态
     */
    previewDate?: (date: DateType) => boolean;
    /**
     * 日期选项是否禁用，该 API 在年份面板上可能有性能瓶颈，优先使用 minDate、maxDate
     */
    disabledDate?: (date: DateType) => boolean;
    /**
     * 最小可选日期，小于该日期的日期选项会被禁用
     */
    minDate?: moment.Moment;
    /**
     * 最大可选日期，大于该日期的日期选项会被禁用
     */
    maxDate?: moment.Moment;
    /**
     * 自定义日期选项渲染
     */
    cellRenderer?: (date: moment.Moment, option: {
        viewMode: MODE;
        defaultNode: React.ReactNode;
    }) => React.ReactNode;
    /**
     * 一周开始于星期几，默认星期天
     * @defaultValue 0
     */
    startOfWeek?: StartOfWeek;
    /**
     * 隐藏整行日期都非本月的行
     * @internal
     * @defaultValue false
     */
    noOutOfViewWeek?: boolean;
    /**
     * 是否隐藏周数
     * @defaultValue true
     */
    hideWeekNumbers?: boolean;
    /**
     * 一年的第一周需要包含1月的 X 天，见 [Moment](https://momentjs.cn/docs/#/customization/dow-doy/)
     */
    janX?: number;
}
