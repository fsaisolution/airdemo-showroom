import type { ReactNode } from 'react';
import React from 'react';
import './style';
export interface BaseProps extends React.HTMLAttributes<HTMLDivElement> {
    hasSider?: boolean;
    children?: ReactNode;
}
export interface SiderControl {
    addSider: (id: string) => void;
    removeSider: (id: string) => void;
}
export interface LayoutContextProps {
    siderControl: SiderControl;
}
export declare const LayoutContext: React.Context<LayoutContextProps>;
declare const Layout: React.ForwardRefExoticComponent<BaseProps & React.RefAttributes<HTMLElement>>;
export default Layout;
