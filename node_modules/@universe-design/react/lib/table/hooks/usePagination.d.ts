import type { TablePaginationConfig } from '../interface';
export default function usePagination(listTotal: number, pagination: TablePaginationConfig | false | undefined, onPaginationChange: (params: {
    current: number;
    pageSize: number;
    total: number;
}) => void): [TablePaginationConfig, (() => void) | null, () => TablePaginationConfig];
