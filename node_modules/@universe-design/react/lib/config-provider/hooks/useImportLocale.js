"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useImportLocale = void 0;
var _react = require("react");
var _ = require(".");
var _util = require("../../util");
const useImportLocale = _ref => {
  let {
    importLocale,
    fallbackLocale
  } = _ref;
  const {
    locale: localeName
  } = (0, _.useLocale)();
  const [locale, setLocale] = (0, _react.useState)();
  (0, _react.useMemo)(() => {
    const maybeThenable = importLocale(localeName);
    if ('then' in maybeThenable) {
      maybeThenable.then(_locale => {
        setLocale(_locale);
      }).catch(err => {
        process.env.NODE_ENV !== "production" ? (0, _util.warning)(false, (0, _util.devWarning)('importLocale',
        // eslint-disable-next-line max-len
        `${err.message}. Locale name \`${localeName}\` is not valid or something unexpected occurs. Your app will use \`en-US\` as a fallback.`)) : void 0;
        setLocale(fallbackLocale);
      });
      return;
    }
    setLocale(maybeThenable);
  }, [fallbackLocale, importLocale, localeName]);
  return locale;
};
exports.useImportLocale = useImportLocale;
//# sourceMappingURL=useImportLocale.js.map
