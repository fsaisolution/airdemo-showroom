import type { DeepPartial } from 'utility-types';
import type { UploadLocale } from '../../upload/interface';
import type { ValidateMessages } from '../../form/interface';
import type { PaginationLocale } from '../../pagination/internal/types';
import type React from 'react';
import type { LocaleName } from '@universe-design/react-provider';
export type IntlMessage = {
    /**
     * formatter
     */
    f: (value: Record<string, string | ((child: string) => React.ReactNode)>) => React.ReactNode;
};
export type AdditionalPickerLocaleLangProps = {
    placeholder: string;
    yearPlaceholder: string;
    quarterPlaceholder: string;
    monthPlaceholder: string;
    rangeYearPlaceholder: [string, string];
    rangeMonthPlaceholder: [string, string];
    rangePlaceholder: [string, string];
};
export type DatePickerLocale = {
    today: string;
    ok: string;
    month: string;
    year: string;
    yesterday: string;
    thisWeek: string;
    thisMonth: string;
    thisBimonthly: string;
    weekdaysMin: string[];
    monthsMin: string[];
    currentTime: string;
    monthYearSelector: IntlMessage;
    longMonthFormat: string;
    longDateFormat: string;
    weekNumber: IntlMessage;
} & AdditionalPickerLocaleLangProps;
export type PickerLocale = {
    morning: string;
    afternoon: string;
    timePlaceholder: string;
    hour: string;
    minute: string;
    second: string;
};
/**
 * @public
 * 全部定义可在这里查看： {@link https://code.byted.org/universe-ui/react/blob/master/%40universe-ui/react/src/shared/locales/types.ts | Locale 定义}
 */
export interface CompleteLocale {
    locale: LocaleName;
    global: {
        placeholder: string;
        notFoundContent: string;
        noOptions: string;
        selectAll: string;
        okText: string;
        cancelText: string;
        more: string;
        search: string;
        colon: string;
    };
    components: {
        modal: {
            okText: string;
            cancelText: string;
        };
        popconfirm: {
            okText: string;
            cancelText: string;
        };
        colorPicker: {
            moreColor: string;
        };
        tabs: {
            more: string;
        };
        rate: {
            descriptions: [string, string, string, string, string, string];
        };
        pagination: PaginationLocale;
        table: {
            filterTitle: string;
            filterConfirm: string;
            filterReset: string;
            filterEmptyText: string;
            emptyText: string;
            selectAll: string;
            selectInvert: string;
            selectionAll: string;
            sortTitle: string;
            expand: string;
            collapse: string;
            triggerDesc: string;
            triggerAsc: string;
            cancelSort: string;
        };
        datePicker: DatePickerLocale;
        picker: PickerLocale;
        empty: {
            description: string;
        };
        form: {
            defaultValidateMessage: ValidateMessages;
        };
        upload: UploadLocale;
        transfer: {
            noOptions: string;
            noSearchResult: string;
            noOptionsSelected: string;
            selectAll: IntlMessage;
            selected: IntlMessage;
            clear: string;
        };
        expandableText: {
            show: string;
            fold: string;
        };
        imageViewer: {
            prevArrow: string;
            nextArrow: string;
            zoomOut: string;
            zoomIn: string;
            originSize: string;
            bestFit: string;
            rotate: string;
            download: string;
        };
    };
}
export type PartialLocale = DeepPartial<CompleteLocale>;
