import type { Moment } from 'moment';
import type { Dispatch, ReactNode, SetStateAction } from 'react';
import type { Context } from 'use-context-selector';
import type { MODE } from './interface';
export interface PickerContextType<DateType> {
    mode: MODE;
    setMode: Dispatch<SetStateAction<MODE>>;
    value?: DateType;
    setValue: Dispatch<SetStateAction<DateType | undefined>>;
    setOpened: Dispatch<SetStateAction<boolean>>;
    showToday?: boolean;
    inDatePicker?: boolean;
    renderExtraFooter?: (ctx: Pick<PickerContextType<DateType>, 'mode' | 'value' | 'setValue'>) => ReactNode;
}
export declare const PickerContext: Context<PickerContextType<any>>;
export declare const getPickerContext: () => Context<PickerContextType<any>>;
export declare function usePickerContext<DateType = Moment>(): PickerContextType<DateType>;
export declare const usePickerContextSelector: <Selected, DateType = Moment>(selector: (value: PickerContextType<DateType>) => Selected) => Selected;
