/**
 * Forked and edited from https://github.com/bvaughn/react-virtualized-auto-sizer/blob/master/src/index.js
 * Thanks for bvaughn's work!
 */
import React from 'react';
type Size = {
    height: number;
    width: number;
};
export interface AutoSizerProps {
    /**
     * 渲染函数
     */
    children: (size: Size) => React.ReactElement;
    /**
     * `className` 属性透传
     */
    className?: string;
    /**
     * `style` 属性透传
     */
    style?: React.CSSProperties;
    /**
     * 默认高度，SSR 时有效
     * @defaultValue 0
     */
    defaultHeight?: number;
    /**
     * 默认宽度，SSR 时有效
     * @defaultValue 0
     */
    defaultWidth?: number;
    /**
     * 是否禁用动态高度
     * @defaultValue false
     */
    disableHeight?: boolean;
    /**
     * 是否禁用动态宽度
     * @defaultValue false
     */
    disableWidth?: boolean;
    /**
     * 尺寸变化回调函数
     */
    onResize?: (size: Size) => void;
}
export declare const AutoSizer: React.ForwardRefExoticComponent<AutoSizerProps & React.RefAttributes<HTMLDivElement>>;
export {};
