import type { RcTriggerPropsWithRef } from '../types';
import type { SizeInfo } from '../../../../hooks';
import type { UDTriggerProps } from '..';
export declare const useTriggerArrow: (config: {
    childSize: SizeInfo;
    popupSize: SizeInfo;
    arrow?: UDTriggerProps["arrow"];
    popup: RcTriggerPropsWithRef["popup"];
    prefixCls?: string;
}) => RcTriggerPropsWithRef["popup"];
