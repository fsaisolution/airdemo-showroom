import type { RcTriggerType } from '../types';
import type { SizeInfo } from '../../../../hooks';
export declare const useTriggerForcePopupAlign: (config: {
    popup: HTMLElement | null;
    rcTrigger: RcTriggerType | null;
    scroll: boolean;
    resize: boolean;
    scrollParents: (Element | typeof window | VisualViewport)[];
    popupSize: SizeInfo;
    childSize: SizeInfo;
    disabled: boolean;
    updatePopupRect: () => void;
    updateChildRect: () => void;
}) => void;
