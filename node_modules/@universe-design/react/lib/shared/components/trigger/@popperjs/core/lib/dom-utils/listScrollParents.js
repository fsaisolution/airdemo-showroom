"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = listScrollParents;
var _getScrollParent = _interopRequireDefault(require("./getScrollParent"));
var _getParentNode = _interopRequireDefault(require("./getParentNode"));
var _getWindow = _interopRequireDefault(require("./getWindow"));
var _isScrollParent = _interopRequireDefault(require("./isScrollParent"));
/**
 * Fork from https://github.com/popperjs/popper-core/blob/4800b37c5b4cabb711ac1d904664a70271487c4b/src/dom-utils/listScrollParents.js
 */

/*
given a DOM element, return the list of all scroll parents, up the list of ancesors
until we get to the top window object. This list is what we attach scroll listeners
to, because if any of these parent elements scroll, we'll need to re-calculate the
reference element's position.
*/
function listScrollParents(element) {
  var _element$ownerDocumen;
  let list = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : [];
  const scrollParent = (0, _getScrollParent.default)(element);
  const isBody = scrollParent === ((_element$ownerDocumen = element.ownerDocument) === null || _element$ownerDocumen === void 0 ? void 0 : _element$ownerDocumen.body);
  const win = (0, _getWindow.default)(scrollParent);
  const target = isBody ? [win].concat(win.visualViewport || [], (0, _isScrollParent.default)(scrollParent) ? scrollParent : []) : scrollParent;
  const updatedList = list.concat(target);
  return isBody ? updatedList // $FlowFixMe[incompatible-call]: isBody tells us target will be an HTMLElement here
  : updatedList.concat(listScrollParents((0, _getParentNode.default)(target)));
}
//# sourceMappingURL=listScrollParents.js.map
