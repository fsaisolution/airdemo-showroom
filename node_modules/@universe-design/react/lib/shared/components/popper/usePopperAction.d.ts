import React from 'react';
import type { UsePopperPopupReturnType, UsePopperSharedReturnType } from './usePopper';
export declare const PopperActionEnum: {
    readonly click: "click";
    readonly contextMenu: "contextMenu";
    readonly hover: "hover";
    readonly focus: "focus";
    readonly blur: "blur";
    readonly mouseEnter: "mouseEnter";
    readonly mouseLeave: "mouseLeave";
};
export type PopperActionType = Extract<keyof typeof PopperActionEnum, 'click' | 'contextMenu' | 'hover' | 'focus'>;
export type PopperShowActionType = Extract<keyof typeof PopperActionEnum, 'click' | 'contextMenu' | 'mouseEnter' | 'focus'>;
export type PopperHideActionType = Extract<keyof typeof PopperActionEnum, 'click' | 'mouseLeave' | 'blur'>;
export interface UsePopperActionSharedProps {
    /**
     * 弹层显示隐藏的触发方式
     */
    trigger?: PopperActionType | PopperActionType[];
    /**
     * 鼠标移入打开 popper 的延迟时间
     * @defaultValue 0
     */
    mouseEnterDelay?: number;
    /**
     * 鼠标移出关闭 popper 的延迟时间
     * @defaultValue 100
     */
    mouseLeaveDelay?: number;
    /**
     * 是否和点击位置对齐，`trigger` 为 `contextMenu` 时 `alignPoint` 默认值为 true
     * @defaultValue false
     */
    alignPoint?: boolean;
    /**
     * 显示弹层的触发动作
     */
    showAction?: PopperShowActionType[];
    /**
     * 隐藏弹层的触发动作
     */
    hideAction?: PopperHideActionType[];
}
export declare function usePopperActionShared(props: UsePopperActionSharedProps, context: {
    visible: boolean;
    setVisible: React.Dispatch<boolean>;
    inlineElementMousePositionRef: React.MutableRefObject<{
        x: number;
        y: number;
    }>;
} & Pick<UsePopperSharedReturnType, 'referenceRef' | 'referenceElementRef'>): {
    handleReferenceClick: React.MouseEventHandler<Element>;
    handleContextMenu: React.MouseEventHandler<Element>;
    handleMouseEnter: React.MouseEventHandler<Element>;
    handleMouseLeave: React.MouseEventHandler<Element>;
    handleKeyDown: React.KeyboardEventHandler<Element>;
    handleReferenceFocus: React.FocusEventHandler<Element>;
    showAction: string;
    hideAction: string;
    alignPoint: NonNullable<boolean | undefined>;
    setVisible: React.Dispatch<boolean>;
    setVisibleDelay: (args: [value: boolean], timeout: number) => void;
    mouseLeaveDelay: number;
};
export type UsePopperActionSharedReturnType = ReturnType<typeof usePopperActionShared>;
export interface UsePopperPopupActionProps {
}
export declare function usePopperPopupAction(props: UsePopperPopupActionProps, context: {
    visible: boolean;
    setVisibleDelay: (args: [value: boolean], timeout: number) => void;
} & Pick<UsePopperSharedReturnType, 'referenceElementRef'> & Pick<UsePopperPopupReturnType, 'floatingRef'>): {
    handlePopupFocus: React.FocusEventHandler<Element>;
    handlePopperMouseDownCapture: React.MouseEventHandler<Element>;
};
export type UsePopperPopupActionReturnType = ReturnType<typeof usePopperPopupAction>;
