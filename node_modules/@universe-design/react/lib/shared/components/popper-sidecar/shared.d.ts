import type { PopperProps } from '../popper';
import type { UDAlignType } from '../trigger';
import type { PositionType } from '../../../util';
export declare const POPUP_BORDER_RADIUS = 8;
export declare const POPUP_ARROW_SIZE: {
    x: number;
    y: number;
};
export interface PopperSidecarProps extends Omit<PopperProps, 'placement' | 'arrow' | 'trigger'> {
    /**
     * 是否开启动画，需要透传用户传入的参数
     */
    enableAnimation?: boolean;
    /**
     * 用户传入的 popupAlign
     */
    popupAlign?: UDAlignType;
    /**
     * 弹出的用户触发动作
     */
    action: PopperProps['trigger'];
    /**
     * popup 的弹出位置
     */
    popupPlacement: PositionType;
    /**
     * popup 弹出动画
     */
    popupMotion: string;
    /**
     * 是否显示 arrow
     */
    showArrow: boolean;
    /**
     * arrow 是否有描边
     */
    arrowHasStroke: boolean;
    /**
     * arrow 顶端到 popup 的默认距离
     */
    defaultMainAxisOffsetWithoutArrow: number;
}
