"use strict";

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getSystemThemeMode = getSystemThemeMode;
exports.useThemeMode = exports.useDerivedThemeModeState = void 0;
var React = _interopRequireWildcard(require("react"));
var _hooks = require("../../hooks");
var _reactUtilities = require("@universe-design/react-utilities");
const PrefersColorSchemeDark = '(prefers-color-scheme: dark)';
function getSystemThemeMode() {
  return (0, _hooks.getDefaultMediaMatches)(PrefersColorSchemeDark) ? 'dark' : 'light';
}
const getSetThemeMode = mode => {
  if (!(0, _reactUtilities.isClient)()) {
    return 'light';
  }
  if (mode !== undefined) {
    document.documentElement.dataset.udThemeMode = mode;
    return mode;
  } else {
    return document.documentElement.dataset.udThemeMode;
  }
};
const useSystemThemeMode = () => (0, _hooks.useMedia)(PrefersColorSchemeDark) ? 'dark' : 'light';

/**
 * 用 body data-ud-theme-mode 作为 light dark 切换的依据
 */
const useDerivedThemeModeState = () => {
  const [state, setState] = React.useState(getSetThemeMode());
  React.useEffect(() => {
    const observer = new MutationObserver(mutations => {
      mutations.forEach(() => {
        setState(getSetThemeMode());
      });
    });
    observer.observe(document.documentElement, {
      attributes: true,
      attributeFilter: ['data-ud-theme-mode']
    });
    return () => {
      observer.disconnect();
    };
  }, []);
  return state;
};
exports.useDerivedThemeModeState = useDerivedThemeModeState;
const useThemeMode = function () {
  let options = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  const {
    defaultMode = getSystemThemeMode(),
    followSystem = false
  } = options;
  const systemThemeMode = useSystemThemeMode();
  const derivedThemeMode = useDerivedThemeModeState();
  (0, _hooks.useIsomorphicLayoutEffect)(() => {
    if (followSystem) {
      getSetThemeMode(systemThemeMode);
    }
  }, [followSystem, systemThemeMode]);
  (0, _hooks.useIsomorphicLayoutEffect)(() => {
    if (!followSystem) {
      getSetThemeMode(defaultMode);
    }
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);
  const setMode = React.useCallback(_mode => {
    getSetThemeMode(followSystem ? systemThemeMode : _mode);
  }, [followSystem, systemThemeMode]);
  return [derivedThemeMode, setMode];
};
exports.useThemeMode = useThemeMode;
//# sourceMappingURL=useThemeMode.js.map
