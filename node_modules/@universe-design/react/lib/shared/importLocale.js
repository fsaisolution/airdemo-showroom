"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.importLocale = void 0;
var _interopRequireWildcard2 = _interopRequireDefault(require("@babel/runtime/helpers/interopRequireWildcard"));
var _util = require("../util");
var _enUS = _interopRequireDefault(require("./locales/en-US"));
/* eslint-disable no-undef */

/**
 * 提供需要加载的 localeName，异步返回 localeData
 */
const importLocale = async locale => {
  // storybook 直接加载源码所以是 ts 结尾，但是 build 以后的产物都是 js 不写 js 后缀的话 rollup 会提示处理不了，webpack5 会加载 d.ts 之类的文件，所以需要加上 .js
  let promise = (specifier => new Promise(r => r(specifier)).then(s => (0, _interopRequireWildcard2.default)(require(s))))(`./locales/${locale}.js`);
  return promise.then(_ref => {
    let {
      default: localeData
    } = _ref;
    return localeData;
  }).catch(err => {
    process.env.NODE_ENV !== "production" ? (0, _util.warning)(false, (0, _util.devWarning)('importLocale',
    // eslint-disable-next-line max-len
    `${err.message}. Locale name \`${locale}\` is not valid or something unexpected occurs. Your app will use \`en-US\` as a fallback.`)) : void 0;
    return _enUS.default;
  });
};
exports.importLocale = importLocale;
//# sourceMappingURL=importLocale.js.map
