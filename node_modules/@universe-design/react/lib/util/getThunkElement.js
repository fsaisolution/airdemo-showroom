"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getThunkElement = void 0;
var _assert = require("./assert");
/**
 * refObject 延迟获取元素方法
 */

/**
 * callback 延迟获取元素方法
 */

/**
 * 延迟获取元素方法，可以是 refObject，callback，query，element
 */

const getThunkElement = container => {
  if (!container) {
    return null;
  }
  if ((0, _assert.isFunction)(container)) {
    return container();
  }
  if (typeof container === 'string') {
    return document.querySelector(container);
  }
  if ('current' in container) {
    return container.current;
  }
  return container;
};
exports.getThunkElement = getThunkElement;
//# sourceMappingURL=getThunkElement.js.map
