"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Cubic = exports.Circ = void 0;
/**
 * t is current time
 * b is start value
 * c is change in value
 * d is duration
 */

const Cubic = exports.Cubic = {
  in(t, b, c, d) {
    t /= d;
    return (c - b) * t * t * t + b;
  },
  out(t, b, c, d) {
    t /= d;
    t--;
    return (c - b) * (t * t * t + 1) + b;
  },
  inOut(t, b, c, d) {
    const cc = c - b;
    t /= d / 2;
    if (t < 1) {
      return cc / 2 * t * t * t + b;
    }
    return cc / 2 * ((t -= 2) * t * t + 2) + b;
  }
};
const Circ = exports.Circ = {
  in(t, b, c, d) {
    t /= d;
    return -(c - b) * (Math.sqrt(1 - t * t) - 1) + b;
  },
  out(t, b, c, d) {
    t /= d;
    t--;
    return (c - b) * Math.sqrt(1 - t * t) + b;
  },
  inOut(t, b, c, d) {
    const cc = c - b;
    t /= d / 2;
    if (t < 1) {
      return -cc / 2 * (Math.sqrt(1 - t * t) - 1) + b;
    }
    t -= 2;
    return cc / 2 * (Math.sqrt(1 - t * t) + 1) + b;
  }
};
//# sourceMappingURL=easings.js.map
