"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getViewportSize = void 0;
const getViewportSize = () => {
  const {
    document: doc
  } = window;
  const {
    body,
    documentElement
  } = doc;
  const {
    clientWidth,
    clientHeight
  } = documentElement;
  const width = doc.compatMode === 'CSS1Compat' && clientWidth || body && body.clientWidth || clientWidth;
  const height = doc.compatMode === 'CSS1Compat' && clientHeight || body && body.clientHeight || clientHeight;
  return {
    width,
    height
  };
};
exports.getViewportSize = getViewportSize;
//# sourceMappingURL=getViewportSize.js.map
