"use strict";

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useRenderedReason = exports._log = void 0;
var React = _interopRequireWildcard(require("react"));
var _useFirstMountState = require("../hooks/useFirstMountState");
const _log = fn => function () {
  for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }
  console.trace(fn.name, 'args:', ...args);
  return fn(...args);
};
exports._log = _log;
const useRenderedReason = (name, depsMap) => {
  const isFirstMount = (0, _useFirstMountState.useFirstMountState)();
  const changedKeysRef = React.useRef(new Set());
  React.useEffect(() => {
    changedKeysRef.current = new Set();
  });
  for (const [k, v] of Object.entries(depsMap)) {
    // eslint-disable-next-line react-hooks/rules-of-hooks
    React.useEffect(() => {
      changedKeysRef.current.add(k);
      // eslint-disable-next-line react-hooks/exhaustive-deps
    }, [v]);
  }
  React.useEffect(() => {
    if (changedKeysRef.current.size && !isFirstMount) {
      console.log(`${name} rendered: deps ${[...changedKeysRef.current].map(o => `\`${o}\``).join(', ')} changed`);
    }
  });
};
exports.useRenderedReason = useRenderedReason;
//# sourceMappingURL=debug.js.map
