"use strict";

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useSelectDataMap = useSelectDataMap;
var React = _interopRequireWildcard(require("react"));
var _reactUtilities = require("@universe-design/react-utilities");
var _util = require("../../util");
var _common = require("../common");
function useSelectDataMap(_ref) {
  let {
    defaultValue,
    value: valueProp,
    options,
    valueInProps
  } = _ref;
  const selectedCache = (0, _reactUtilities.useConstant)(() => new Map());

  /**
   * 记录 optionMap 用于方便索引
   */
  const {
    optionMap
  } = React.useMemo(() => options.reduce((_ref2, cur) => {
    let {
      optionMap: preOptMap
    } = _ref2;
    preOptMap.set(cur.value, cur);
    return {
      optionMap: preOptMap
    };
  }, {
    optionMap: new Map()
  }), [options]);
  const updateCache = React.useCallback(valueSet => {
    for (const o of valueSet) {
      selectedCache.set(o, (0, _common.getOption)(o, optionMap, selectedCache));
    }
  }, [optionMap, selectedCache]);
  const [valueSet, setValueSet] = (0, _reactUtilities.useControlledState)(new Set((0, _util.genArray)(defaultValue)), new Set((0, _util.genArray)(valueProp)), {
    onChange: updateCache,
    isControlledOverride: valueInProps
  });

  // 用 useMemo 降低一下 render 触发该流程的频率
  React.useMemo(() => {
    // 每次 render 把 value 值对应的 options 存进 selectedCache
    updateCache(valueSet);
  }, [updateCache, valueSet]);
  return React.useMemo(() => ({
    valueSet,
    setValueSet,
    optionMap,
    getOption: v => (0, _common.getOption)(v, optionMap, selectedCache)
  }), [optionMap, selectedCache, setValueSet, valueSet]);
}
//# sourceMappingURL=useSelectDataMap.js.map
