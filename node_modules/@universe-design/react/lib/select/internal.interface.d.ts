import type { ReactElement } from 'react';
import type React from 'react';
import type { PopperProps } from '../shared/components/popper';
import type { TAction, DefaultSizeType, DefaultExtraSizeType, StrictUnion } from '../util';
import type { TagProps } from '../tag';
import type { SelectContextType } from './context/SelectContext';
import type { SelectOptionConfig } from './interface';
export type SelectMode = 'single' | 'multiple';
export type SelectValue = string | number | boolean;
/**
 * @public
 */
export declare enum SelectSearchAction {
    /** 失焦触发清空搜索值 */
    Blur = 0,
    /** 用户输入触发搜索值变化 */
    Input = 1,
    /** 选中选项触发清空搜索值 */
    AutoClear = 2
}
export interface InternalSelectOptionConfig<V extends SelectValue> {
    value: V;
    htmlTitle?: string;
    disabled?: boolean;
}
export declare enum SelectActiveOptionChangeAction {
    /** 键盘事件 向上移动 */
    ArrowUp = 0,
    /** 键盘事件 向下移动 */
    ArrowDown = 1,
    /** 鼠标事件 鼠标移动 */
    MouseMove = 2,
    /** 用户手动设置 */
    Manual = 3,
    /** 搜索之后选中第一个 */
    AfterSearch = 4
}
export type SelectOnActiveOptionChange<V extends SelectValue> = (activeKey: V | undefined, meta: {
    option: SelectOptionConfig<V> | undefined;
    action: SelectActiveOptionChangeAction;
}) => unknown;
/**
 * @public
 */
export type SelectOnSearch = (inputValue: string, meta: {
    action: SelectSearchAction;
}) => void;
/**
 * @public
 * Internal Select 的基础事件处理函数接口
 */
export interface BaseInternalSelectEventProps<V extends SelectValue, OP extends InternalSelectOptionConfig<V>> {
    /**
     * 下拉菜单的显示隐藏状态切换事件
     */
    onDropdownVisibleChange?: (visible: boolean) => void;
    /**
     * 输入框失去焦点事件回调
     */
    onBlur?: React.FocusEventHandler<Element>;
    /**
     * 输入框获取焦点事件回调
     */
    onFocus?: React.FocusEventHandler<Element>;
    /**
     * 清除按钮点击事件回调
     */
    onClear?: () => void;
    /**
     * 选中事件回调
     */
    onSelect?: (value: V, meta: {
        option: OP;
    }) => void;
    /**
     * 取消选中事件回调
     */
    onDeselect?: (value: V, meta: {
        option: OP;
    }) => void;
    /**
     * 鼠标移入输入区域事件回调
     */
    onMouseEnter?: React.MouseEventHandler<Element>;
    /**
     * 鼠标移出输入区域事件回调
     */
    onMouseLeave?: React.MouseEventHandler<Element>;
    /**
     * 输入框值变化的事件回调
     */
    onSearch?: SelectOnSearch;
    /**
     * 输入框按键抬起事件回调
     */
    onKeyUp?: React.KeyboardEventHandler<Element>;
    /**
     * 输入框按键按下事件回调
     */
    onKeyDown?: React.KeyboardEventHandler<Element>;
}
export interface BaseInternalSelectProps<V extends SelectValue, OP extends InternalSelectOptionConfig<V>> extends BaseInternalSelectEventProps<V, OP> {
    /**
     * 开启清除功能
     * @defaultValue true
     */
    allowClear?: boolean;
    /**
     * 自动聚焦
     * @defaultValue false
     */
    autoFocus?: boolean;
    /**
     * 是否为错误状态
     * @defaultValue false
     */
    error?: boolean;
    /**
     * 是否可以输入搜索
     * @defaultValue false
     */
    showSearch?: boolean;
    /**
     * 是否在选中项后清空搜索框，只在 `mode` 为 `multiple` 时有效
     * @defaultValue true
     */
    autoClearSearchValue?: boolean;
    /**
     * 是否为不可选中状态
     * @defaultValue false
     */
    disabled?: boolean;
    /**
     * 下拉选择输入框的占位符
     */
    placeholder?: string;
    /**
     * 尺寸
     * @defaultValue `medium`
     */
    size?: Extract<DefaultSizeType | DefaultExtraSizeType, 'medium' | 'large' | 'small'>;
    /**
     * 边框样式，提供 `boolean` 类型参数兼容其他 DS
     * @defaultValue `normal`
     */
    bordered?: 'normal' | 'noborder' | 'underlined' | boolean;
    /**
     * 下拉菜单是否和选择器输入框宽度相同，如果参数为 `number` 类型则作为下拉菜单的宽度。
     * @defaultValue false
     */
    dropdownMatchSelectWidth?: boolean | number;
    /**
     * `className` 属性透传
     */
    className?: string;
    /**
     * `style` 属性透传
     */
    style?: React.CSSProperties;
    /**
     * `id` 属性透传
     */
    id?: string;
    /**
     * 菜单渲染父节点。默认渲染到 body 上，如果你遇到菜单滚动定位问题，试试修改为滚动的区域，并相对其定位。
     */
    getPopupContainer?: PopperProps['getPopupContainer'];
    /**
     * 是否显示下拉小箭头
     * @defaultValue true
     */
    showArrow?: boolean;
    /**
     * 自定义选择框清空图标
     * @defaultValue `<EmptyFilled />`
     */
    clearIcon?: React.ReactNode;
    /**
     * @deprecated
     * 多选标签上的删除按钮
     */
    removeIcon?: React.ReactNode;
    /**
     * 自定义选择框后缀图标
     * @defaultValue `<DownBoldOutlined />`
     */
    suffixIcon?: React.ReactNode;
    /**
     * 下拉菜单的 `className` 属性透传
     */
    dropdownClassName?: string;
    /**
     * 下拉菜单的 `style` 属性透传
     */
    dropdownStyle?: React.CSSProperties;
    /**
     * 是否默认打开菜单
     * @defaultValue false
     */
    defaultOpen?: boolean;
    /**
     * 菜单打开的受控值
     */
    open?: boolean;
    /**
     * 加载状态
     * @defaultValue false
     */
    loading?: boolean;
    /**
     * @deprecated
     * 筛选所用的 prop 名称
     */
    optionFilterProp?: string;
    /**
     * 回填到选择框的选项属性值，默认是选项的子元素
     */
    optionLabelProp?: string | ((option: OP) => React.ReactNode);
    /**
     * `multiple` 模式下自动分词的分隔符
     */
    tokenSeparators?: string[];
    /**
     * 控制搜索文本
     */
    searchValue?: string;
    /**
     * 选择器输入标签的 `tabIndex` 属性透传
     */
    tabIndex?: number;
    /**
     * 自定义触发元素
     */
    triggerElement?: ReactElement | ((ctx: SelectContextType<V, OP>) => ReactElement);
    /**
     * 初始弹出方位
     * @defaultValue `bottom`
     */
    placement?: 'top' | 'bottom';
    /**
     * 初次渲染时是否强制 render 下拉框
     * @defaultValue `false`
     */
    forceRender?: PopperProps['forceRender'];
    /**
     * 隐藏下拉框后是否自动销毁
     * @defaultValue `false`
     */
    destroyOnHide?: PopperProps['destroyOnHide'];
    /**
     * 当搜索后下拉列表为空时显示的内容
     */
    notFoundContent?: React.ReactNode;
    /**
     * 当传进来的 Options 里头没东西时显示的内容。
     */
    noOptionsContent?: React.ReactNode;
    /**
     * 自定义下拉框内容
     */
    dropdownRender?: (menu: React.ReactElement) => React.ReactElement;
}
export interface RefSelectRef {
    /**
     * 选择器聚焦方法
     *
     * @public
     */
    focus: HTMLOrSVGElement['focus'];
    /**
     * 选择器失焦方法
     *
     * @public
     */
    blur: HTMLOrSVGElement['blur'];
    /**
     * 选择器输入框对应的 DOM 元素，是一个 getter
     *
     * @public
     */
    readonly input: HTMLInputElement | undefined | null;
}
export declare enum SelectActionType {
    Select = "Select",
    DeSelect = "DeSelect",
    Clear = "Clear",
    Set = "Set",
    Toggle = "Toggle",
    Checked = "Checked",
    UnChecked = "UnChecked",
    SelectAll = "SelectAll",
    DeSelectAll = "DeSelectAll"
}
export type SelectActonPayloadMap = {
    [SelectActionType.Select]: SelectValue;
    [SelectActionType.DeSelect]: SelectValue;
    [SelectActionType.Clear]: undefined;
    [SelectActionType.Set]: undefined;
    [SelectActionType.Toggle]: undefined;
    [SelectActionType.Checked]: SelectValue;
    [SelectActionType.UnChecked]: SelectValue;
    [SelectActionType.SelectAll]: SelectValue[];
    [SelectActionType.DeSelectAll]: SelectValue[];
};
export type TActionUnionize<T extends Record<string, unknown>> = {
    [P in keyof T]: TAction<P, T[P]>;
}[keyof T];
export type SelectAction = TActionUnionize<SelectActonPayloadMap>;
export type SingleInternalSelectProps<V extends SelectValue, OP extends InternalSelectOptionConfig<V>> = {
    /**
     * 下拉菜单模式，分为单选和多选两种
     */
    mode?: 'single';
    /**
     * 选项值的受控值
     */
    value?: V;
    /**
     * 默认的选项值
     */
    defaultValue?: V;
    /**
     * 下拉菜单选中值变化触发的回调
     */
    onChange?: (value: V | undefined, meta: {
        option: OP | undefined;
        action: SelectAction;
    }) => void;
};
export declare enum MultipleSelectTagType {
    TAG = 0,
    MAX_TAG = 1
}
export type MultipleSelectTagProps<V extends SelectValue> = {
    value: V;
    /**
     * 禁用状态。该模式下会有颜色变换，并且 `closable` 会失效，永远都为 `false`
     */
    disabled: boolean;
    children?: React.ReactNode;
    title?: string;
} & Required<Pick<TagProps, 'onClose' | 'closable'>>;
/**
 * @public
 */
export type MultipleSelectMaxTagProps = {
    /**
     * 是否禁用
     */
    disabled: boolean;
    /**
     * children
     */
    children?: React.ReactNode;
};
/**
 * @public
 */
export type TagRenderer<V extends SelectValue> = (props: MultipleSelectTagProps<V>) => React.ReactNode;
/**
 * @public
 */
export type MaxTagRenderer<V extends SelectValue> = (props: MultipleSelectMaxTagProps, tags: MultipleSelectTagProps<V>[]) => React.ReactNode;
/**
 * @public
 */
export interface MultipleInternalSelectProps<V extends SelectValue, OP extends InternalSelectOptionConfig<V>> {
    mode: 'multiple';
    /**
     * 最多显示多少个 Tag，响应式模式会对性能产生损耗
     */
    maxTagCount?: number | 'responsive';
    value?: V[];
    defaultValue?: V[];
    onChange?: (value: V[], meta: {
        option: OP[];
        action: SelectAction;
    }) => void;
    /**
     * 自定义最大标签内容渲染函数
     */
    maxTagRenderer?: MaxTagRenderer<V>;
    /**
     * 自定义标签内容渲染函数
     */
    tagRenderer?: TagRenderer<V>;
    /**
     * 自定义标签内容渲染函数，为了兼容其他 DS
     */
    tagRender?: TagRenderer<V>;
    /**
     * 多选选择器最大高度
     * @defaultValue 188
     */
    multipleSelectorMaxHeight?: string | number;
}
export type InternalSelectProps<V extends SelectValue, OP extends InternalSelectOptionConfig<V>> = BaseInternalSelectProps<V, OP> & StrictUnion<SingleInternalSelectProps<V, OP> | MultipleInternalSelectProps<V, OP>> & {
    overlayNode: React.ReactElement;
    options: OP[];
};
export interface SelectGeneratorConfig<V extends SelectValue, OP extends InternalSelectOptionConfig<V>> {
    selectorCssScope: string;
    dropdownCssScope: string;
    defaultOptionLabelProp?: (option: OP) => React.ReactNode;
}
