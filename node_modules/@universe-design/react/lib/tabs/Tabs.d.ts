import React from 'react';
import type { DropdownProps } from '../dropdown';
import type { TabsProps } from './TabsContext';
import './style';
export declare const TabMoreArrow: () => JSX.Element;
export declare const TabMoreContent: () => JSX.Element;
/**
 * `<TabMore />` 组件的属性
 * @public
 */
export interface TabMoreProps extends Omit<DropdownProps, 'overlay' | 'children' | 'visible' | 'onVisibleChange'> {
    /**
     * 子节点
     */
    children?: React.ReactNode | ((ctx: {
        omittedKeys: string[];
    }) => React.ReactNode);
}
export declare const TabMore: ({ children, ...dropDownProps }: TabMoreProps) => JSX.Element;
export declare const Tabs: React.ForwardRefExoticComponent<TabsProps & React.RefAttributes<HTMLDivElement>>;
