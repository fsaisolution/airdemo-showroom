"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isSlideEqual = exports.getSlideFromAbsoluteIndex = exports.getAbsoluteIndexFromSlide = void 0;
const isSlideEqual = (s1, s2) => s1.groupIndex === s2.groupIndex && s1.slideIndex === s2.slideIndex;
exports.isSlideEqual = isSlideEqual;
const getAbsoluteIndexFromSlide = (slide, slideCount) => slide.groupIndex * slideCount + slide.slideIndex;
exports.getAbsoluteIndexFromSlide = getAbsoluteIndexFromSlide;
const getSlideFromAbsoluteIndex = (absoluteIndex, total) => {
  const groupIndex = Math.floor(absoluteIndex / total);
  return {
    groupIndex,
    slideIndex: absoluteIndex - total * groupIndex
  };
};
exports.getSlideFromAbsoluteIndex = getSlideFromAbsoluteIndex;
//# sourceMappingURL=utils.js.map
