import type { CarouselManager } from '../CarouselManager';
import type { Slide, CarouselTransitionDescriptor } from './utils';
export declare class PushCarouselTransitionDescriptor implements CarouselTransitionDescriptor {
    defaults: {
        duration: number;
    };
    private _transitionTimingFunction;
    constructor(params?: {
        transitionTimingFunction?: string;
    });
    getSlidesTOBeRendered(manager: CarouselManager): Slide[];
    onGotoSlicklyStart(params: {
        slide: Slide;
        element: HTMLElement;
    }, manager: CarouselManager): void;
    onGotoSlicklyActive(params: {
        slide: Slide;
        element: HTMLElement;
    }, manager: CarouselManager): void | (() => void);
    onGotoSlicklyDone(params: {
        slide: Slide;
        element: HTMLElement;
    }, manager: CarouselManager): void;
    onGotoDirectly(params: {
        slide: Slide;
        element: HTMLElement;
    }, manager: CarouselManager): void;
    private _setPosition;
    private _getSlidePosition;
}
