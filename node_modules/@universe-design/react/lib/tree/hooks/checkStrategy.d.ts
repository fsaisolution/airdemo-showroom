import { type TKeyValueMap } from '../../util';
import { CheckedType } from '../interface';
import type { DoublyTreeNode } from '../util';
export type CheckedTypeCache = Map<string, CheckedType>;
export interface CheckStrategy {
    /**
     * 得到 onChange 输出的结果
     */
    cache2CheckedSet: (doublyTreeNodeMap: TKeyValueMap<DoublyTreeNode>, cache: CheckedTypeCache) => TKeyValueMap<boolean>;
    /**
     * 从 checkedSet 得到 chache 结果
     */
    checkedSet2Cache: (doublyTreeNodeMap: TKeyValueMap<DoublyTreeNode>, checkedSet: TKeyValueMap<boolean>) => CheckedTypeCache;
}
/**
 * show all
 */
export declare const checkStrategyShowAll: CheckStrategy;
/**
 * show parent
 * 如果所有的 children 都选中则只显示 parent
 */
export declare const checkStrategyShowParent: CheckStrategy;
/**
 * show children
 * 如果所有的 children 都只显示 parent
 */
export declare const checkStrategyShowChild: CheckStrategy;
export declare const strategyMap: {
    0: CheckStrategy;
    2: CheckStrategy;
    1: CheckStrategy;
};
