import type { Required } from 'utility-types';
import type { DoublyTreeNode, genDoublyTreeNodeMap } from '../util';
import type { TKeyValueMap } from '../../util';
import type { TreeProps } from '../interface';
/**
 * 1. 默认使用 defaultExpandAll，defaultExpandKeys 和 expandKeys 去计算 expandedMap
 * 2. 点击 expand icon，setExpandKeys
 * 3. 如果有 searchValue，说明是 search 模式，第一次出来的时候通过搜索结果去展开对应的选项
 * 4. 点击 expand icon，修改 searchExpandedMap，
 */
export declare function useTreeExpand(props: ReturnType<typeof genDoublyTreeNodeMap> & Required<Pick<TreeProps, 'defaultExpandedKeys' | 'expandedKeys' | 'defaultExpandAll' | 'onExpand' | 'filterTreeNode' | 'searchValue'>, 'filterTreeNode'>): {
    expandedMap: TKeyValueMap<boolean>;
    toggleExpanded: (key: string) => void;
    finalTreeNodeList: DoublyTreeNode[];
    filteredTreeNodes: Set<DoublyTreeNode>;
    visibleMap: TKeyValueMap<boolean>;
    finalTreeNodeListRef: {
        current: DoublyTreeNode[];
    };
};
