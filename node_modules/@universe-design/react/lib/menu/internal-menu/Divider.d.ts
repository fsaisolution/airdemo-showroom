import * as React from 'react';
export interface RcDividerProps {
    className?: string;
    style?: React.CSSProperties;
}
export default function RcDivider({ className, style }: RcDividerProps): React.JSX.Element | null;
