import type { ReactElement } from 'react';
import React from 'react';
import type { PositionType } from '../util';
import type { RcMenuInfo } from '../menu';
import type { UDAlignType } from '../shared/components/trigger';
import type { PopperProps, PopperRef } from '../shared/components/popper';
import './style';
/**
 * @public
 */
export type DropdownPlacementType = Exclude<PositionType, 'leftCenter' | 'rightCenter'>;
/**
 * @public
 * @docgen
 */
export interface DropdownProps extends Pick<PopperProps, 'getPopupContainer' | 'mouseEnterDelay' | 'mouseLeaveDelay' | 'showAction' | 'hideAction' | 'disabled' | 'modifiers' | 'destroyOnHide' | 'visible' | 'defaultVisible' | 'onVisibleChange' | 'openClassName' | 'udTriggerRef' | 'trigger' | 'offset' | 'popupPortalContainer' | 'flip' | 'shift' | 'enableAutoHide' | 'eventListeners' | 'enableMaxHeight' | 'middlewares' | 'inline' | 'boundary' | 'initialFocus' | 'enableFocusLock' | 'enableRestoreFocus' | 'boundaryPadding'> {
    /**
     * 点击弹层事件回调
     */
    onOverlayClick?: (ev: RcMenuInfo) => void;
    prefixCls?: string;
    /**
     * 弹层的 `className` 属性透传
     */
    overlayClassName?: string;
    /**
     *  弹层的 `style` 属性透传
     */
    overlayStyle?: React.CSSProperties;
    /**
     * 完全自定义对齐规则，会与内建对齐规则合并
     */
    align?: UDAlignType;
    /**
     * 内置方位
     */
    placement?: DropdownPlacementType;
    /**
     * 弹层。`overlay`菜单使用：[导航菜单 Navigation Menu](/develop/react/menu)
     */
    overlay: (() => React.ReactElement) | React.ReactElement;
    children: ReactElement;
}
export declare const Dropdown: React.ForwardRefExoticComponent<DropdownProps & React.RefAttributes<PopperRef>>;
