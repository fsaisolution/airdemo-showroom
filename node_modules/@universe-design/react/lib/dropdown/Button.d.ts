import React from 'react';
import type { ButtonProps } from '../button';
import type { DropdownProps } from './Dropdown';
import './style';
/**
 * @public
 * @docgen
 * <Dropdown.Button> 的属性和方法。
 */
export interface DropdownButtonProps extends Omit<DropdownProps, 'children'>, Pick<ButtonProps, 'size' | 'type' | 'htmlType' | 'icon' | 'href' | 'title'>, React.PropsWithChildren<unknown> {
    /**
     * 按钮组的 `style` 属性透传
     */
    style?: React.CSSProperties;
    /**
     * 按钮组的 `className` 属性透传
     */
    className?: string;
    /**
     * 左按钮的点击事件回调
     */
    onClick?: React.MouseEventHandler<HTMLButtonElement>;
    /**
     * 按钮组的自定义渲染函数
     */
    buttonsRender?: (buttons: [React.ReactNode, React.ReactElement]) => [React.ReactNode, React.ReactElement];
}
export declare const DropdownButton: React.FC<DropdownButtonProps>;
