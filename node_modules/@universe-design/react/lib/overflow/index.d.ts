import type { CSSProperties, Key } from 'react';
import React from 'react';
import './style';
export interface OverflowProps<K extends Key> {
    style?: CSSProperties;
    getItemStyle?: (key: K) => CSSProperties;
    restStyle?: CSSProperties;
    className?: string;
    keys: K[];
    maxCount?: number | 'responsive';
    itemRenderer?: (key: K) => React.ReactNode;
    restRenderer?: (omittedKeys: K[]) => React.ReactNode;
    suffixRenderer?: (ctx: {
        omittedKeys: K[];
    }) => React.ReactNode;
    addonRenderer?: (ctx: {
        omittedKeys: K[];
    }) => React.ReactNode;
    suffixClassName?: string;
    restPosition?: number;
    /**
     * 在非 responsive 模式下也进行 resize observe
     * @internal
     */
    alwaysObserveResize?: boolean;
    /**
     * 是否允许 suffix 换行
     * @defaultValue true
     */
    suffixWrap?: boolean;
}
export declare const Overflow: <K extends Key = Key>({ maxCount, keys, style, getItemStyle, restStyle, className, itemRenderer, restRenderer, suffixRenderer, suffixClassName, restPosition, addonRenderer, alwaysObserveResize, suffixWrap, }: OverflowProps<K>) => React.JSX.Element;
