import type { Path, MultiLevelPath } from '../shared';
import type { UseFieldProps, UseFieldInternalReturnValue } from './useFieldInternal';
/** Form List 配置 */
export type UseFormListProps = Pick<UseFieldProps, 'validate'> & {
    /**
     * 表单列表注册的字段名称
     */
    name: Path;
    rowKey?: string;
};
/** FormList 返回行数据(fields) */
export type FormListField = {
    /** 字段名称，多级路径类型 */
    name: MultiLevelPath;
    key: string;
    /** 删除当前字段 */
    remove: () => void;
};
/** FormList 操作帮助方法 */
export type FormListActions<FieldValue> = {
    /** 删除一行 */
    remove: (index: number) => void;
    /** 往后面添加 n 行 */
    push: (...appendItems: FieldValue[]) => void;
    /** 往前面添加 n 行 */
    unshift: (...prependItems: FieldValue[]) => void;
    /** 在 `index` 位置插入 n 行 */
    insert: (index: number, ...insertItems: FieldValue[]) => void;
    /** 移动行 */
    move: (from: number, to: number) => void;
    /** 交换 2 行 */
    swap: (swapItemIndex1: number, swapItemIndex2: number) => void;
};
/**
 * @docgen
 * @public
 * @typeParam FieldValue - 字段值
 */
export type UseFormListReturnValue<FieldValue> = Pick<UseFieldInternalReturnValue, 'error' | 'validateField'> & {
    /**
     * 表单列表的所有字段
     */
    fields: FormListField[];
    /**
     * 表单列表的可用操作
     */
    actions: FormListActions<FieldValue>;
};
export declare function useFormList<FieldValue = any, Values = Record<any, any>>(options: UseFormListProps): UseFormListReturnValue<FieldValue>;
