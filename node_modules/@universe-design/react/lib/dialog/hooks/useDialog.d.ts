/**
 * @file useDialog
 */
import React from 'react';
import type { ConfirmProps } from '../Confirm';
import type { ModalProps } from '../Modal';
import type { DialogIsntance } from './RefDialog';
/**
 * useDialog 可以生成能获取到 context 的 dialog 实例
 */
export declare function useDialog(): readonly [{
    confirm: (props: ConfirmProps) => DialogIsntance<ConfirmProps>;
    info: (props: ConfirmProps) => DialogIsntance<ConfirmProps>;
    success: (props: ConfirmProps) => DialogIsntance<ConfirmProps>;
    error: (props: ConfirmProps) => DialogIsntance<ConfirmProps>;
    warn: (props: ConfirmProps) => DialogIsntance<ConfirmProps>;
    open: (props: ModalProps) => DialogIsntance<ModalProps>;
}, React.JSX.Element];
