import type { RGB, HSV, HSL, ColorParam, Color } from './type';
import type { ThemeMode } from '../shared';
export declare function formatColor(data: string | RGB | HSV | HSL): Color;
export declare function judgeNeedHighLight(color: string, themeMode: ThemeMode): boolean;
export declare function judgeEmptyColorGroup(colorGroup: ColorParam[][]): boolean;
export declare function defaultShowDarkCheckmark(color: string): boolean;
