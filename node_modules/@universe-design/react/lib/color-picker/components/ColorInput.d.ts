import type { FC } from 'react';
import { HrgbKey } from '../type';
interface ColorInputProps {
    color: number | string;
    colorKey: HrgbKey;
    onChange: (key: HrgbKey, val: string | number) => void;
    prefixCls?: string;
}
export declare const ColorInput: FC<ColorInputProps>;
export {};
