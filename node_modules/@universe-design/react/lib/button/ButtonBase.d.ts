import type { ReactNode } from 'react';
import React from 'react';
import type { DefaultSizeType, DefaultExtraSizeType } from '../util';
import './style';
/**
 * Deprecated 组件的分类：https://bytedance.feishu.cn/docs/doccngkxeLeLStbebeToeKkPW7b#
 * Breaking change
 * - 重写 type，type 表示按钮类型，每种类型有自己的限制条件，如:
 *  - filled 限制背景改变，color 不变
 *  - outlined 限制背不变
 *  - ghost 限制背景透明
 * - 重写样式，降低样式的层叠优先级
 * - 修正 ButtonLink, LinkButton 的实现
 * - 去掉部分 Button 负 margin
 * - 去掉 info danger 属性，统一使用 color
 *  - info => color="primary"
 *  - danger => color="danger"
 * - 去掉 ghost 属性，ghost => type="ghost"
 *
 */
/**
 * @public
 */
export interface BaseButtonProps {
    prefixCls?: string;
    /**
     * 设置按钮类型
     * @defaultValue `outlined`
     */
    type?: 'filled' | 'primary' | 'outlined' | 'default' | 'text' | 'text-secondary' | 'icon' | 'link';
    /**
     * 设置按钮形状
     */
    shape?: 'circle' | 'square' | 'round';
    /**
     * 设置按钮大小
     * @defaultValue `medium`
     */
    size?: DefaultSizeType | DefaultExtraSizeType;
    /**
     * 设置按钮颜色
     */
    color?: 'primary' | 'danger';
    /**
     * 将按钮宽度调整为其父宽度的选项
     * @defaultValue false
     */
    block?: boolean;
    /**
     * 设置按钮失效状态
     * @defaultValue false
     */
    disabled?: boolean;
    /**
     * 设置按钮载入状态
     * @defaultValue false
     */
    loading?: boolean;
    /**
     * 延迟显示加载状态
     */
    delay?: number;
    /**
     * 图标按钮选中模式
     * @defaultValue false
     */
    selected?: boolean;
    /**
     * 设置按钮的图标组件
     */
    icon?: ReactNode;
    endIcon?: ReactNode;
    noMinWidth?: boolean;
    children?: React.ReactNode;
    danger?: boolean;
    info?: boolean;
    /**
     * 选中后是否自动失焦
     * @defaultValue false
     * @deprecated
     */
    autoBlur?: boolean;
}
export type ButtonType = BaseButtonProps['type'];
export interface AnchorButtonProps extends Omit<React.AnchorHTMLAttributes<HTMLAnchorElement>, 'type' | 'onClick' | 'color'>, BaseButtonProps {
    /**
     * 点击跳转的地址，指定此属性按钮的行为和 a 链接一致
     */
    href: string;
    /**
     * 相当于 a 链接的`target`属性，`href`存在时生效
     */
    target?: string;
    /**
     * 点击按钮时的回调
     */
    onClick?: React.MouseEventHandler<HTMLElement>;
}
export type ButtonSize = NonNullable<BaseButtonProps['size']>;
export interface NativeButtonProps extends Omit<React.ButtonHTMLAttributes<HTMLButtonElement>, 'type' | 'onClick' | 'color'>, BaseButtonProps {
    /**
     * 设置 `button` 标签的 `type` 值，可选值请参考 [HTML 标准](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/button#attr-type)
     * @defaultValue `button`
     */
    htmlType: React.ButtonHTMLAttributes<HTMLButtonElement>['type'];
    onClick?: React.MouseEventHandler<HTMLElement>;
}
/**
 * 支持原生`button`的所有事件和属性。
 * @public
 * @docgen
 */
export type ButtonProps = Partial<AnchorButtonProps & NativeButtonProps>;
export type ButtonRefType = HTMLButtonElement | HTMLAnchorElement | undefined;
declare const ButtonCmp: React.ForwardRefExoticComponent<Partial<AnchorButtonProps & NativeButtonProps> & React.RefAttributes<ButtonRefType>>;
export type ButtonCmpType = typeof ButtonCmp & {
    __UD_BUTTON?: boolean;
};
declare const ButtonBase: import("../shared/markAsDisabledableComp").DisabledableComp<ButtonCmpType>;
export { ButtonBase };
