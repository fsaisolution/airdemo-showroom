"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.SliderSteps = void 0;
var _clsx = _interopRequireDefault(require("clsx"));
var _react = _interopRequireDefault(require("react"));
var _hooks = require("../config-provider/hooks");
const SliderSteps = props => {
  const {
    max,
    min,
    range,
    dots = [],
    value,
    rangeValue,
    reverse,
    vertical,
    included
  } = props;
  const cssScope = (0, _hooks.useCssScope)('slider');
  const stepsCls = (0, _clsx.default)(cssScope('__steps'));
  return dots && dots.length > 0 ? /*#__PURE__*/_react.default.createElement("div", {
    className: stepsCls
  }, (() => {
    if (dots) {
      return dots.sort((a, b) => a - b).map(item => {
        const isActive = range ? rangeValue && item >= rangeValue[0] && item <= rangeValue[1] : value && item <= value;
        const cls = (0, _clsx.default)(cssScope('__steps-item'), {
          [cssScope('__steps-item--active')]: isActive && included
        });
        const point = item;
        const style = vertical ? {
          [reverse ? 'top' : 'bottom']: `${(point - min) / (max - min) * 100}%`
        } : {
          [reverse ? 'right' : 'left']: `${(point - min) / (max - min) * 100}%`
        };
        return /*#__PURE__*/_react.default.createElement("div", {
          key: `ud-slider-step-${item}`,
          style: style,
          className: cls
        });
      });
    }
    return null;
  })()) : null;
};
exports.SliderSteps = SliderSteps;
//# sourceMappingURL=steps.js.map
