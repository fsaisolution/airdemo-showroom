import { mat2d, vec2 } from 'gl-matrix';
/**
 * mutable 方法，可以对变换后的 image rect 进行限制
 */
export declare const clampPoints: (nextPoints: Points, { containerRect, }: {
    /**
     * 容器矩形
     */
    containerRect: DOMRect;
}) => Points;
/**
 * 解线性方程得到两个三角之间的变化矩阵
 */
export declare const getTransformBetweenTwoRects: (points1: Points, points2: Points) => mat2d;
export type Points = [leftTop: vec2, rightTop: vec2, leftBottom: vec2];
export declare const rectToPoints: (rect: DOMRect) => Points;
export declare const pointsToRect: (points: Points) => DOMRect;
export declare const getMatScale: (mat: mat2d) => number;
export declare const getMatRotationRadians: (mat: mat2d) => number;
/**
 * 判断点是否在矩形内
 *
 * @param rect - 矩形区域
 * @param points - 点集合
 * @returns
 */
export declare const isPointsInRectangleRegion: (rect: DOMRect, points: Points) => boolean;
