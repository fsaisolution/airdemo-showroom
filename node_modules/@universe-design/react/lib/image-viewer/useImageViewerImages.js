"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useImageViewerImages = void 0;
var _reactUtilities = require("@universe-design/react-utilities");
/**
 * ================================
 *
 * useImageViewerImages
 *
 * ================================
 */

/**
 * 与图片查看器图片控制相关的数据逻辑
 */
const useImageViewerImages = props => {
  var _index$defaultValue;
  const {
    images,
    index
  } = props;
  const [currentIndex, setCurrentIndex] = (0, _reactUtilities.useControlledState)((_index$defaultValue = index === null || index === void 0 ? void 0 : index.defaultValue) !== null && _index$defaultValue !== void 0 ? _index$defaultValue : 0, index === null || index === void 0 ? void 0 : index.value, {
    onChange: index === null || index === void 0 ? void 0 : index.onChange
  });
  const image = images[currentIndex];
  return {
    state: {
      currentIndex,
      setCurrentIndex,
      image
    }
  };
};

/**
 * 图片查看器中的图片
 */

/**
 * 图片查看器的图片下标
 */
exports.useImageViewerImages = useImageViewerImages;
//# sourceMappingURL=useImageViewerImages.js.map
