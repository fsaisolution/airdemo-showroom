import type { ThrottleSettings } from 'lodash';
export declare function useThrottleFn<T extends (...args: any[]) => any>(fn: T, wait?: number, options?: ThrottleSettings): {
    run: T;
    cancel: () => void;
    flush: () => ReturnType<T>;
};
