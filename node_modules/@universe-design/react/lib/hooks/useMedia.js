"use strict";

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getDefaultMediaMatches = getDefaultMediaMatches;
exports.useMedia = void 0;
var React = _interopRequireWildcard(require("react"));
var _util = require("../util");
var _useMountedState = require("./useMountedState");
var _reactUtilities = require("@universe-design/react-utilities");
function getDefaultMediaMatches(rawQuery) {
  if (!(0, _reactUtilities.isClient)()) {
    return false;
  }
  return window.matchMedia(rawQuery).matches;
}
const useMedia = function (rawQuery) {
  let {
    onChange,
    skipped = false
  } = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
  const [state, setState] = React.useState(() => getDefaultMediaMatches(rawQuery));
  const isMounted = (0, _useMountedState.useMountedState)();
  React.useEffect(() => {
    if (skipped) {
      return;
    }
    const mediaQueryList = window.matchMedia(rawQuery);
    const handleChange = () => {
      if (!isMounted()) {
        return;
      }
      setState(Boolean(mediaQueryList.matches));
      (0, _util.isFunction)(onChange) && onChange();
    };
    mediaQueryList.addListener(handleChange);
    setState(mediaQueryList.matches);
    return () => {
      mediaQueryList.removeListener(handleChange);
    };
  }, [isMounted, onChange, rawQuery, skipped]);
  return state;
};
exports.useMedia = useMedia;
//# sourceMappingURL=useMedia.js.map
