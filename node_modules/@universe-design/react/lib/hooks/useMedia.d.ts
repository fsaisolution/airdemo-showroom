import type { HookSharedOptions } from '../util';
export declare function getDefaultMediaMatches(rawQuery: string): boolean;
export declare const useMedia: (rawQuery: string, { onChange, skipped }?: {
    onChange?: () => void;
} & HookSharedOptions) => boolean;
