import type { VirtualElement } from '@floating-ui/core';
import type { HookSharedOptions } from '../util';
import type { ThunkElement } from '../util/getThunkElement';
export declare const getElementFromRefOrVirtualElement: <T extends Node>(elementOrRef: ThunkElement<T | VirtualElement>) => T | null;
export declare function useComplementEvent<K extends keyof DocumentEventMap>(type: K, { listener, elements, options, skipped, }: {
    listener: (ev: DocumentEventMap[K]) => any;
    elements: ThunkElement<Node | VirtualElement>[];
    options?: boolean | AddEventListenerOptions;
} & HookSharedOptions): void;
