import * as React from 'react';
import type { EqualFunc } from '@universe-design/react-utilities';
/**
 * 半受控模式
 * @internal
 */
export declare function useSyncState<T>(propState: T, { isEqual, }?: {
    isEqual?: EqualFunc<T>;
}): readonly [T, React.Dispatch<React.SetStateAction<T>>, React.MutableRefObject<T>];
export default useSyncState;
