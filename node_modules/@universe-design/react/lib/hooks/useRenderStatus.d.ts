export declare enum RenderStatus {
    /**
     * 放弃此次渲染
     */
    discard = "discard",
    /**
     * 渲染此次内容
     */
    active = "active",
    /**
     * 渲染并缓存内容 （display: none）
     */
    cache = "cache"
}
/**
 * 判断是否要渲染此次内容
 * @param forceRender 是否第一次渲染
 * @param destroyOnHide 是否退出后销毁
 */
export declare function useRenderStatus(params: {
    forceRender?: boolean;
    destroyOnHide?: boolean;
    active?: boolean;
}): RenderStatus;
