"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useRenderEffect = useRenderEffect;
var _react = _interopRequireDefault(require("react"));
var _util = require("../util");
var _useIsomorphicLayoutEffect = require("./useIsomorphicLayoutEffect");
/**
 * 可以比 useLayoutEffect 更早执行，但是父子节点的执行顺序与 useLayoutEffect 是相反的。这个方法要慎用，因为 strict mode render 会跑两次
 */
function useRenderEffect(effect) {
  const isFirstRenderRef = _react.default.useRef(true);
  const destructorRef = _react.default.useRef();
  const prevEffect = _react.default.useRef();
  if (isFirstRenderRef.current || prevEffect.current !== effect) {
    if ((0, _util.isFunction)(destructorRef.current)) {
      destructorRef.current();
    }
    destructorRef.current = effect();
    prevEffect.current = effect;
  }
  if (isFirstRenderRef.current) {
    isFirstRenderRef.current = false;
  }
  (0, _useIsomorphicLayoutEffect.useIsomorphicLayoutEffect)(() => () => {
    if ((0, _util.isFunction)(destructorRef.current)) {
      destructorRef.current();
      destructorRef.current = undefined;
    }
    prevEffect.current = undefined;
  }, []);
}
//# sourceMappingURL=useRenderEffect.js.map
