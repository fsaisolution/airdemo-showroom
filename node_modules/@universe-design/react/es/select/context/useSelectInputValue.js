import { useControlledState } from '@universe-design/react-utilities';
import { useEventCallback } from '../../hooks';
import { isFunction } from '../../util';
import { SelectSearchAction } from '../internal.interface';
/**
 * @public
 */
export function useSelectInputValue(_ref) {
  let {
    searchValue,
    onSearch
  } = _ref;
  const [inputValue, _setInputValue] = useControlledState('', searchValue);
  const setInputValue = useEventCallback(function (v) {
    let meta = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {
      action: SelectSearchAction.Input
    };
    _setInputValue(v, nextInputValue => {
      isFunction(onSearch) && onSearch(nextInputValue, meta);
    });
  });
  return {
    inputValue,
    setInputValue
  };
}
//# sourceMappingURL=useSelectInputValue.js.map
