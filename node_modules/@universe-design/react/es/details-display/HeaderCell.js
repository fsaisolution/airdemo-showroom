/** @jsxRuntime classic */
/** @jsx jsx */
import React from 'react';
// eslint-disable-next-line @typescript-eslint/no-unused-vars
import { jsx } from '@universe-design/stylex/react';
import { useCellStylexStyles } from './sx';
import { useDetailsDisplayContext } from './DetailsDisplayContext';
import { useDetailsDisplayItemContext } from './DetailsDisplayItemContext';
const HeaderCell = _ref => {
  let {
    children
  } = _ref;
  const {
    align,
    prefix,
    size,
    layout
  } = useDetailsDisplayContext();
  const {
    isRowEnd,
    isColumnEnd,
    span
  } = useDetailsDisplayItemContext();
  const vertical = layout === 'vertical';
  const styles = useCellStylexStyles({
    prefix: `${prefix}__header-cell`
  });
  return jsx("th", {
    sx: [styles.th, align === 'center' && styles.align.right, align === 'justify' && styles.align.left, align === 'start' && styles.align.left, vertical && [styles.headerGap, !isColumnEnd && styles.layout.verticalGap], !vertical && !isRowEnd && styles.size[size]],
    colSpan: span
  }, children);
};
export default HeaderCell;
//# sourceMappingURL=HeaderCell.js.map
