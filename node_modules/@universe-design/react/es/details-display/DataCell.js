/** @jsxRuntime classic */
/** @jsx jsx */
import React from 'react';
// eslint-disable-next-line @typescript-eslint/no-unused-vars
import { jsx } from '@universe-design/stylex/react';
import { useCellStylexStyles } from './sx';
import { useDetailsDisplayContext } from './DetailsDisplayContext';
import { useDetailsDisplayItemContext } from './DetailsDisplayItemContext';
const DataCell = _ref => {
  let {
    children
  } = _ref;
  const {
    align,
    prefix,
    size,
    layout
  } = useDetailsDisplayContext();
  const {
    isRowEnd,
    isColumnEnd,
    span
  } = useDetailsDisplayItemContext();
  const vertical = layout === 'vertical';
  const styles = useCellStylexStyles({
    prefix: `${prefix}__data-cell`
  });
  return jsx("td", {
    sx: [styles.td, align === 'center' && styles.align.left, align === 'justify' && styles.align.right, align === 'start' && styles.align.left, !isRowEnd && styles.size[size], !isColumnEnd && [vertical && styles.layout.verticalGap, !vertical && styles.layout.horizontalGap], layout === 'inline' && styles.inlineFlex],
    colSpan: span
  }, children);
};
export default DataCell;
//# sourceMappingURL=DataCell.js.map
