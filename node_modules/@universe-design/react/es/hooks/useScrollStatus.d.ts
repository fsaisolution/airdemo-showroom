import type { ThunkElement } from '../util/getThunkElement';
/**
 * 检测滚动容器的滚动状态
 */
export declare function useScrollStatus<T extends HTMLElement = HTMLDivElement>(params?: {
    /** 禁用水平滚动监听 */
    disableHorizontal?: boolean;
    /** 禁用竖直滚动监听 */
    disableVertical?: boolean;
}): {
    checkOverflow: (target: ThunkElement<T>) => void;
    overflow: {
        t: boolean;
        b: boolean;
        l: boolean;
        r: boolean;
    };
};
