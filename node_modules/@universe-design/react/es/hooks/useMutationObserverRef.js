import { useRef } from 'react';
import { useLatest } from './useLatest';
import { useProxyRef } from '@universe-design/react-utilities';
const config = {
  attributes: true,
  characterData: true,
  childList: true,
  subtree: true
};
export function useMutationObserverRef(callback) {
  let {
    options = config,
    skipElement
  } = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
  const callbackRef = useLatest(callback);
  const observerRef = useRef(null);
  const ref = useProxyRef(null, node => {
    var _observerRef$current;
    if (!node || skipElement && skipElement(node)) {
      return;
    }
    (_observerRef$current = observerRef.current) === null || _observerRef$current === void 0 ? void 0 : _observerRef$current.disconnect();
    const observer = new MutationObserver(callbackRef.current);
    observer.observe(node, options);
    observerRef.current = observer;
  });
  return ref;
}
//# sourceMappingURL=useMutationObserverRef.js.map
