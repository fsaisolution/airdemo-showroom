import React from 'react';
/**
 * 可以比 useLayoutEffect 更早执行，但是父子节点的执行顺序与 useLayoutEffect 是相反的。这个方法要慎用，因为 strict mode render 会跑两次
 */
export declare function useRenderEffect(effect: React.EffectCallback): void;
