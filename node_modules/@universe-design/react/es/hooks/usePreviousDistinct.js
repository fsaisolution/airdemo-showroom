import { useRef } from 'react';
import { useFirstMountState } from './useFirstMountState';
const strictEquals = (prev, next) => prev === next;
export function usePreviousDistinct(value) {
  let compare = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : strictEquals;
  const prevRef = useRef();
  const curRef = useRef(value);
  const isFirstMount = useFirstMountState();
  if (!isFirstMount && !compare(curRef.current, value)) {
    prevRef.current = curRef.current;
    curRef.current = value;
  }
  return prevRef.current;
}
//# sourceMappingURL=usePreviousDistinct.js.map
