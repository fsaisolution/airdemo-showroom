/* eslint-disable max-depth */

import React, { forwardRef, memo, useCallback, useImperativeHandle } from 'react';
import { useForm } from './internal/useFormInternal';
import { FormInternalContextProvider } from './internal/FormInternalContext';

/**
 * @public
 */

export const Form = /*#__PURE__*/memo(/*#__PURE__*/forwardRef(function (_ref, ref) {
  let {
    className,
    component: F = 'form',
    validate,
    initialValues,
    onSubmit,
    onValuesChange,
    children,
    debug
  } = _ref;
  const formCtx = useForm({
    initialValues,
    validate,
    onSubmit,
    onValuesChange,
    debug
  });
  const {
    submit
  } = formCtx;
  useImperativeHandle(ref, () => formCtx, [formCtx]);

  // ================ Handle submit ================
  const handleSubmit = useCallback(e => {
    e.preventDefault();
    e.stopPropagation();
    submit();
  }, [submit]);
  const childNode = children;
  if (!F) {
    return /*#__PURE__*/React.createElement(FormInternalContextProvider, {
      value: formCtx
    }, childNode);
  }
  return /*#__PURE__*/React.createElement(FormInternalContextProvider, {
    value: formCtx
  }, /*#__PURE__*/React.createElement(F, {
    className: className,
    onSubmit: handleSubmit
  }, childNode));
}));
//# sourceMappingURL=Form.js.map
