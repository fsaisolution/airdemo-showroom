import pick from 'lodash/pick';
import { useFormInternalContext, useFormInternalContextSelector, FormInternalContextProvider } from './internal/FormInternalContext';

/**
 * @public
 * @typeParam Values - values of this form
 */

export const FormContextProvider = FormInternalContextProvider;

/**
 * useFormContextSelector
 * get form context, similar to redux selector
 */
export function useFormContextSelector(selector) {
  return useFormInternalContextSelector(selector);
}

/**
 * useFormContext
 * get from context
 */
export function useFormContext() {
  const formInternalCtx = useFormInternalContext();
  return pick(formInternalCtx, ['values', 'errors', 'touched', 'setValues', 'setFieldsValue', 'setFieldValue', 'setErrors', 'resetErrors', 'setFieldsError', 'setFieldError', 'submit', 'reset', 'isSubmitting', 'isValidating', 'isDirty', 'validateFields']);
}
//# sourceMappingURL=FormContext.js.map
