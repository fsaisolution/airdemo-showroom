import { freeze as immerFreeze, Immer } from 'immer';

// https://immerjs.github.io/immer/performance/
// freeze 会对性能造成影响，并且对用户造成困扰，所以我们默认关闭 autoFreeze
const immer = new Immer({
  autoFreeze: false
});
const {
  produce
} = immer;
const setAutoFreeze = immer.setAutoFreeze.bind(immer);
const freeze = obj => immer.autoFreeze_ ? immerFreeze(obj, true) : obj;
export { immer, produce, freeze, setAutoFreeze };
//# sourceMappingURL=formImmer.js.map
