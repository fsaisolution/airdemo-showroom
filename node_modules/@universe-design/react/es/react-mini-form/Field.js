/* eslint-disable max-depth */
import without from 'lodash/without';
import pick from 'lodash/pick';
import React, { cloneElement, isValidElement, memo } from 'react';
import { shallowEqualObjects } from 'shallow-equal';
import { isFunction, isReactHTMLElement, isReactInputElement, omit } from '../util';
import { useFieldInternal } from './internal/useFieldInternal';
import { marshalNamePath } from './shared';
import { useEventCallback } from '../hooks/useEventCallback';
export function useField(props) {
  const returnValue = useFieldInternal(props);
  return pick(returnValue, ['value', 'error', 'handleChange']);
}
export const FieldPropsAreEqual = (prevProps, nextProps) => {
  if (!shallowEqualObjects(omit(prevProps, ['name']), omit(nextProps, ['name']))) {
    return false;
  }
  if (prevProps.name === undefined && nextProps.name === undefined || prevProps.name !== undefined && nextProps.name !== undefined && marshalNamePath(prevProps.name) === marshalNamePath(nextProps.name)) {
    return true;
  }
  return false;
};
export const Field = /*#__PURE__*/memo(function (props) {
  const {
    children,
    valuePropName,
    validateStatus
  } = props;
  const fieldCtx = useFieldInternal({
    ...props
  });
  const {
    value,
    handleChange,
    error,
    allValidateTriggers,
    trigger,
    validateField
  } = fieldCtx;
  const isError = validateStatus === 'error' || Boolean(error);
  /**
   * clone 给子元素的 handleChange 函数
   */
  const composedHandleChange = useEventCallback(function () {
    handleChange(...arguments);
    if ('props' in children && trigger in children.props && isFunction(children.props[trigger])) {
      children.props[trigger](...arguments);
    }
  });

  /**
   * 所有 trigger validateTrigger 相关的回调函数
   */
  const mergedChildProps = (() => {
    const mergedTrigger = 'props' in children ? {
      ...children.props
    } : {};
    if ('props' in children) {
      mergedTrigger[trigger] = composedHandleChange;
    }
    without(allValidateTriggers, trigger).forEach(triggerName => {
      const originTrigger = mergedTrigger[triggerName];
      mergedTrigger[triggerName] = function () {
        if (isFunction(originTrigger)) {
          originTrigger(...arguments);
        }
        validateField({
          triggerName
        });
      };
    });
    return mergedTrigger;
  })();
  if (isFunction(children)) {
    return /*#__PURE__*/React.createElement(React.Fragment, null, children(pick(fieldCtx, ['value', 'error', 'handleChange'])));
  } else if (/*#__PURE__*/isValidElement(children)) {
    if (isReactHTMLElement(children)) {
      // html tags
      if (isReactInputElement(children)) {
        switch (children.props.type) {
          case 'radio':
          case 'checkbox':
            return /*#__PURE__*/cloneElement(children, {
              [valuePropName !== null && valuePropName !== void 0 ? valuePropName : 'checked']: value,
              ...mergedChildProps
            });
          case 'file':
            throw new Error('Field cannot be used on file inputs since they are read-only. Use a onChange listener instead.');
          default:
            return /*#__PURE__*/cloneElement(children, {
              [valuePropName !== null && valuePropName !== void 0 ? valuePropName : 'value']: value,
              ...mergedChildProps
            });
        }
      } else {
        return /*#__PURE__*/cloneElement(children, {
          [valuePropName !== null && valuePropName !== void 0 ? valuePropName : 'value']: value,
          ...mergedChildProps
        });
      }
    }
    return /*#__PURE__*/cloneElement(children, {
      [valuePropName !== null && valuePropName !== void 0 ? valuePropName : 'value']: value,
      error: isError,
      ...mergedChildProps
    });
  } else {
    return children;
  }
}, FieldPropsAreEqual);
if (process.env.NODE_ENV !== "production") {
  Field.displayName = 'Field';
}
//# sourceMappingURL=Field.js.map
