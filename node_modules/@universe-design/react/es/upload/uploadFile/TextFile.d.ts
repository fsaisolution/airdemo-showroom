import React from 'react';
import type { FileProps } from './interface';
import '../style/text-file.less';
export declare const TextFileAction: {
    Download: () => React.JSX.Element | null;
    Retry: () => React.JSX.Element | null;
    Preview: () => React.JSX.Element | null;
    Remove: () => React.JSX.Element | null;
};
/**
 * 与 Upload 无关的 File 组件
 */
export interface TextFileProps extends FileProps {
    /**
     * 是否是简略模式
     */
    isSimple?: boolean;
    /**
     * 自定义操作按钮，如果设置则不显示内置按钮
     */
    action?: React.ReactNode;
}
export declare function TextFile(props: TextFileProps): JSX.Element;
