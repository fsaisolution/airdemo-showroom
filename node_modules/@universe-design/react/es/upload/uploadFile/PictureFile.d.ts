import * as React from 'react';
import type { FileProps, PreviewFileHandler } from './interface';
import '../style/picture-file.less';
export type PictureFileProps = FileProps & {
    /**
     * 自定义文件预览行为，需要返回文件 url
     */
    previewFile?: PreviewFileHandler;
};
export declare function PictureFile(props: PictureFileProps): React.JSX.Element;
