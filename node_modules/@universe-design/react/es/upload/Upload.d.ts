import * as React from 'react';
import type { UploadProps, UploadRef } from './interface';
export interface CompoundedComponent extends React.ForwardRefExoticComponent<React.PropsWithChildren<UploadProps> & React.RefAttributes<UploadRef>> {
    LIST_IGNORE: string;
}
/**
 *  推荐使用 `ComposableUpload` 来实现更灵活的组合式用法
 */
export declare const Upload: CompoundedComponent;
