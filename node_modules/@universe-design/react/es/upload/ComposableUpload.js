import clsx from 'clsx';
import * as React from 'react';
import { UploadContextProvider } from './hooks/UploadContext';
import { useUpload } from './hooks/useUpload';
export const ComposableUpload = /*#__PURE__*/React.forwardRef((props, ref) => {
  const {
    className,
    children,
    disabled,
    style,
    id
  } = props;
  const uploadCtxValue = useUpload(props, ref);
  const {
    rootPrefixCls
  } = uploadCtxValue;
  const classNames = clsx({
    [rootPrefixCls]: true,
    [`${rootPrefixCls}--disabled`]: disabled
  }, className);
  return /*#__PURE__*/React.createElement(UploadContextProvider, {
    value: uploadCtxValue
  }, /*#__PURE__*/React.createElement("div", {
    className: classNames,
    style: style,
    id: id
  }, children));
});
//# sourceMappingURL=ComposableUpload.js.map
