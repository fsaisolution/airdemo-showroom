import type { EventHandler, SyntheticEvent } from 'react';
/**
 * 组合所有传入的事件句柄，并返回一个新的句柄函数。用户句柄要放在靠前的参数里。
 */
export declare function composeEventHandlers<T extends EventHandler<SyntheticEvent>>(...fns: (T | undefined)[]): (event: Parameters<T>[0]) => void;
