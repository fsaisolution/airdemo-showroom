import { isNonNullable } from './assert';

/**
 * 组合所有传入的事件句柄，并返回一个新的句柄函数。用户句柄要放在靠前的参数里。
 */
export function composeEventHandlers() {
  for (var _len = arguments.length, fns = new Array(_len), _key = 0; _key < _len; _key++) {
    fns[_key] = arguments[_key];
  }
  return event => {
    for (const fn of fns) {
      var _event$isDefaultPreve;
      if (isNonNullable(fn)) {
        fn(event);
      }
      // 防御性编程，可能没有 event 或者 event 没有 isDefaultPrevented 方法
      if (event !== null && event !== void 0 && (_event$isDefaultPreve = event.isDefaultPrevented) !== null && _event$isDefaultPreve !== void 0 && _event$isDefaultPreve.call(event)) {
        return;
      }
    }
  };
}
//# sourceMappingURL=function.js.map
