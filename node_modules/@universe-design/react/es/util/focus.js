// refer to https://github.com/chakra-ui/chakra-ui/blob/b577eac5d127b2e4cbedc2a15c2b0fcda03d7a17/packages/utils/src/focus.ts

import { getThunkElement } from './getThunkElement';
import { isInputElement } from './dom';
import { isActiveElement } from './tabbable';
import { devWarning, warning } from './warning';
let supportsPreventScrollCached = null;
function supportsPreventScroll() {
  if (supportsPreventScrollCached === null) {
    supportsPreventScrollCached = false;
    try {
      const div = document.createElement('div');
      div.focus({
        get preventScroll() {
          supportsPreventScrollCached = true;
          return true;
        }
      });
    } catch (e) {
      // Ignore
    }
  }
  return supportsPreventScrollCached;
}
export function focus(thunkElement) {
  let options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
  const {
    nextTick,
    preventScroll = true,
    selectTextIfInput = false
  } = options;
  function triggerFocus() {
    const element = getThunkElement(thunkElement);
    if (!element) {
      process.env.NODE_ENV !== "production" ? warning(false, devWarning('focus', "can't call focus() on `null` or `undefined` element")) : void 0;
      return;
    }
    if (isActiveElement(element)) {
      return;
    }
    if (supportsPreventScroll()) {
      element.focus({
        preventScroll
      });
    } else {
      element.focus();
    }
    if (isInputElement(element) && selectTextIfInput) {
      element.select();
    }
  }
  if (nextTick) {
    return requestAnimationFrame(triggerFocus);
  }
  triggerFocus();
}
//# sourceMappingURL=focus.js.map
