export const SizeEnum = ['xl', 'lg', 'md', 'sm', 'xs'];
/**
 * @public
 */
export const DefaultSizeEnum = ['large', 'medium', 'small'];
export const DefaultExtraSizeEnum = ['extra-large', 'extra-small'
// 'default',
];
export const sizeMap = {
  'extra-large': 'xl',
  large: 'lg',
  medium: 'md',
  // default: 'md',
  small: 'sm',
  'extra-small': 'xs'
};
export function isValidDefaultSize(sizeName) {
  return DefaultSizeEnum.includes(sizeName);
}
export function isValidSize(sizeName) {
  return DefaultSizeEnum.includes(sizeName) || DefaultExtraSizeEnum.includes(sizeName);
}
export function getDefaultSizeAbbr(sizeName, defaultName) {
  if (isValidDefaultSize(sizeName)) {
    return sizeMap[sizeName];
  }
  if (defaultName && isValidDefaultSize(sizeName)) {
    return sizeMap[defaultName];
  }
  return '';
}
export { getSizeAbbr } from '@universe-design/react-utilities';
//# sourceMappingURL=size.js.map
