import _extends from "@babel/runtime/helpers/esm/extends";
import React from 'react';
import clsx from 'clsx';
import { useCssScope } from '../config-provider/hooks';
import Line from './Line';
import Circle from './Circle';
import './style';
export const Progress = props => {
  const {
    type = 'line',
    percent = 0,
    className,
    style
  } = props;
  let optimizePercent = 0;
  if (percent > 100) {
    optimizePercent = 100;
  } else if (percent < 0) {
    optimizePercent = 0;
  } else {
    optimizePercent = percent;
  }
  const cssScope = useCssScope('progress');
  const renderProgress = () => {
    let progressNode;
    switch (type) {
      case 'line':
        progressNode = /*#__PURE__*/React.createElement(Line, _extends({}, props, {
          percent: optimizePercent
        }));
        break;
      case 'circle':
        {
          progressNode = /*#__PURE__*/React.createElement(Circle, _extends({}, props, {
            percent: optimizePercent
          }));
        }
      default:
        break;
    }
    return progressNode;
  };
  return /*#__PURE__*/React.createElement("div", {
    style: style,
    className: clsx(cssScope(), cssScope(`--${type}`), className)
  }, /*#__PURE__*/React.createElement("div", {
    className: cssScope(`--${type}-wrapper`)
  }, renderProgress()));
};
//# sourceMappingURL=Progress.js.map
