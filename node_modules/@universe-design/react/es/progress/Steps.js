import React from 'react';
import SucceedFilled from '@universe-design/icons-react/SucceedFilled';
import ErrorFilled from '@universe-design/icons-react/ErrorFilled';
import clsx from 'clsx';
import { useCssScope } from '../config-provider/hooks';
import { ProgressStatusEnum } from './shared';
import { isFunction } from '../util';
const Steps = props => {
  const {
    percent,
    format,
    status
  } = props;
  const cssScope = useCssScope('progress');
  const [success, exception, normal] = ProgressStatusEnum;
  return /*#__PURE__*/React.createElement(React.Fragment, null, (() => {
    let progressInfoNode;
    switch (status) {
      case success:
        progressInfoNode = /*#__PURE__*/React.createElement("span", {
          className: clsx([cssScope('__icon'), cssScope('__icon--success')])
        }, /*#__PURE__*/React.createElement(SucceedFilled, null));
        break;
      case exception:
        progressInfoNode = /*#__PURE__*/React.createElement("span", {
          className: clsx([cssScope('__icon'), cssScope('__icon--error')])
        }, /*#__PURE__*/React.createElement(ErrorFilled, null));
        break;
      case normal:
        if (isFunction(format)) {
          progressInfoNode = format(percent);
        } else {
          progressInfoNode = `${percent}%`;
        }
      default:
        break;
    }
    return progressInfoNode;
  })());
};
export default Steps;
//# sourceMappingURL=Steps.js.map
