import React, { forwardRef } from 'react';
import { AnchorContextProvider } from './AnchorContext';
import { Affix } from '../affix';
import { useAnchor } from './useAnchor';
import './style';
export const Anchor = /*#__PURE__*/forwardRef((props, ref) => {
  const {
    children
  } = props;
  const {
    simple,
    affix,
    showInk,
    getAnchorBodyProps,
    getAnchorInkProps,
    getAffixProps,
    activeLink,
    registerLink,
    unregisterLink,
    handleLinkClick,
    scrollToSection,
    forceUpdateActiveLink
  } = useAnchor(props);
  React.useImperativeHandle(ref, () => ({
    scrollToSection,
    forceUpdateActiveLink
  }), [scrollToSection, forceUpdateActiveLink]);
  const renderBody = () => /*#__PURE__*/React.createElement("div", getAnchorBodyProps(), !simple && showInk && /*#__PURE__*/React.createElement("div", getAnchorInkProps()), children);
  return /*#__PURE__*/React.createElement(AnchorContextProvider, {
    value: {
      registerLink,
      unregisterLink,
      activeLink,
      handleLinkClick
    }
  }, (() => {
    if (affix) {
      return /*#__PURE__*/React.createElement(Affix, getAffixProps(), renderBody());
    }
    return renderBody();
  })());
});
if (process.env.NODE_ENV !== "production") {
  Anchor.displayName = 'Anchor';
}
//# sourceMappingURL=Anchor.js.map
