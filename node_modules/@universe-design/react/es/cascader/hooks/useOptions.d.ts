import React from 'react';
import type { CascaderProps } from '../interface';
import type { Required } from 'utility-types';
import type { DoublyTreeNode } from '../../tree';
export declare function useOptions(props: Required<Pick<CascaderProps, 'filterOption' | 'filterResultRenderer' | 'filterResultSorter'>, 'filterOption'> & {
    inputValue: string;
    doublyTreeNodeList: DoublyTreeNode[];
}): {
    searching: boolean;
    labelRenderer: (node: DoublyTreeNode) => React.ReactNode;
    treeNodeList: DoublyTreeNode[];
};
