import type { ReactElement } from 'react';
import React from 'react';
import type { DefaultSizeType, DefaultExtraSizeType, LiteralUnion, ElementOf, StylexProps } from '../util';
import './style';
/**
 * 废弃的 color 命名，以及新命名
 */
declare const CompatibleColorMap: {
    readonly 'neutral-select': "neutral-option";
    readonly 'neutral-light': "neutral-solid";
};
export declare const TagPresetColorEnum: readonly ["neutral", "neutral-solid", "neutral-option", "neutral-ghost", "neutral-ghost-transparent", "blue", "wathet", "turquoise", "green", "lime", "yellow", "orange", "red", "violet", "purple", "indigo", "carmine", "blue-solid", "wathet-solid", "turquoise-solid", "green-solid", "lime-solid", "yellow-solid", "orange-solid", "red-solid", "violet-solid", "purple-solid", "indigo-solid", "carmine-solid", "blue-option", "wathet-option", "turquoise-option", "green-option", "lime-option", "yellow-option", "orange-option", "red-option", "violet-option", "purple-option", "indigo-option", "carmine-option", "blue-ghost", "carmine-ghost", "red-ghost", "waiting", "success", "info", "warning", "error", "missing", "neutral-avatar", "blue-avatar", "orange-avatar"];
/**
 * @public
 */
export type TagPropColor = LiteralUnion<ElementOf<typeof TagPresetColorEnum> | keyof typeof CompatibleColorMap, string>;
export type TagPropSize = DefaultSizeType | Extract<DefaultExtraSizeType, 'extra-small'>;
/**
 * @public
 */
export interface TagProps extends React.HTMLAttributes<HTMLSpanElement>, StylexProps {
    /**
     * 标签颜色，包含预设颜色以及预设状态色，如果提供的颜色不在预设范围内，则会使用纯底色和 `@tag-other-color` 字色
     * @defaultValue `neutral`
     */
    color?: TagPropColor;
    /**
     * 标签的尺寸，拥有 `extra-small`、`small`、`medium` 和 `large` 四种尺寸
     * @defaultValue `medium`
     */
    size?: TagPropSize;
    /**
     * 标签形状，拥有 `round` 圆角矩形、`rect` 矩形和 `square` 方形，三种类型的形状
     * @defaultValue `rect`
     */
    shape?: 'round' | 'rect' | 'square';
    /**
     * 左侧状态图标
     */
    icon?: ReactElement;
    /**
     * 是否可关闭，`true` 则显示右侧可关闭图标，`false` 则不显示右侧可关闭图标。方形标签不能设置为可关闭。
     */
    closable?: boolean;
    /**
     * 是否显示标签的受控值
     */
    visible?: boolean;
    /**
     * 标签关闭的回调
     */
    onClose?: (e: React.MouseEvent<HTMLSpanElement>) => void;
    /**
     * 是否禁用
     * @defaultValue false
     */
    disabled?: boolean;
}
export declare const Tag: React.ForwardRefExoticComponent<TagProps & React.RefAttributes<HTMLSpanElement>>;
export {};
