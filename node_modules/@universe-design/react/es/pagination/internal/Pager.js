import React from 'react';
import clsx from 'clsx';
const Pager = props => {
  const {
    className
  } = props;
  const prefixCls = `${props.rootPrefixCls}-item`;
  const cls = clsx(prefixCls, `${prefixCls}-${props.page}`, {
    [`${prefixCls}-active`]: props.active,
    [`${prefixCls}-disabled`]: !props.page
  }, className);
  return /*#__PURE__*/React.createElement("li", {
    title: props.showTitle ? String(props.page) : undefined,
    className: cls,
    onClick: () => {
      props.onClick(props.page);
    },
    onKeyPress: e => {
      var _props$onKeyPress;
      (_props$onKeyPress = props.onKeyPress) === null || _props$onKeyPress === void 0 ? void 0 : _props$onKeyPress.call(props, e, props.onClick, props.page);
    },
    tabIndex: 0
  }, props.itemRender(props.page, 'page', /*#__PURE__*/React.createElement("a", {
    rel: "nofollow"
  }, props.page)));
};
export default Pager;
//# sourceMappingURL=Pager.js.map
