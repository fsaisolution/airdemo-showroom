import { useContextSelector } from 'use-context-selector';
import { RcTriggerPopperSidecar } from '../../shared/components/popper-sidecar/RcTriggerPopperSidecar';
import { ConfigContext } from '../ConfigContext';
export function usePopperSidecar() {
  var _useContextSelector;
  return (_useContextSelector = useContextSelector(ConfigContext, o => o.PopperSidecar)) !== null && _useContextSelector !== void 0 ? _useContextSelector : RcTriggerPopperSidecar;
}
//# sourceMappingURL=usePopperSidecar.js.map
