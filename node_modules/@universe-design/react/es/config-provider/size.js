import React from 'react';
/**
 * @file size.ts
 * 单独提供 size context 的原因是 form 组件会重新设置 ctx.size 的值
 */
import { createContext, useContext } from 'use-context-selector';
// 在使用 InputGroup 的场景中设置 InputGroup 内的元素大小
export const InputGroupSizeContext = createContext(undefined);
export const InputGroupSizeContextProvider = _ref => {
  let {
    size,
    children
  } = _ref;
  const origSize = useContext(InputGroupSizeContext);
  return /*#__PURE__*/React.createElement(InputGroupSizeContext.Provider, {
    value: size !== null && size !== void 0 ? size : origSize
  }, children);
};
//# sourceMappingURL=size.js.map
