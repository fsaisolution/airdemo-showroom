/**
 * 减弱动态效果
 * https://developer.mozilla.org/en-US/docs/Web/CSS/@media/prefers-reduced-motion
 */
import * as React from 'react';
import { useMedia } from '../hooks';
import { createGlobalConfig } from '@universe-design/react-provider';
const {
  ConfigProvider,
  useConfig
} = /* #__PURE__*/createGlobalConfig('reduced-motion', false);
const PrefersReducedMotion = '(prefers-reduced-motion)';
export const Unstable_ConfigReducedMotion = _ref => {
  let {
    value,
    children
  } = _ref;
  const systemReducedMotion = useMedia(PrefersReducedMotion, {
    skipped: value !== 'follow-system'
  });
  return /*#__PURE__*/React.createElement(ConfigProvider, {
    value: value === 'follow-system' ? systemReducedMotion : value
  }, children);
};
export const unstable_useConfigReducedMotion = useConfig;
//# sourceMappingURL=ConfigReducedMotion.js.map
