import React from 'react';
/**
 * Tab 标签下划线
 */
export declare const TabsInkBar: ({ offsetY, }: {
    /**
     * 标签下划线的偏移量，是 divider 的高度，因为我们的 divider 是 div 元素实现的而非 border 等
     */
    offsetY: number;
}) => React.JSX.Element | null;
