import * as React from 'react';
import { useOverflowContext } from '../overflow/hooks';
import { createContextHook } from '@universe-design/react-utilities';
export const useTab = props => {
  const {
    key
  } = props;
  const {
    omittedKeys
  } = useOverflowContext();
  return React.useMemo(() => ({
    key,
    isOmitted: omittedKeys.includes(key)
  }), [key, omittedKeys]);
};
export const {
  useContext: useTabContext,
  Provider: TabContextProvider
} = createContextHook('TabContext', {
  strict: true
});
export const TabProvider = _ref => {
  let {
    tabKey,
    children
  } = _ref;
  return /*#__PURE__*/React.createElement(TabContextProvider, {
    value: useTab({
      key: tabKey
    })
  }, children);
};
//# sourceMappingURL=TabContext.js.map
