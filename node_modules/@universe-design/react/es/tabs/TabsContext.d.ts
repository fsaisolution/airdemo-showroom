import type { CSSProperties, KeyboardEvent, ReactNode, MouseEvent, RefObject, ReactElement } from 'react';
import * as React from 'react';
import type { Required } from 'utility-types';
import type { DefaultSizeType } from '../util';
import type { SizingBehavior } from './sizing_behavior';
/**
 * Tab 尺寸行为类型
 * 定义单个 Tab 的尺寸控制方式
 * 支持简单字符串类型和详细的 SizingBehavior 对象类型
 */
export type TabSizingBehavior = 'flex' | 'hug' | SizingBehavior;
export declare const UD_TABPANE_ID = "UD_TABPANE_ID";
export declare function isTabPane(node: any): node is React.ReactElement<TabPaneProps>;
export type TabPosition = 'left' | 'right' | 'top' | 'bottom';
/**
 * @public
 */
export type TabBarExtraContent = ReactNode | {
    left: ReactNode;
    right: ReactNode;
};
export type TabsType = 'line' | 'card' | 'tag' | 'capsule';
export type onEditFunction = (action: 'add' | 'delete', targetKey?: string) => void;
/**
 * `<Tabs />` 组件的属性
 * @public
 */
export interface TabsProps {
    /**
     * 节点样式
     */
    style?: CSSProperties;
    /**
     * 节点类名
     */
    className?: string;
    /**
     * 页签激活 `key` 的受控值
     */
    activeKey?: string;
    /**
     * 页签激活 `key` 的默认值
     */
    defaultActiveKey?: string;
    /**
     * 页签条左右的额外内容元素
     */
    tabBarExtraContent?: TabBarExtraContent;
    /**
     * 页签之间的间隙
     * @defaultValue `tag`: 16, `others`: 28
     */
    tabBarGutter?: number;
    /**
     * ScrollButton 和 TabBar 的间距，默认 line 类型的为 28，其他为 0
     */
    scrollButtonGutter?: number;
    /**
     * 页签条的 `style` 属性透传
     */
    tabBarStyle?: CSSProperties;
    children: ReactNode;
    /**
     * 页签类型
     * @defaultValue `line`
     */
    type?: TabsType;
    /**
     * 页签大小
     * @defaultValue `medium`
     */
    size?: DefaultSizeType;
    /**
     * 最大页签数量
     * @defaultValue `responsive`
     */
    maxCount?: number | 'responsive';
    /**
     * 切换面板后触发的回调
     */
    onChange?: (activeKey: string) => void;
    /**
     * 页签被点击的回调
     */
    onTabClick?: (activeKey: string, e: KeyboardEvent | MouseEvent) => void;
    /**
     * 「更多」页签节点
     * @defaultValue `<TabMore />`
     */
    tabMore?: ReactElement;
    /**
     * 双击事件
     */
    onTabDoubleClick?: (activeKey: string, e: KeyboardEvent | MouseEvent) => void;
    /**
     * 当页签过多时，是否横向滚动。默认收起到【更多】
     * @defaultValue false
     */
    scrollable?: boolean;
    /**
     * 页面滚动时，TabBar 是否固定在顶部
     * @defaultValue false
     */
    sticky?: boolean;
    /**
     * 统一设置 tabs 中每个 tab 的 sizingBehavior
     * 支持简单字符串类型：
     * - `flex`: 弹性布局，平均分配空间
     * - `hug`: 内容包裹，根据内容自适应宽度
     * 也支持 SizingBehavior 对象类型，可以精确控制 minWidth、maxWidth 等属性
     * @defaultValue `hug`
     */
    tabsSizingBehavior?: TabSizingBehavior;
}
/**
 * `<Tabs.Pane>` 组件的属性
 * @public
 */
export interface TabPaneProps {
    /**
     * 窗格样式
     */
    style?: CSSProperties;
    /**
     * 窗格类名
     */
    className?: string;
    /**
     * 选项卡头显示文字
     */
    tab?: ReactNode;
    /**
     * 对应`activeKey`
     */
    key?: string;
    /**
     * 禁用窗格
     * @defaultValue false
     */
    disabled?: boolean;
    /**
     * 是否将隐藏的 Pane 也提前渲染出来
     * @defaultValue false
     */
    forceRender?: boolean;
    /**
     * 激活态的 Pane 被切换之后，是否将其销毁， 默认不销毁。
     * @defaultValue false
     */
    destroyOnHide?: boolean;
    internalKey?: string;
    children?: ReactNode;
}
export interface TabsContextType extends Required<Omit<TabsProps, 'defaultActiveKey' | 'tabsSizingBehavior'>, 'type' | 'size' | 'maxCount' | 'activeKey' | 'tabBarGutter' | 'scrollable'> {
    rootCssScope: (suffix?: string) => string;
    setActiveKey: (k: string) => void;
    handleTabClick: (key: string, e: MouseEvent<HTMLElement> | KeyboardEvent<HTMLElement>) => void;
    handleTabDoubleClick: (key: string, e: MouseEvent<HTMLElement> | KeyboardEvent<HTMLElement>) => void;
    panes: Required<TabPaneProps, 'key'>[];
    activeTabIndex: number;
    tabElementMapRef: RefObject<Map<string, HTMLDivElement | null>>;
    tabBarWrapperRef: RefObject<HTMLDivElement>;
    moreTabRef: RefObject<HTMLDivElement>;
    rootTabsSizingBehavior: SizingBehavior;
}
export declare const TabsContext: import("use-context-selector").Context<TabsContextType>;
export declare const useTabs: (props: TabsProps) => TabsContextType;
export declare const useTabsContext: () => TabsContextType;
