export declare const MAX_SAFE_INTEGER: number;
/**
 * 当直接使用 up/down 时
 *  1. 如果设置了 min, 则将值切换到 min
 *  2. 如果没有设置 min, 则将值变为 1
 */
export declare const DEFAULT_VALUE = 0;
export declare const internalIsNumber: (val: unknown) => val is number;
export declare const getValidValueNumberFromAny: (val: unknown) => number;
export declare const getPrecision: (value: number) => number;
export declare enum ChangeType {
    submit = 0,
    addition = 1,
    subtraction = 2
}
