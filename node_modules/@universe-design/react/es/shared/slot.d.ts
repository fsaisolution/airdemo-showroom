import React from 'react';
type SlotRendererFunc<Props extends UnknownSlotProps> = (component: React.ElementType<Props>, props: Props) => React.ReactElement<Props>;
type WithSlotRenderer<Props extends UnknownSlotProps> = 'slotRenderer' extends keyof Props ? never : Props & {
    slotRenderer?: SlotRendererFunc<Props>;
};
type SlotShorthandValue = React.ReactChild | React.ReactNode[] | React.ReactPortal;
type UnknownSlotProps = {};
type WithSlotShorthandValue<Props extends UnknownSlotProps> = Props | ('children' extends keyof Props ? Extract<SlotShorthandValue, Props['children']> : never);
export declare function resolveShorthandOld<Props>(value: Props | SlotShorthandValue): Props;
declare function resolveShorthand<Props extends UnknownSlotProps>(value: WithSlotShorthandValue<Props> | undefined): Props;
type Slot<Props extends UnknownSlotProps> = WithSlotShorthandValue<WithSlotRenderer<Props>> | null | undefined;
declare const SLOT_ELEMENT_TYPE_SYMBOL: unique symbol;
declare const SLOT_RENDERER_SYMBOL: unique symbol;
/**
 * 定义 `SlotNode` 接口，用于表示解析后的插槽节点。
 * 该接口包含插槽的属性、组件类型以及可选的插槽渲染函数。
 *
 * @typeParam Props - 插槽组件的属性类型，必须扩展自 `UnknownSlotProps`。
 */
interface SlotNode<Props extends UnknownSlotProps> {
    props: Props;
    [SLOT_ELEMENT_TYPE_SYMBOL]: React.ElementType<Props>;
    [SLOT_RENDERER_SYMBOL]?: SlotRendererFunc<Props>;
}
/**
 * 定义解析插槽时使用的选项类型。
 * 该类型包含解析插槽所需的元素类型和可选的默认属性。
 *
 * @typeParam Props - 插槽组件的属性类型，必须扩展自 `UnknownSlotProps`。
 */
type SlotOptions<Props extends UnknownSlotProps> = {
    elementType: React.ElementType<Props>;
    defaultProps?: Partial<Props>;
    renderByDefault?: boolean;
};
/**
 * 解析给定的插槽属性值，将其转换为 `SlotNode` 对象。
 * 如果传入的插槽属性值为 `null`，则返回 `null`。
 *
 * @typeParam Props - 插槽组件的属性类型，必须扩展自 `UnknownSlotProps`。
 * @param propSlot - 要解析的插槽属性值，类型为 `Slot<Props>`。
 * @param options - 解析插槽时使用的选项，包含元素类型和默认属性。
 * @returns 解析后的 `SlotNode<Props>` 对象，如果 `propSlot` 为 `null` 则返回 `null`。
 */
declare const resolveSlot: <Props extends UnknownSlotProps>(propSlot: Slot<Props>, options: SlotOptions<Props>) => SlotNode<Props> | null;
/**
 * 根据传入的 `SlotNode` 对象渲染对应的 React 节点。
 * 如果 `SlotNode` 为 `null`，则返回 `null`。
 * 支持使用传入的 `children` 覆盖 `SlotNode` 中的 `children` 属性。
 * 如果 `SlotNode` 中存在 `slotRenderer`，则使用该渲染函数进行渲染；否则，使用 `React.createElement` 进行渲染。
 *
 * @typeParam Props - 插槽组件的属性类型，必须扩展自 `UnknownSlotProps`。
 * @param slotNode - 要渲染的 `SlotNode` 对象，可能为 `null`。
 * @param children - 可选的子节点，用于覆盖 `SlotNode` 中的 `children` 属性。仅当 `Props` 中包含 `children` 属性时可用。
 * @returns 渲染后的 React 节点，如果 `slotNode` 为 `null` 则返回 `null`。
 */
declare const renderSlotNode: <Props extends UnknownSlotProps>(slotNode: SlotNode<Props> | null, children?: "children" extends keyof Props ? Props["children"] : never) => React.ReactElement<Props> | null;
export type { SlotRendererFunc, UnknownSlotProps, Slot, WithSlotShorthandValue, WithSlotRenderer, SlotShorthandValue, SlotNode, };
export { resolveShorthand, resolveSlot, renderSlotNode, SLOT_RENDERER_SYMBOL, SLOT_ELEMENT_TYPE_SYMBOL };
