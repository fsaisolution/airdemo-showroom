import React from 'react';
import type { PopperProviderProps, PopperRef } from './PopperProvider';
import type { PopperPopupProps } from './PopperPopup';
import type { PopperReferenceProps } from './PopperReference';
import type { PopperPopupVisibleControllerProps } from './PopperPopupVisibleController';
/**
 * @public
 * @docgen
 */
export interface PopperProps extends PopperProviderProps, PopperPopupVisibleControllerProps, PopperPopupProps, PopperReferenceProps {
    popup: React.ReactNode;
}
export declare const Popper: React.ForwardRefExoticComponent<PopperProps & React.RefAttributes<PopperRef>>;
