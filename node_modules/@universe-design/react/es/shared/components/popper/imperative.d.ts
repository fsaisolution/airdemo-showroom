import type { VirtualElement } from '@floating-ui/core';
import type { PopperProps } from '.';
export interface ImperativePopperControllerProps extends Omit<PopperProps, 'children' | 'trigger' | 'showAction' | 'hideAction' | 'alignPoint'> {
    reference: Element | VirtualElement;
}
export declare class ImperativePopperController {
    private _popperManager;
    private _container;
    private _imperativePopperRef;
    private _isAlive;
    constructor(props: ImperativePopperControllerProps, popper: PopperManager);
    update: (props: ImperativePopperControllerProps) => void;
    destroy: () => void;
    private props2PopperProps;
    private _initOrUpdate;
    private _cleaup;
}
declare class PopperManager {
    controllers: Set<ImperativePopperController>;
    create: (props: ImperativePopperControllerProps) => ImperativePopperController;
}
export declare const popper: PopperManager;
export {};
