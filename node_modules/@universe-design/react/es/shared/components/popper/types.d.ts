import type { ElementOf } from '../../../util';
/**
 * Placement
 */
export declare const PlacementEnum: readonly ["top-start", "top", "top-end", "right-start", "right", "right-end", "bottom-start", "bottom", "bottom-end", "left-start", "left", "left-end"];
export type Placement = ElementOf<typeof PlacementEnum>;
export declare const ZERO_TIMEOUT: {
    appear: number;
    enter: number;
    exit: number;
};
export declare const defaults: {
    timeout: {
        appear: number;
        enter: number;
        exit: number;
    };
};
