import _extends from "@babel/runtime/helpers/esm/extends";
import React from 'react';
import Picker from '..';
export default function generateSinglePicker(generateConfig) {
  const TimePicker = function (picker, displayName) {
    const BasePicker = props => /*#__PURE__*/React.createElement(Picker, _extends({}, props, {
      picker: picker,
      generateConfig: generateConfig
    }));
    if (displayName) {
      BasePicker.displayName = displayName;
    }
    return BasePicker;
  }('time', 'TimePicker');
  return {
    TimePicker
  };
}
//# sourceMappingURL=generateSinglePicker.js.map
