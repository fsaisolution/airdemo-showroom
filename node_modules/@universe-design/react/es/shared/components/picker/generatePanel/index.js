import _extends from "@babel/runtime/helpers/esm/extends";
import React from 'react';
import PickerPanel from '../Comp/PickerPanel';
function generateInnerPanel(generateConfig) {
  const getPanel = mode => {
    return props => /*#__PURE__*/React.createElement(PickerPanel, _extends({}, props, {
      generateConfig: generateConfig,
      mode: mode
    }));
  };
  const TimePanel = getPanel('time');
  const TimeCombinePanel = getPanel('timeCombine');
  return {
    TimePanel,
    TimeCombinePanel
  };
}
export default function generatePicker(generateConfig) {
  const {
    TimePanel,
    TimeCombinePanel
  } = generateInnerPanel(generateConfig);
  return {
    TimePanel,
    TimeCombinePanel
  };
}
//# sourceMappingURL=index.js.map
