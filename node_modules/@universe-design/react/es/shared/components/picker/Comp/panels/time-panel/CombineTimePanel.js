/* eslint-disable @typescript-eslint/no-magic-numbers */
import React from 'react';
import { useLocale } from '../../../../../../config-provider/hooks';
import Column from './Column';
import { leftPad } from '../../../utils';
import { parseValue } from '../../../utils/ui';
import { isSameTime } from '../../../utils/date';
import { ListBox } from '../../../../list/List';
function CombineTimePanel(props) {
  const {
    prefixCls,
    activeColumnIndex,
    value = -1,
    use12Hours,
    timeStep = 15,
    // 时间间隔，单位是分钟，默认15min，建议15min, 30min, 45min, 60min
    disabledTime,
    generateConfig,
    parse,
    mode,
    locale,
    hideDisabledOptions,
    onPanelChange,
    customTime = [],
    popupStyle
  } = props;
  const {
    locale: localeName
  } = useLocale();

  // timeCombine格式下将其统一化为HH:mm格式比较
  const option = {
    format: 'HH:mm',
    parse,
    mode,
    locale,
    localeName
  };
  const parseWidth = popupStyle && parseFloat(popupStyle === null || popupStyle === void 0 ? void 0 : popupStyle.width);
  const columns = [];
  const time = value ? value : -1;
  const disabledTimes = (disabledTime === null || disabledTime === void 0 ? void 0 : disabledTime.map(i => parseValue(generateConfig, i, option))) || [];

  // 在这里可以通过修改前两个参数从而改变时间的开始与结束 customTime自定义的时间列表
  const rawTime = customTime.length > 0 ? customTime : function (start, end, step,
  // 分钟
  disabledUnits) {
    const units = [];
    if (step < 60) {
      for (let i = start; i <= end; i += 1) {
        for (let j = 0; j <= 59; j += step) {
          const label = `${i > 9 ? i : `0${i}`}:${leftPad(j, 2)}`;
          const parseTime = parseValue(generateConfig, label, option);
          const isDisabled = !(disabledUnits !== null && disabledUnits !== void 0 && disabledUnits.some(item => isSameTime(generateConfig, item, parseTime))) || false;
          units.push({
            label,
            value: parseTime || -1,
            disabled: !isDisabled
          });
        }
      }
    } else if (step === 60) {
      for (let i = start; i <= end; i += 1) {
        const label = i > 9 ? `${i}:00` : `0${i}:00`;
        const parseTime = parseValue(generateConfig, label, option);
        const isDisabled = !(disabledUnits !== null && disabledUnits !== void 0 && disabledUnits.some(item => isSameTime(generateConfig, item, parseTime))) || false;
        units.push({
          label,
          value: parseTime || -1,
          disabled: !isDisabled
        });
      }
    }
    return units;
  }(0, 23, (number => {
    if (60 % number === 0) {
      return true;
    } else {
      return false;
    }
  })(timeStep) ? timeStep : 15, disabledTimes);
  const showTime = hideDisabledOptions ? rawTime.filter(i => !i.disabled) : rawTime;
  const memorizedRawTime = showTime;
  const times = React.useMemo(() => {
    if (!use12Hours) {
      return memorizedRawTime;
    }
    return memorizedRawTime.map(v => ({
      ...v,
      label: generateConfig.formatLabel(v.value, 'hh:mm A')
    }));
  }, [use12Hours, memorizedRawTime, generateConfig]);

  // ======================== Render ========================

  (function (node, columnValue, units, onColumnSelect) {
    columns.push({
      node: /*#__PURE__*/React.cloneElement(node, {
        prefixCls: `${prefixCls}-time-panel`,
        value: columnValue,
        active: activeColumnIndex === columns.length,
        onPanelChange: onColumnSelect,
        units,
        hideDisabledOptions
      }),
      onPanelChange: onColumnSelect,
      value: columnValue,
      units
    });
  })(/*#__PURE__*/React.createElement(Column, {
    key: "timeCombine",
    mode: "timeCombine",
    value: value,
    generateConfig: generateConfig
  }), time, times, num => onPanelChange(num));
  return /*#__PURE__*/React.createElement("div", {
    className: `${prefixCls}-time-combine-panel`
  }, /*#__PURE__*/React.createElement(ListBox, {
    style: {
      width: parseWidth ? parseWidth : 144
    }
  }, columns.map(_ref => {
    let {
      node
    } = _ref;
    return node;
  })));
}
export default CombineTimePanel;
//# sourceMappingURL=CombineTimePanel.js.map
