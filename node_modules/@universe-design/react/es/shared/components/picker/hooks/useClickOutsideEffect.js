import { useEffect, useCallback } from 'react';
import { elementsContains } from '../utils/ui';
function useClickOutsideEffect(elementRefs, condition, callback) {
  const closeOnClickOutside = useCallback(_ref => {
    let {
      target
    } = _ref;
    if (condition) {
      if (!elementsContains(elementRefs.map(e => e.current), target)) {
        callback();
      }
    }
  }, [elementRefs, condition, callback]);
  useEffect(() => {
    window.addEventListener('mouseup', closeOnClickOutside);
    return () => {
      window.removeEventListener('mouseup', closeOnClickOutside);
    };
  }, [closeOnClickOutside]);
}
export default useClickOutsideEffect;
//# sourceMappingURL=useClickOutsideEffect.js.map
