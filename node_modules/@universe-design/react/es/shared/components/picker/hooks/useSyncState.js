import { useState, useRef } from 'react';
import { useIsomorphicLayoutEffect as useLayoutEffect } from '../../../../hooks';
function useSyncState(state, initialState) {
  const isUpdate = useRef(false);
  const [currentState, setCurrentState] = useState(initialState !== undefined ? initialState : state);
  useLayoutEffect(() => {
    if (!isUpdate.current) {
      isUpdate.current = true;
      return;
    }
    setCurrentState(state);
  }, [state]);
  return [currentState, setCurrentState];
}
export default useSyncState;
//# sourceMappingURL=useSyncState.js.map
