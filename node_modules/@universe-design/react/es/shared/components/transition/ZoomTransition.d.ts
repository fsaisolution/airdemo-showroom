import type React from 'react';
import type { TransitionProps } from 'react-transition-group/Transition';
import type { Placement } from '../popper/types';
/**
 * 用于缩放场景的动画，比如 tooltip popover popconfirm
 */
export declare const ZoomTransition: {
    (props: Omit<TransitionProps, "nodeRef"> & {
        transitions?: Record<"appearActive" | "enterActive" | "exitActive", (t: number) => string>;
        nodeRef?: React.Ref<HTMLElement> | undefined;
        children: TransitionProps["children"];
    } & {
        extraProps: {
            placement: Placement;
        };
    }): JSX.Element;
    displayName?: string;
};
