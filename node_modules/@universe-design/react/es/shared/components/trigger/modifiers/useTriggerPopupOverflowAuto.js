import { cloneElement, isValidElement, useCallback, useMemo, useState } from 'react';
import { alignElement } from 'dom-align';
import { useIsomorphicLayoutEffect as useLayoutEffect } from '../../../../hooks';
import { isFunction, isNonNullable } from '../../../../util';
import getWindow from '../@popperjs/core/lib/dom-utils/getWindow';

/**
 * dom-align 用于获取可视区域的函数
 */
const getVisibleRectForElement = alignElement.__getVisibleRectForElement;
const passive = {
  passive: true
};

/**
 * 已监听：
 * window scroll, resize
 * scrollParents scroll
 * popup resize
 * trigger resize
 * 未监听：
 * scrollParents resize
 */
export const useTriggerPopupOverflowAuto = (props, _ref) => {
  var _popupAlign$overflow2, _popupAlign$overflow7, _popupAlign$overflow8;
  let {
    popup,
    popupSize,
    popupAlign,
    childSize,
    scrollParents,
    scroll,
    resize,
    disabled
  } = _ref;
  const [rect, setRect] = useState(null);
  const updateRect = useCallback(() => {
    var _popupAlign$overflow;
    // FIXME 现在这里逻辑还有点问题，如果 visible rect 大小变了其实我们是无法得到监听的
    const alwaysByViewport = Boolean((_popupAlign$overflow = popupAlign.overflow) === null || _popupAlign$overflow === void 0 ? void 0 : _popupAlign$overflow.alwaysByViewport);
    setRect(popup && getVisibleRectForElement(popup, alwaysByViewport));
  }, [popup, (_popupAlign$overflow2 = popupAlign.overflow) === null || _popupAlign$overflow2 === void 0 ? void 0 : _popupAlign$overflow2.alwaysByViewport]);
  useLayoutEffect(() => {
    updateRect();
  }, [updateRect, childSize.bottom, childSize.left, childSize.right, childSize.top, popupSize.bottom, popupSize.left, popupSize.right, popupSize.top]);
  const popupContentStyle = useMemo(() => {
    var _popupAlign$overflow3, _popupAlign$overflow4, _popupAlign$overflow5, _popupAlign$overflow6;
    const style = {};
    if (!popup || disabled) {
      return undefined;
    }
    // 如果两个方向都要翻转就不做 maxWidth 和 maxHeight 的计算了
    if ((_popupAlign$overflow3 = popupAlign.overflow) !== null && _popupAlign$overflow3 !== void 0 && _popupAlign$overflow3.adjustX && (_popupAlign$overflow4 = popupAlign.overflow) !== null && _popupAlign$overflow4 !== void 0 && _popupAlign$overflow4.adjustY) {
      return undefined;
    }
    if (!rect) {
      return undefined;
    }
    style.overflow = 'auto';
    if (!((_popupAlign$overflow5 = popupAlign.overflow) !== null && _popupAlign$overflow5 !== void 0 && _popupAlign$overflow5.adjustX)) {
      if (childSize.right < popupSize.left) {
        style.maxWidth = rect.right - popupSize.left;
      } else if (childSize.left > popupSize.right) {
        style.maxWidth = popupSize.right - rect.left;
      }
    }
    if (!((_popupAlign$overflow6 = popupAlign.overflow) !== null && _popupAlign$overflow6 !== void 0 && _popupAlign$overflow6.adjustY)) {
      if (childSize.bottom < popupSize.top) {
        style.maxHeight = rect.bottom - popupSize.top;
      } else if (childSize.top > popupSize.bottom) {
        style.maxHeight = popupSize.bottom - rect.top;
      }
    }
    return style;
  }, [childSize.bottom, childSize.left, childSize.right, childSize.top, disabled, popup, (_popupAlign$overflow7 = popupAlign.overflow) === null || _popupAlign$overflow7 === void 0 ? void 0 : _popupAlign$overflow7.adjustX, (_popupAlign$overflow8 = popupAlign.overflow) === null || _popupAlign$overflow8 === void 0 ? void 0 : _popupAlign$overflow8.adjustY, popupSize.bottom, popupSize.left, popupSize.right, popupSize.top, rect]);
  const window = getWindow(popup);
  useLayoutEffect(() => {
    if (disabled) {
      return;
    }
    if (scroll) {
      scrollParents.forEach(scrollParent => {
        scrollParent.addEventListener('scroll', updateRect, passive);
      });
    }
    if (resize) {
      window.addEventListener('resize', updateRect, passive);
    }
    return () => {
      if (scroll) {
        scrollParents.forEach(scrollParent => {
          scrollParent.removeEventListener('scroll', updateRect, !passive.passive);
        });
      }
      if (resize) {
        window.removeEventListener('resize', updateRect, !passive.passive);
      }
    };
  }, [disabled, resize, scroll, scrollParents, updateRect, window]);
  const popupProps = isFunction(props.popup) ? props.popup() : props.popup;
  return [{
    ...props,
    popup: /*#__PURE__*/isValidElement(popupProps) && isNonNullable(popupContentStyle) ? /*#__PURE__*/cloneElement(popupProps, {
      style: {
        ...popupContentStyle,
        ...popupProps.props.style
      }
    }) : popupProps
  }];
};
//# sourceMappingURL=useTriggerPopupOverflowAuto.js.map
