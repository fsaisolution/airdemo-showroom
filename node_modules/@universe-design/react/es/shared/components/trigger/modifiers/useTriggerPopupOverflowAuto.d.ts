import type { RcTriggerPropsWithRef, UDAlignType } from '../types';
import type { SizeInfo } from '../../../../hooks';
/**
 * 已监听：
 * window scroll, resize
 * scrollParents scroll
 * popup resize
 * trigger resize
 * 未监听：
 * scrollParents resize
 */
export declare const useTriggerPopupOverflowAuto: (props: RcTriggerPropsWithRef, { popup, popupSize, popupAlign, childSize, scrollParents, scroll, resize, disabled, }: {
    popup: HTMLElement | null;
    popupSize: SizeInfo;
    popupAlign: UDAlignType;
    childSize: SizeInfo;
    scrollParents: (Element | typeof window | VisualViewport)[];
    scroll: boolean;
    resize: boolean;
    disabled: boolean;
}) => [RcTriggerPropsWithRef];
