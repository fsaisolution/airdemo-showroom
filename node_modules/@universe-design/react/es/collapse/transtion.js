function updateNodeStyle(node, height, opacity) {
  node.style.height = height;
  node.style.opacity = opacity;
}
function resetNodeStyle(node) {
  node.style.removeProperty('height');
  node.style.removeProperty('opacity');
  node.style.removeProperty('overflow');
}
export const collapseTransitionProps = {
  onEnter: node => {
    updateNodeStyle(node, '0', '0');
  },
  onEntering: node => {
    updateNodeStyle(node, `${node.scrollHeight}px`, '1');
  },
  onEntered: node => {
    resetNodeStyle(node);
  },
  onExit: node => {
    updateNodeStyle(node, `${node.offsetHeight}px`, '1');
  },
  onExiting: node => {
    updateNodeStyle(node, '0', '0');
  },
  onExited: node => {
    resetNodeStyle(node);
  },
  timeout: 200
};
//# sourceMappingURL=transtion.js.map
