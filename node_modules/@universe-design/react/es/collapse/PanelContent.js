import React from 'react';
import clsx from 'clsx';
export const PanelContent = /*#__PURE__*/React.forwardRef((props, ref) => {
  const {
    isActive,
    style,
    className,
    forceRender,
    children,
    prefixCls,
    role
  } = props;
  const [rendered, setRendered] = React.useState(isActive || forceRender);
  React.useEffect(() => {
    if (forceRender || isActive) {
      setRendered(true);
    }
  }, [forceRender, isActive]);
  if (!rendered) {
    return null;
  }
  return /*#__PURE__*/React.createElement("div", {
    style: style,
    className: clsx(`${prefixCls}-content`, className),
    ref: ref,
    role: role
  }, /*#__PURE__*/React.createElement("div", {
    className: `${prefixCls}-content-box`
  }, children));
});
//# sourceMappingURL=PanelContent.js.map
