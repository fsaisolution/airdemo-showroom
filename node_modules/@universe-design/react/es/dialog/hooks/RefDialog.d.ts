import React from 'react';
import type { ConfirmProps } from '../Confirm';
import type { ModalProps } from '../Modal';
export interface DialogIsntance<T> {
    destroy: () => void;
    update: (config: React.SetStateAction<T>) => void;
}
interface RefDialogProps<T> {
    defaultConfig?: T;
}
export declare const RefConfirm: React.ForwardRefExoticComponent<RefDialogProps<ConfirmProps> & React.RefAttributes<DialogIsntance<ConfirmProps>>>;
export declare const RefModal: React.ForwardRefExoticComponent<RefDialogProps<ModalProps> & React.RefAttributes<DialogIsntance<ModalProps>>>;
export {};
