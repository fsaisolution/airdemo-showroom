import type { CSSProperties, ReactNode, HTMLAttributes } from 'react';
import React from 'react';
import type { DialogProps } from './dialog';
import { DialogCancelType } from './dialog';
import type { ButtonProps } from '../button';
import type { StylexProps } from '@universe-design/stylex/react';
import './style';
export interface ModalContentExoticProps {
    /**
     * 对话框头部
     */
    header?: ReactNode;
    /**
     * 标题
     */
    title?: ReactNode;
    /**
     * 说明
     */
    description?: ReactNode;
    /**
     * 是否显示右上角的关闭按钮
     * @defaultValue true
     */
    closable?: boolean;
    /**
     * 是否显示取消按钮
     * @defaultValue true
     */
    enableCancelButton?: boolean;
    /**
     * 取消按钮文字
     * @defaultValue `取消`
     */
    cancelText?: string;
    /**
     * 取消按钮组件属性
     */
    cancelButtonProps?: ButtonProps;
    /**
     * 确认按钮文字
     * @defaultValue `确定`
     */
    okText?: string;
    /**
     * 确认按钮组件属性组件属性
     */
    okButtonProps?: ButtonProps;
    /**
     * 对话框主体 `style` 属性透传
     */
    bodyStyle?: CSSProperties;
    /**
     * 确定按钮是否为加载状态
     * @defaultValue false
     */
    confirmLoading?: boolean;
    /**
     * 底部内容，当不需要默认底部按钮时，可以设为 `footer={null}`
     */
    footer?: ReactNode;
    /**
     * 点击遮罩层、右上角叉、取消按钮或按下ESC键的回调
     */
    onCancel?: (e: React.MouseEvent<HTMLElement, MouseEvent> | React.KeyboardEvent | undefined, payload: {
        cancelType: DialogCancelType;
    }) => void;
    /**
     * 点击确定回调
     */
    onOk?: (e: React.MouseEvent<HTMLElement, MouseEvent>) => void;
    /**
     * 额外操作
     */
    extraAction?: ReactNode;
    /**
     * body 区域的高度自适应模式
     * - `off` 可视区高度按内容高度撑开
     * - `scroll` 可视区高度由内容和安全边距决定，内容可滚动
     * - `stretch` 可视区域内容一样高，内容拉伸，用户需要提供内容拉伸能力
     * @defaultValue `scroll`
     */
    bodyHeightResponsiveMode?: 'off' | 'scroll' | 'stretch';
    /**
     * 内容的最大高度
     */
    maxHeight?: React.CSSProperties['maxHeight'];
}
/**
 * @public
 * @docgen
 * <Dialog.Content /> 的属性。
 */
export interface ModalContentProps extends ModalContentExoticProps, Omit<HTMLAttributes<HTMLDivElement>, 'title'>, StylexProps {
}
/**
 * @public
 * @docgen
 * <Dialog /> 的属性。
 */
export interface ModalProps extends Pick<DialogProps, 'zIndex' | 'centered' | 'visible' | 'getContainer' | 'keyboardClosable' | 'destroyOnClose' | 'forceRender' | 'onAfterClose' | 'onAfterOpen' | 'size' | 'safeMargin' | 'enableAnimation' | 'initialFocusRef' | 'finalFocusRef' | 'enableFocusLock' | 'enableRestoreFocus' | 'width' | 'minWidth' | 'maxWidth'>, ModalContentExoticProps, Omit<HTMLAttributes<HTMLDivElement>, 'title'> {
    /**
     * 是否展示遮罩
     * @defaultValue true
     */
    mask?: boolean;
    /**
     * 点击遮罩是否允许关闭
     * @defaultValue true
     */
    maskClosable?: boolean;
    /**
     * 对话框 `style` 属性透传
     */
    style?: CSSProperties;
    /**
     * 对话框遮罩 `style` 属性透传
     */
    maskStyle?: CSSProperties;
    /**
     * 对话框 `className` 属性透传
     */
    className?: string;
    /**
     * 对话框外包裹 `className` 属性透传
     */
    wrapClassName?: string;
    transitionName?: string;
    maskTransitionName?: string;
    /**
     * @internal
     */
    onClose?: () => void;
}
export declare const ModalContent: React.ForwardRefExoticComponent<ModalContentProps & React.RefAttributes<HTMLDivElement>>;
export declare const Modal: React.ForwardRefExoticComponent<ModalProps & React.RefAttributes<HTMLDivElement>>;
