import { createContext, useContext, useContextSelector } from 'use-context-selector';
import { createContextHook } from '@universe-design/react-utilities';
export const {
  Provider: FormUIContextProvider,
  useContextSelector: useFormUIContextSelector
} = createContextHook('FormUIContext', {
  strict: false,
  defaultValue: {
    requiredMark: false
  },
  customizedContextMethods: {
    createContext,
    useContext,
    useContextSelector
  }
});
//# sourceMappingURL=FormUIContext.js.map
