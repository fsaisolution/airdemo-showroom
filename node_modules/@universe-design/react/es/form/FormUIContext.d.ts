import type { Required } from 'utility-types';
import type { ColProps } from '../col';
import type { DefaultSizeType } from '../util';
import type { FormItemUIProps } from './FormItemUI';
import type { ValidateMessages } from './interface';
export interface FormUIProps extends Pick<FormItemUIProps, 'errorDisplay'> {
    /**
     * Label 标签的文本对齐方式。
     * @defaultValue `right`
     */
    labelAlign?: 'left' | 'right';
    /**
     * Label 标签布局，同 Col 组件，设置 `span offset` 值，如 `{span: 3, offset: 12}` 或 `sm: {span: 3, offset: 12}`。
     */
    labelCol?: ColProps;
    /**
     * 表单布局
     * @defaultValue `vertical`
     */
    layout?: 'vertical' | 'horizontal' | 'inline';
    /**
     * 必选样式，可以切换为必选或者可选展示样式。此为 `Form` 配置，`Form.Item` 无法单独配置。
     * @defaultValue true
     */
    requiredMark?: boolean | 'optional';
    /**
     * 设置字段组件的尺寸
     * @defaultValue `medium`
     */
    size?: DefaultSizeType;
    /**
     * 需要为输入控件设置布局样式时，使用该属性，用法同 `labelCol`。
     */
    wrapperCol?: ColProps;
    /**
     * 验证提示模板
     */
    validateMessages?: ValidateMessages;
}
export type FormUIContextType = Required<Omit<FormUIProps, 'size' | 'layout'>, 'requiredMark'>;
export declare const FormUIContextProvider: import("react").ComponentType<{
    value: FormUIContextType;
    children: React.ReactNode;
}>, useFormUIContextSelector: <Selected>(selector: (value: FormUIContextType) => Selected) => Selected;
