import type { RuleItem } from 'async-validator';
import type { ValidateMessages } from '../interface';
type Value = any;
export declare function validateRule(namePathString: string, value: Value, rule: RuleItem, options: {
    validateMessages: ValidateMessages;
}, messageVariables?: Record<string, string>): Promise<string[]>;
export {};
