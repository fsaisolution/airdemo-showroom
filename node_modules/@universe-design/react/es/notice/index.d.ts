import type { ButtonProps } from '../button';
import type { HTMLAttributes } from 'react';
import React from 'react';
import type { PresetStatusColorType } from '../util';
import type { Slot } from '../shared/slot';
import './style';
export interface NoticeProps {
    /**
     * 设置主题，outlined 包含边框，filled 为无边框
     * @internal
     */
    theme?: 'outlined' | 'filled' | 'plain';
    /**
     * 状态类型
     * @defaultValue `info`
     */
    type?: PresetStatusColorType | 'loading';
    /**
     * 是否可关闭
     * @defaultValue false
     */
    closable?: boolean;
    /**
     * 关闭按钮，UD 不建议使用
     * @deprecated
     */
    closeText?: React.ReactNode;
    /**
     * 关闭按钮
     */
    closeButton?: Slot<ButtonProps>;
    /**
     * 提示内容，如果有`description`则会变为粗体样式
     */
    message: React.ReactNode;
    /**
     * 提示辅助描述
     */
    description?: React.ReactNode;
    /**
     * 跟在提示内容后面的操作按钮
     */
    action?: React.ReactNode;
    /**
     * 可见状态变化回调
     */
    onClose?: React.MouseEventHandler<HTMLSpanElement | HTMLButtonElement>;
    /**
     * UD 不支持 afterClose
     * @deprecated
     */
    afterClose?: () => void;
    /**
     * 是否显示图标，这里和其他开源框架不同，UD 默认为 true。
     * @defaultValue true
     */
    showIcon?: boolean;
    /**
     * [aria-role-attribute](https://www.w3.org/TR/2014/REC-html5-20141028/dom.html#aria-role-attribute)。
     * @defaultValue `alert`
     */
    role?: string;
    /**
     *  替换默认的 class 前缀
     */
    prefixCls?: string;
    /**
     * 是否为无边框，无 margin 模式
     * @defaultValue false
     */
    banner?: boolean;
    /**
     * 替换状态按钮。
     */
    icon?: React.ReactNode;
    /**
     * 可见状态的受控值。
     */
    visible?: boolean;
    /**
     * 标题对齐方式。在同时有`message`和`description`属性时不生效。
     * @defaultValue `left`
     */
    align?: 'left' | 'center';
    /**
     * 最大行数，默认 4
     */
    maxLine?: number;
}
export declare const Notice: React.ForwardRefExoticComponent<HTMLAttributes<HTMLDivElement> & NoticeProps & React.RefAttributes<HTMLDivElement>>;
