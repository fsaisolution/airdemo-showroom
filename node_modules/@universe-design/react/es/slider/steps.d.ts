import type { FC } from 'react';
export interface SliderMarksProps {
    max: number;
    min: number;
    range?: boolean;
    value?: number;
    rangeValue?: [number, number];
    dots?: number[];
    reverse?: boolean;
    vertical?: boolean;
    included?: boolean;
}
export declare const SliderSteps: FC<SliderMarksProps>;
