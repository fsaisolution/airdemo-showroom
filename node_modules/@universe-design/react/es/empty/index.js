import React from 'react';
import clsx from 'clsx';
import { EmptyNeutralNoContent } from '@universe-design/illustration-react';
import { useCssScope, useLocale } from '../config-provider/hooks';
import { Illustration } from '../illustration';
import './style';
export const Empty = props => {
  const locale = useLocale();
  const {
    image,
    description = locale.components.empty.description,
    className,
    ...restProps
  } = props;
  const cssScope = useCssScope('empty');
  const cls = clsx({
    [cssScope('--default')]: !image // 这里 `--default` 没有相关样式绑定，只是为了兼容之前的 className
  }, className);
  const illustrationProps = {
    ...restProps,
    description,
    prefixCls: cssScope(),
    className: cls,
    image: image !== null && image !== void 0 ? image : /*#__PURE__*/React.createElement(EmptyNeutralNoContent, null)
  };
  return /*#__PURE__*/React.createElement(Illustration, illustrationProps);
};
//# sourceMappingURL=index.js.map
