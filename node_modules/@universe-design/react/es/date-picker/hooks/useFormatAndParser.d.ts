import type { IFormat, IParse, MODE } from '../interface';
import moment from 'moment';
declare const _default: ({ format: formatProp, parse, mode, showTime, }: {
    format?: IFormat;
    parse?: IParse;
    mode: MODE;
    showTime: boolean;
}) => {
    formatValue: (value?: moment.Moment) => string;
    parseValue: (value?: string) => moment.Moment | null;
    format: IFormat;
};
export default _default;
