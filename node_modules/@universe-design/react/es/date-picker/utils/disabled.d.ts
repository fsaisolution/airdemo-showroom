import type { DateCalendarUIProps } from '../panels/DateCalendar/interface';
export declare function isInRange(date: moment.Moment, props: Pick<DateCalendarUIProps, 'mode' | 'minDate' | 'maxDate'>): boolean;
export declare function shouldCellDisabled(date: moment.Moment, props: Pick<DateCalendarUIProps, 'disabledDate' | 'mode' | 'minDate' | 'maxDate'>): boolean;
/**
 * 判断输入框输入的日期是否在禁用范围内，如果在的话，后续逻辑会忽略此值
 */
export declare function isInputDateDisabled(date: moment.Moment, props: Pick<DateCalendarUIProps, 'disabledDate' | 'minDate' | 'maxDate'>): boolean | undefined;
