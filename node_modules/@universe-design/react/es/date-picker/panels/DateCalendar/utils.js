export function isSelectCell(_ref) {
  let {
    selectedDates,
    selectedRange,
    date,
    mode
  } = _ref;
  return [...selectedDates, ...(selectedRange || [])].some(d => d.isSame(date, mode));
}
//# sourceMappingURL=utils.js.map
