import type { Required } from 'utility-types';
import type { TKeyValueMap } from '../../util';
import type { TreeCheckProps, TreeCheckStricklyProps } from '../interface';
import type { DoublyTreeNode } from '../util';
import type { ITreeCheckedContext } from './TreeCheckedContext';
import type { CheckStrategy } from './checkStrategy';
export declare function useTreeCheck(props: {
    doublyTreeNodeMap: TKeyValueMap<DoublyTreeNode>;
    checkStrategy: CheckStrategy;
} & (Required<TreeCheckProps, 'checkStrictly'> | TreeCheckStricklyProps), { skipped: skippedOpt }?: {
    skipped?: boolean;
}): ITreeCheckedContext;
