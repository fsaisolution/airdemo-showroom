import type { TKeyValueMap } from '../../util';
import type { TreeProps } from '../interface';
import type { DoublyTreeNode } from '../util';
export declare function useTreeSelect(props: {
    doublyTreeNodeMap: TKeyValueMap<DoublyTreeNode>;
} & Pick<TreeProps, 'selectedKeys' | 'onSelect' | 'multiple'> & Required<Pick<TreeProps, 'defaultSelectedKeys'>>): {
    selectedCache: Map<string, boolean>;
    toggleSelected: (key: string) => void;
};
