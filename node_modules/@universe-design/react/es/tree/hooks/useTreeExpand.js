/* eslint-disable max-depth */
import React from 'react';
import isEqual from 'lodash/isEqual';
import { useControlledState } from '@universe-design/react-utilities';
import { useEventCallback, useIsomorphicLayoutEffect as useLayoutEffect, useLatest } from '../../hooks';
import { array2Set, getTreeNodesAncestors, getTreeNodesWithAncestorsAndAllChildren } from '../util';
import { isFunction } from '../../util';
/**
 * 1. 默认使用 defaultExpandAll，defaultExpandKeys 和 expandKeys 去计算 expandedMap
 * 2. 点击 expand icon，setExpandKeys
 * 3. 如果有 searchValue，说明是 search 模式，第一次出来的时候通过搜索结果去展开对应的选项
 * 4. 点击 expand icon，修改 searchExpandedMap，
 */
export function useTreeExpand(props) {
  const {
    searchValue,
    filterTreeNode,
    doublyTreeNodeMap,
    doublyTreeNodeList,
    expandedKeys,
    defaultExpandedKeys,
    defaultExpandAll,
    onExpand
  } = props;

  // 根据 filterTreeNode 得到筛选后的所有节点
  const filteredTreeNodes = React.useMemo(() => getTreeNodesWithAncestorsAndAllChildren(doublyTreeNodeList, filterTreeNode), [filterTreeNode, doublyTreeNodeList]);

  // ======== 计算 Expand map ========
  const _expandedMap = expandedKeys && array2Set(expandedKeys);
  const [expandedMap, setExpandedMap] = useControlledState(() => {
    let res;
    if (defaultExpandAll) {
      res = array2Set(Object.values(doublyTreeNodeMap).map(node => node.__node__.value));
    } else if (!defaultExpandedKeys) {
      res = {};
    } else {
      res = array2Set(defaultExpandedKeys);
    }
    return res;
  }, _expandedMap, {
    isEqual
  });

  // ======== Search 相关 ========
  // 这段不拆出去主要是因为 doublyTreeNodeMap 在 tree 内部，提升出去太麻烦了
  const [searchExpandedMap, setSearchExpandedMap] = useControlledState(expandedMap, _expandedMap, {
    isEqual
  });
  const _setSearchExpandedMapLatest = useEventCallback(setSearchExpandedMap);
  useLayoutEffect(() => {
    // 如果传入了 expandedKeys 使展开状态受控，优先走受控逻辑
    if (expandedKeys) {
      return;
    }
    // 输入值以后展开对应的选项
    if (searchValue) {
      const keysToExpand = array2Set([...getTreeNodesAncestors(doublyTreeNodeList, filterTreeNode)].map(o => o.__node__.value));
      _setSearchExpandedMapLatest(keysToExpand);
    }
  }, [expandedKeys, filterTreeNode, doublyTreeNodeList, searchValue, _setSearchExpandedMapLatest]);
  const mergedExpandedMap = searchValue ? searchExpandedMap : expandedMap;

  /**
   * visibleMap 主要是根据 expand 状态来判断节点是否显示隐藏
   */
  const visibleMap = React.useMemo(() => {
    // 结果 cache
    const visibleCache = new Map();
    const getVisible = node => {
      let visible = visibleCache.get(node);
      if (visible !== undefined) {
        return visible;
      }
      visible = node.parent ? getVisible(node.parent) && Boolean(mergedExpandedMap[node.parent.__node__.value]) : true;
      visibleCache.set(node, visible);
      return visible;
    };
    return Object.values(doublyTreeNodeMap).reduce((pre, cur) => {
      const visible = getVisible(cur);
      pre[cur.__node__.value] = visible;
      return pre;
    }, {});
  }, [mergedExpandedMap, doublyTreeNodeMap]);

  // 根据 visibleMap 过滤出展开可见的节点
  const finalTreeNodeList = React.useMemo(() => doublyTreeNodeList.filter(
  // 使用 doublyTreeNodeList 保证顺序正确
  node => filteredTreeNodes.has(node) && visibleMap[node.__node__.value]), [doublyTreeNodeList, filteredTreeNodes, visibleMap]);
  const finalTreeNodeListRef = useLatest(finalTreeNodeList);

  /**
   * 切换 expanded 状态
   */
  const toggleExpanded = React.useCallback(key => {
    const _setExpandedMap = function () {
      setExpandedMap(...arguments);
      setSearchExpandedMap(arguments.length <= 0 ? undefined : arguments[0]);
    };
    const prevStateClone = {
      ...mergedExpandedMap
    };
    if (prevStateClone[key]) {
      delete prevStateClone[key];
      _setExpandedMap(prevStateClone, nextState => {
        isFunction(onExpand) && onExpand(Object.keys(nextState), {
          expanded: false,
          key
        });
      });
    } else {
      prevStateClone[key] = true;
      _setExpandedMap(prevStateClone, nextState => {
        isFunction(onExpand) && onExpand(Object.keys(nextState), {
          expanded: true,
          key
        });
      });
    }
  }, [mergedExpandedMap, onExpand, setExpandedMap, setSearchExpandedMap]);
  return React.useMemo(() => ({
    expandedMap: mergedExpandedMap,
    toggleExpanded,
    finalTreeNodeList,
    filteredTreeNodes,
    visibleMap,
    finalTreeNodeListRef
  }), [filteredTreeNodes, finalTreeNodeList, mergedExpandedMap, toggleExpanded, visibleMap, finalTreeNodeListRef]);
}
//# sourceMappingURL=useTreeExpand.js.map
