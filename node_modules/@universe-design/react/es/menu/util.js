export const milliSeconds2Seconds = _ref => {
  let {
    subMenuOpenDelay,
    subMenuCloseDelay
  } = _ref;
  const ret = {};
  if (typeof subMenuOpenDelay === 'number') {
    ret.subMenuOpenDelay = subMenuOpenDelay / 1000;
  }
  if (typeof subMenuCloseDelay === 'number') {
    ret.subMenuCloseDelay = subMenuCloseDelay / 1000;
  }
  return ret;
};
export const isSubMenu = c => {
  var _c$type;
  return (_c$type = c.type) === null || _c$type === void 0 ? void 0 : _c$type.isSubMenu;
};
//# sourceMappingURL=util.js.map
