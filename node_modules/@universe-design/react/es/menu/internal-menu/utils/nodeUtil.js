import * as React from 'react';
import toArray from '@universe-design/rc-util/lib/Children/toArray';
export function parseChildren(children, keyPath) {
  return toArray(children).map((child, index) => {
    if (/*#__PURE__*/React.isValidElement(child)) {
      var _eventKey, _child$props;
      const {
        key
      } = child;
      let eventKey = (_eventKey = (_child$props = child.props) === null || _child$props === void 0 ? void 0 : _child$props.eventKey) !== null && _eventKey !== void 0 ? _eventKey : key;
      const emptyKey = eventKey === null || eventKey === undefined;
      if (emptyKey) {
        eventKey = `tmp_key-${[...keyPath, index].join('-')}`;
      }
      const cloneProps = {
        key: eventKey,
        eventKey
      };
      if (process.env.NODE_ENV !== 'production' && emptyKey) {
        cloneProps.warnKey = true;
      }
      return /*#__PURE__*/React.cloneElement(child, cloneProps);
    }
    return child;
  });
}
//# sourceMappingURL=nodeUtil.js.map
