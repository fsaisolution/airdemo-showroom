import * as React from 'react';
import type { CSSMotionProps } from '@universe-design/rc-motion';
import type { BuiltinPlacements, MenuClickEventHandler, RcMenuMode, MenuSelectEventHandler, TriggerSubMenuAction, RenderIconType } from './interface';
export interface RcMenuProps extends Omit<React.HTMLAttributes<HTMLUListElement>, 'onClick' | 'onSelect' | 'dir'> {
    prefixCls?: string;
    children?: React.ReactNode;
    /**
     * 是否禁用
     */
    disabled?: boolean;
    /** @private Disable auto overflow. Pls note the prop name may refactor since we do not final decided. */
    disabledOverflow?: boolean;
    /**
     *  direction of menu
     *  @internal
     **/
    direction?: 'ltr' | 'rtl';
    mode?: RcMenuMode;
    /**
     * `inline`时菜单是否收起状态
     */
    inlineCollapsed?: boolean;
    /**
     * 初始展开的 SubMenu 菜单项`key`数组
     */
    defaultOpenKeys?: string[];
    /**
     * 当前展开的 SubMenu 菜单项`key`数组
     */
    openKeys?: string[];
    /**
     * @private
     **/
    activeKey?: string;
    /**
     * @private
     **/
    defaultActiveFirst?: boolean;
    /**
     * 是否允许选中
     * @defaultValue true
     */
    selectable?: boolean;
    /**
     * 是否允许多选
     * @defaultValue false
     */
    multiple?: boolean;
    /**
     * 初始选中的菜单项`key`数组
     */
    defaultSelectedKeys?: string[];
    /**
     * 当前选中的菜单项`key`数组
     */
    selectedKeys?: string[];
    /**
     * 被选中时调用
     */
    onSelect?: MenuSelectEventHandler;
    /**
     * 取消选中时调用
     */
    onDeselect?: MenuSelectEventHandler;
    /**
     * `inline`时菜单的缩进宽度
     */
    inlineIndent?: number;
    /**
     * Menu motion define. Use `defaultMotions` if you need config motion of each mode
     * @private
     * @internal
     **/
    motion?: CSSMotionProps;
    /**
     * @private
     * @internal
     * Default menu motion of each mode
     **/
    defaultMotions?: Partial<{
        [key in RcMenuMode | 'other']: CSSMotionProps;
    }>;
    /**
     * 用户鼠标进入子菜单后开启延时，单位为毫秒
     * @defaultValue 0
     */
    subMenuOpenDelay?: number;
    /**
     * 用户鼠标离开子菜单后关闭延时，单位为毫秒
     * @defaultValue 100
     */
    subMenuCloseDelay?: number;
    /**
     * 在子菜单展示之前就渲染进 DOM
     * @defaultValue false
     */
    forceSubMenuRender?: boolean;
    /**
     * SubMenu 展开/关闭的触发行为 ( mode = "inline" 模式下仅支持 click )
     * @defaultValue hover
     */
    triggerSubMenuAction?: TriggerSubMenuAction;
    builtinPlacements?: BuiltinPlacements;
    itemIcon?: RenderIconType;
    /**
     * 自定义次级菜单展开图标
     */
    expandIcon?: RenderIconType;
    /**
     * 自定义 Menu 折叠时的图标
     */
    overflowedIndicator?: React.ReactNode;
    /**
     * 自定义弹出容器
     */
    getPopupContainer?: (node: HTMLElement) => HTMLElement;
    /**
     * @private
     */
    onClick?: MenuClickEventHandler;
    /**
     * SubMenu 展开/关闭的回调
     */
    onOpenChange?: (openKeys: React.Key[]) => void;
}
declare const RcMenu: React.FC<RcMenuProps>;
export default RcMenu;
