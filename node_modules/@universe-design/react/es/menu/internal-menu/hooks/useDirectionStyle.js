import * as React from 'react';
import { navMenuDefaults } from '../../constants';
import { RcMenuContext } from '../context/MenuContext';
export default function useDirectionStyle(level) {
  const {
    mode,
    rtl,
    inlineIndent
  } = React.useContext(RcMenuContext);
  if (mode !== 'inline') {
    return {};
  }
  const paddingInlineStart = navMenuDefaults.itemPaddingX + (level - 1) * inlineIndent;
  return rtl ? {
    paddingRight: paddingInlineStart
  } : {
    paddingLeft: paddingInlineStart
  };
}
//# sourceMappingURL=useDirectionStyle.js.map
