import clsx from 'clsx';
import React from 'react';
import { useCssScope } from '../../config-provider';
import { Tag } from '../../tag';
export default function Counter(props) {
  const {
    showCount,
    count,
    maxLength,
    disabled,
    tagSize = 'extra-small'
  } = props;
  const cssScope = useCssScope('input');
  if (!showCount) {
    return null;
  }
  return /*#__PURE__*/React.createElement(Tag, {
    size: tagSize,
    className: cssScope('__count'),
    color: disabled ? 'neutral-option' : 'neutral-solid',
    disabled: disabled,
    style: props.style
  }, /*#__PURE__*/React.createElement("span", {
    className: clsx(count && maxLength && maxLength < count && cssScope('__count-exceed'), count && cssScope('__count-current'))
  }, count), maxLength && /*#__PURE__*/React.createElement("span", null, "/", maxLength));
}
//# sourceMappingURL=Counter.js.map
