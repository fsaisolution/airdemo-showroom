import * as React from 'react';
import { useTransferPanelContext } from '../hooks/PanelContext';
import { useTransferContext } from '../hooks/TransferContext';
import { TransferOptionContextProvider } from '../hooks/TransferOptionContext';
export const TransferOptionProvider = _ref => {
  let {
    option,
    children
  } = _ref;
  const {
    toggle,
    valueSet,
    searchValue
  } = useTransferContext();
  const {
    optionRenderer
  } = useTransferPanelContext();
  const isChecked = valueSet.has(option.value);
  return /*#__PURE__*/React.createElement(TransferOptionContextProvider, {
    value: {
      toggle,
      isChecked,
      option,
      searchValue,
      disabled: option.disabled || false
    }
  }, optionRenderer ? optionRenderer({
    option,
    isChecked,
    toggle
  }) : children);
};
//# sourceMappingURL=OptionProvider.js.map
