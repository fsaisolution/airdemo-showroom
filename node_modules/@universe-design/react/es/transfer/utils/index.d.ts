import type { TransferGroupOption, TransferOption, TransferValue } from '../interface';
export declare enum TransferListType {
    CAPION = 0,
    OPTION = 1
}
/**
 * List 只接受一维数组，这里是拍平后的数据结构
 */
export type FlatItem<V extends TransferValue = string, OP extends TransferOption<V> = TransferOption<V>> = {
    type: TransferListType.OPTION;
    option: OP;
} | {
    type: TransferListType.CAPION;
    group: Omit<TransferGroupOption<V, OP>, 'children'>;
};
/**
 * 将带分组的嵌套数据展平，
 * 因为 List 组件只接受一维数组
 */
export declare const flatTransferGroups: <V extends TransferValue, OP extends TransferOption<V>>(options: OP[], groupWeakMap: WeakMap<OP, TransferGroupOption<V, OP>>, showGroup: boolean) => FlatItem<V, OP>[];
export declare const defaultFilterOption: <V extends TransferValue, OP extends TransferOption<V>>(searchValue: string, option: OP) => boolean;
export declare const optionLabel: <V extends TransferValue>(option: TransferOption<V>) => string;
