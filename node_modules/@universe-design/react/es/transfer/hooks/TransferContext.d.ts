import React from 'react';
import type { TransferGroupOption, TransferOption, TransferValue, UseTransferProps } from '../interface';
import { TransferChangeAction } from '../interface';
/**
 * @public
 */
export interface TransferContextType<V extends TransferValue = string, OP extends TransferOption<V> = TransferOption<V>> {
    /**
     * cssScope
     */
    cssScope: (suffix: string) => string;
    /**
     * @internal
     * 切换全选状态
     */
    toggleSelectAll: () => void;
    /**
     * 已选值
     */
    value: V[];
    /**
     * 设置已选值
     */
    setValue: (newValue: V[], action: TransferChangeAction) => void;
    /**
     * 已选值集合
     * @internal
     */
    valueSet: Set<V>;
    /**
     * 已选值对应的 options
     */
    selectedOptions: OP[];
    /**
     * 清除全部
     */
    clearAll: () => void;
    /**
     * 根据 searchValue 筛选后的值
     */
    filteredOptions: OP[];
    /**
     * 输入的搜索值
     */
    searchValue: string;
    /**
     * 切换某一选项
     */
    toggle: (v: V) => void;
    /**
     * 设置搜索条件
     */
    setSearchValue: (searchValue: string) => void;
    /**
     * 当前筛选条件下的选项是否已经全部选中
     */
    isAllSelected: boolean;
    /**
     * 所有分组信息（key: 分组的 groupKey, value: 分组 ）
     */
    groupWeakMap: WeakMap<OP, TransferGroupOption<V, OP>>;
    /**
     * 是否显示分组信息
     */
    group: boolean | [boolean, boolean];
}
export declare function useTransfer<V extends TransferValue = string, OP extends TransferOption<V> = TransferOption<V>>(props: UseTransferProps<V, OP>): TransferContextType<V, OP>;
export declare const useTransferContext: <V extends TransferValue = string, OP extends TransferOption<V> = TransferOption<V>>() => TransferContextType<V, OP>;
export declare const TransferProvider: <V extends TransferValue = string, OP extends TransferOption<V> = TransferOption<V>>(props: {
    value: TransferContextType<V, OP>;
    children: React.ReactNode;
}) => React.ReactElement;
