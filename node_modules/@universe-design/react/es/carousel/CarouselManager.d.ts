import type { Size, Slide, CarouselTransitionDescriptor } from './transition-descriptors/utils';
export declare class CarouselManager {
    slideCount: number;
    duration: number;
    onSlideIndexChange: ((slide: number) => unknown) | undefined;
    private _subject;
    private _trackDescriptor;
    private _prevSlide;
    private _slide;
    private _windowSize;
    private _slideSizes;
    private _slidesToBeRendered;
    private _slideMetaCache;
    private _transitionDisposers;
    private _userEventLockSet;
    constructor(params: {
        trackDescriptor: CarouselTransitionDescriptor;
        defaultSlideIndex: number;
    });
    /**
     * ====================
     *   Public Methods
     * ====================
     */
    get state(): {
        slide: Slide;
        prevSlide: Slide | null;
        windowSize: Size;
        slideSizes: (Size & {
            mounted: boolean;
        })[];
        slidesToBeRendered: Slide[];
    };
    get canUserInteract(): boolean;
    subscribe: (onStoreChange: () => void) => () => void;
    /**
     * 注册 slide 节点
     */
    register: <T extends HTMLElement>(slide: Slide) => (element: T | null) => void;
    /**
     * 更新视口尺寸
     */
    updateWindowSize(size: Size): void;
    /**
     * 更新 slide 尺寸
     */
    updateSlideSize(slide: Slide, size: Size): void;
    /**
     * 按 slide index 来设置当前 slide
     */
    setSlideIndex(slide: number, { triggerChange }?: {
        triggerChange?: boolean;
    }): void;
    /**
     * 根据步长来设置新的 slide
     */
    setSlideIndexByStep(step: number): void;
    /**
     * 更新渲染的 slides 列表
     */
    updateSlidesToBeRendered(): void;
    getSlideSize(slideIndex: number): Size & {
        mounted: boolean;
    };
    resetNodes(options: {
        slick: boolean;
    }): void;
    /**
     * ====================
     *   Slide Meta Cache
     * ====================
     */
    private _marshalSlide;
    private _unmarshalSlide;
    private _getSlideMeta;
    private _setSlideMeta;
    private _updateSlideMeta;
    /**
     * ====================
     *   State
     * ====================
     */
    private _setSlide;
    private _emit;
    private _resetNode;
    private _resetNodeSlickly;
}
export declare const CarouselManagerContextProvider: import("react").ComponentType<{
    value: CarouselManager;
    children: React.ReactNode;
}>, useCarouselManagerContext: () => CarouselManager;
