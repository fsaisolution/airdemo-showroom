import type { CarouselManager } from '../CarouselManager';
import type { Disposer, Slide, CarouselTransitionDescriptor } from './utils';
export declare class FadeCarouselTransitionDescriptor implements CarouselTransitionDescriptor {
    defaults: {
        duration: number;
    };
    private _transitionTimingFunction;
    constructor(params?: {
        transitionTimingFunction?: string;
    });
    onGotoSlicklyStart(params: {
        slide: Slide;
        element: HTMLElement;
    }, manager: CarouselManager): void;
    onGotoSlicklyActive(params: {
        slide: Slide;
        element: HTMLElement;
    }, manager: CarouselManager): void | Disposer;
    onGotoSlicklyDone(params: {
        slide: Slide;
        element: HTMLElement;
    }, manager: CarouselManager): void;
    onGotoDirectly(params: {
        slide: Slide;
        element: HTMLElement;
    }, manager: CarouselManager): void;
    getSlidesTOBeRendered(manager: CarouselManager): Slide[];
    private _getSlidePosition;
    private _setPosition;
    private _show;
    private _hide;
}
