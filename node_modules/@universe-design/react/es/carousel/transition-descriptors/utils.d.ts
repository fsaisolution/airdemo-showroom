import type { CarouselManager } from '../CarouselManager';
export interface Position {
    x: number;
    y: number;
    z: number;
}
export interface Slide {
    groupIndex: number;
    slideIndex: number;
}
export declare const isSlideEqual: (s1: Slide, s2: Slide) => boolean;
export declare const getAbsoluteIndexFromSlide: (slide: Slide, slideCount: number) => number;
export declare const getSlideFromAbsoluteIndex: (absoluteIndex: number, total: number) => Slide;
export interface Size {
    width: number;
    height: number;
}
export type Disposer = () => void;
export interface CarouselTransitionDescriptor {
    defaults: {
        /**
         * 默认的动画时长
         */
        duration: number;
    };
    onGotoSlicklyStart: (params: {
        slide: Slide;
        element: HTMLElement;
    }, manager: CarouselManager) => void;
    onGotoSlicklyActive: (params: {
        slide: Slide;
        element: HTMLElement;
    }, manager: CarouselManager) => Disposer | void;
    onGotoSlicklyDone: (params: {
        slide: Slide;
        element: HTMLElement;
    }, manager: CarouselManager) => void;
    onGotoDirectly: (params: {
        slide: Slide;
        element: HTMLElement;
    }, manager: CarouselManager) => void;
    getSlidesTOBeRendered: (manager: CarouselManager) => Slide[];
}
