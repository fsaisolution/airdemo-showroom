export const isSlideEqual = (s1, s2) => s1.groupIndex === s2.groupIndex && s1.slideIndex === s2.slideIndex;
export const getAbsoluteIndexFromSlide = (slide, slideCount) => slide.groupIndex * slideCount + slide.slideIndex;
export const getSlideFromAbsoluteIndex = (absoluteIndex, total) => {
  const groupIndex = Math.floor(absoluteIndex / total);
  return {
    groupIndex,
    slideIndex: absoluteIndex - total * groupIndex
  };
};
//# sourceMappingURL=utils.js.map
