import type { CSSProperties, MouseEventHandler, ReactNode } from 'react';
import React from 'react';
import type { UDTriggerProps, UDAlignType } from '../shared/components/trigger';
import type { PositionType } from '../util';
import type { PopperProps, PopperRef } from '../shared/components/popper';
import './style';
export type TooltipPlacement = Exclude<PositionType, 'topCenter' | 'leftCenter' | 'rightCenter' | 'bottomCenter'>;
export declare const splitObject: (obj: any, keys: string[]) => {
    picked: any;
    omitted: any;
};
export interface BaseTooltipProps extends Pick<PopperProps, 'prefixCls' | 'className' | 'openClassName' | 'showAction' | 'hideAction' | 'visible' | 'defaultVisible' | 'onVisibleChange' | 'mouseEnterDelay' | 'mouseLeaveDelay' | 'destroyOnHide' | 'disabled' | 'getPopupContainer' | 'checkOverflow' | 'udTriggerRef' | 'trigger' | 'offset' | 'popupPortalContainer' | 'flip' | 'shift' | 'enableAutoHide' | 'eventListeners' | 'enableMaxHeight' | 'middlewares' | 'inline' | 'boundary' | 'initialFocus' | 'enableFocusLock' | 'enableRestoreFocus' | 'boundaryPadding' | 'popupStopPropagation'>, Pick<UDTriggerProps, 'popupAlign'> {
    /**
     * 是否显示指向小箭头
     * @deprecated 已废弃，请使用 `showArrow`
     * @defaultValue false
     */
    hideArrow?: boolean;
    /**
     * 是否显示指向小箭头
     * @defaultValue true
     */
    showArrow?: boolean;
    /**
     * 最大宽度
     * @deprecated 请使用 overlayStyle 进行覆盖
     */
    maxWidth?: string | number;
    /**
     * 气泡框位置
     * @defaultValue `top`
     */
    placement?: TooltipPlacement;
    /**
     * 卡片外容器 `style` 属性透传
     */
    overlayStyle?: CSSProperties;
    /**
     * 卡片外容器 `className` 属性透传
     */
    overlayClassName?: string;
    /**
     * @deprecated 边界溢出检查设置，已废弃，直接使用 `popupAlign.overflow` 进行规则覆盖
     */
    adjustOverflow?: boolean | UDAlignType['overflow'];
    children?: ReactNode;
    /**
     * 卡片外容器的鼠标移入事件
     */
    onOverlayMouseEnter?: MouseEventHandler<HTMLDivElement>;
    /**
     * 卡片外容器的鼠标移出事件
     */
    onOverlayMouseLeave?: MouseEventHandler<HTMLDivElement>;
    /**
     * 是否开启动画
     * @defaultValue true
     */
    enableAnimation?: boolean;
    /**
     * 是否给子元素添加wrapper，解决disabled态表单元素无法触发的问题
     * @defaultValue false
     */
    wrapDisabledChildren?: boolean;
}
/**
 * @public
 * @docgen
 */
export type TooltipProps = BaseTooltipProps & (({
    /**
     * 提示文字
     */
    title: ReactNode;
    overlay?: undefined;
} | {
    title?: undefined;
    overlay: ReactNode;
}) & {
    /**
     * @internal
     */
    arrowSize?: {
        x: number;
        y: number;
    };
    /**
     * @internal
     */
    arrowHasStroke?: boolean;
});
export declare const Tooltip: React.ForwardRefExoticComponent<TooltipProps & React.RefAttributes<PopperRef>>;
