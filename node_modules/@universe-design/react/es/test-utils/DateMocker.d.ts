import React from 'react';
export declare function useMockDate(date?: Date, { skipped, }?: {
    skipped?: boolean;
}): void;
export default function DateMocker(comp: React.ReactNode, date?: Date): React.ReactNode;
