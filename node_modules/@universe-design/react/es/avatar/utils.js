/* eslint-disable @typescript-eslint/no-magic-numbers */
import React from 'react';
import MoreOutlined from '@universe-design/icons-react/MoreOutlined';
export function getTextAvatarFontSizeBySize(size) {
  if (size >= 32) {
    return 12;
  }
  return 10;
}
export function getAvatarGroupMoreContent(_ref) {
  let {
    size,
    hideCount
  } = _ref;
  // 较小尺寸的 avatar 显示到 99+
  if (size < 32) {
    if (hideCount < 100) {
      return `+${hideCount}`;
    }
    return /*#__PURE__*/React.createElement(MoreOutlined, null);
  }

  //  较大尺寸的 avatar 可以显示到 999+
  if (hideCount < 1000) {
    return `+${hideCount}`;
  }
  return /*#__PURE__*/React.createElement(MoreOutlined, null);
}
//# sourceMappingURL=utils.js.map
