import * as React from 'react';
import type { ImageProps } from '../useImageViewerImages';
import '../style';
/**
 * 图片查看器下载工具 hook
 *
 * @public
 */
export declare const useImageViewerToolbarDownload: () => {
    /**
     * 下载图片
     */
    download: () => void;
};
export interface DownloadProps {
    onDownload?: (image: ImageProps, context: {
        downloadImage: (source: string | Blob, name: string) => void;
    }) => void;
}
export declare const Download: React.MemoExoticComponent<({ onDownload }: DownloadProps) => React.JSX.Element>;
