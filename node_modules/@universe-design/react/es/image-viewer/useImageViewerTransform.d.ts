/**
 * ================================
 *
 * useImageViewerTransform
 *
 * ================================
 */
import * as React from 'react';
import { mat2d, vec2 } from 'gl-matrix';
import type { Points } from './transform';
/**
 * 与图片变换相关的逻辑
 */
export declare const useImageViewerTransform: (props: {
    /**
     * 最大缩放比例
     * @defaultValue 5
     */
    maxScale?: number;
    /**
     * 最小缩放比例
     * @defaultValue 0.1
     */
    minScale?: number;
}) => {
    state: {
        getMatrix: () => Float32Array | [number, number, number, number, number, number];
        setMatrix: React.Dispatch<mat2d>;
        setMatrixByPoints: (pointsOrGetNextPoint: Points | ((point: vec2) => vec2)) => void;
        imageRef: React.RefObject<HTMLImageElement>;
        containerRef: React.RefObject<HTMLDivElement>;
        getContainerRect: (entry?: ResizeObserverEntry) => DOMRect;
        initTransform: (options?: {
            fitToContainer?: boolean;
        }) => void;
        getScale: () => number;
        scale: number;
        rotationRadians: number;
        scaleByOrigin: ({ origin: originParam, getNextScale, }: {
            /**
             * 缩放原点
             */
            origin?: vec2;
            /**
             * 缩放尺度
             */
            getNextScale: (currentScale: number) => number;
        }) => void;
        minScale: number;
        maxScale: number;
        /**
         * 判断图片区域是否超出容器区域
         */
        isImageContained: boolean;
    };
};
export type UseImageViewerTransformProps = Parameters<typeof useImageViewerTransform>[0];
