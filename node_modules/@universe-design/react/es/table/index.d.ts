import React from 'react';
import type { TableProps as RcTableProps } from '@universe-design/rc-table/lib/Table';
import type { SpinProps } from '../loading';
import type { DefaultSizeType } from '../util';
import type { Key, GetRowKey, TableLocale, ColumnsType, TableRowSelection, SorterResult, SortOrder, TableCurrentDataSource, TablePaginationConfig, TableHeaderType } from './interface';
import './style';
/**
 * @docgen
 * @public
 */
export interface TableProps<RecordType> extends Omit<RcTableProps<RecordType>, 'transformColumns' | 'internalRefs' | 'data' | 'columns' | 'scroll' | 'emptyText' | 'groupConfig' | 'rowKey'> {
    prefixCls?: string;
    dropdownPrefixCls?: string;
    className?: string;
    style?: React.CSSProperties;
    /**
     * 是否全边框
     */
    bordered?: boolean;
    /**
     * 表格尺寸：small | medium | large
     */
    size?: DefaultSizeType;
    /**
     * 配置默认的表头样式，默认为无底色，注意:多级表头始终为灰色底色
     * @defaultValue 'default'
     */
    headerType?: TableHeaderType;
    /**
     * 加载中
     */
    loading?: boolean | SpinProps;
    /**
     * 多语言
     */
    locale?: TableLocale;
    /**
     * 列配置
     */
    columns: ColumnsType<RecordType>;
    /**
     * 表格数据
     */
    dataSource: RcTableProps<RecordType>['data'];
    /**
     * 行是否可选择，及其配置
     */
    rowSelection?: TableRowSelection<RecordType>;
    /**
     * 是否展示分页，及其分页配置
     */
    pagination?: false | TablePaginationConfig;
    /**
     * 表格中所有浮层的渲染节点
     */
    getPopupContainer?: (triggerNode: HTMLElement) => HTMLElement;
    /**
     * 排序，也可以在 columns 中指定
     */
    sortDirections?: SortOrder[];
    /**
     * 是否显示排序提示文案，默认为 true
     */
    showSorterTooltip?: boolean;
    /**
     * 无数据时展示的缺省提示文案
     */
    emptyText?: React.ReactNode | (() => React.ReactNode);
    /**
     * 无数据时展示的内容
     */
    renderEmpty?: React.ReactNode | (() => React.ReactNode);
    /**
     * 排序、筛选、分页变化后的回调
     */
    onChange?: (pagination: TablePaginationConfig, filters: Record<string, (Key | boolean)[] | null>, sorter: SorterResult<RecordType> | SorterResult<RecordType>[], extra: TableCurrentDataSource<RecordType>) => void;
    /**
     * 表格是否可滚动，及滚动区域的宽高配置
     */
    scroll?: RcTableProps<RecordType>['scroll'] & {
        scrollToFirstRowOnChange?: boolean;
    };
    /**
     * 是否拉伸表格
     */
    stretch?: boolean;
    /**
     * 自定义行悬停状态，不会禁用触发事件
     */
    rowHoverable?: (record: RecordType, index: number, indent: number) => boolean;
    /**
     * 表格数据分组配置
     */
    groupConfig?: RcTableProps<RecordType>['groupConfig'] & {
        groupTitleRenderer?: (groupKey: Key) => React.ReactNode;
    };
    /**
     * 获取行数据对应的唯一键
     */
    rowKey?: string | GetRowKey<RecordType>;
}
export declare const Table: {
    <RecordType extends Record<string, any>>(props: TableProps<RecordType>): React.JSX.Element;
    SELECTION_ALL: string;
    SELECTION_INVERT: string;
    Summary: typeof import("@universe-design/rc-table/lib/Footer/Summary").default;
};
export * from './interface';
