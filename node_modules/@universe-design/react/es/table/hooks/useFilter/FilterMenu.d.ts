import React from 'react';
import type { TableLocale, ColumnFilterItem, Key, GetPopupContainerInTable } from '../../interface';
interface FilterMenuProps {
    prefixCls: string;
    dropdownPrefixCls: string;
    tableLocale: TableLocale;
    filters: ColumnFilterItem[];
    filterMultiple: boolean;
    openKeys: Key[];
    selectedKeys: Key[];
    getPopupContainer?: GetPopupContainerInTable;
    onMenuClick: () => void;
    onSelectKeys: ({ selectedKeys }: {
        selectedKeys: Key[];
    }) => void;
    onOpenChange: (openKeys: Key[]) => void;
    onReset: () => void;
    onConfirm: () => void;
}
export default function FilterMenu(props: FilterMenuProps): React.JSX.Element;
export {};
