import React from 'react';
import clsx from 'clsx';
import stylex from '@universe-design/stylex';
import { cssBlockBase, cssTypographyTitle4, cssTypographyBody0 } from '../stylex';
import { useCssScope } from '../config-provider/hooks';
import { sxDashed, sxDirection, sxDivider, sxTitle, sxWithText, sxLeft, sxRight, sxWithTextDashed } from './sx';
export const Divider = props => {
  const {
    text,
    children,
    style,
    className,
    type = 'horizontal',
    plain = false,
    dashed = false,
    orientation = 'center'
  } = props;
  const cssScope = useCssScope('divider');
  const hasText = Boolean(children) || Boolean(text);
  const cssTypo = (() => {
    if (!hasText || plain) {
      return cssTypographyBody0;
    }
    return cssTypographyTitle4;
  })();
  const cls = clsx(cssScope(), cssScope(`--${type}`), {
    [cssScope('--with-text')]: hasText,
    [cssScope(`--with-text-${orientation}`)]: hasText,
    [cssScope('--dashed')]: dashed,
    [cssScope('--plain')]: plain
  }, stylex(sxDivider, cssBlockBase, cssTypo, sxDirection(type), hasText && sxWithText, dashed && sxDashed, orientation === 'left' && sxLeft, orientation === 'right' && sxRight, hasText && dashed && sxWithTextDashed), className);
  return /*#__PURE__*/React.createElement("div", {
    style: style,
    className: cls
  }, (() => {
    if (hasText) {
      return /*#__PURE__*/React.createElement("span", {
        className: clsx(cssScope('__title'), stylex(sxTitle))
      }, text !== null && text !== void 0 ? text : children);
    }
    return null;
  })());
};
//# sourceMappingURL=index.js.map
