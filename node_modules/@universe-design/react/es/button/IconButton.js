import _extends from "@babel/runtime/helpers/esm/extends";
import React, { forwardRef } from 'react';
import { ButtonBase } from './ButtonBase';
import tuple from '../util/tuple';
import { omit } from '../util/omit';
import './style';
import { markAsDisabledableComp } from '../shared/markAsDisabledableComp';
const omitProps = tuple('children', 'type', 'size', 'endIcon', 'block', 'noMinWidth');
export const pickIconButtonProps = props => omit(props, omitProps);

/**
 * 支持原生`button`的所有事件和属性。
 * @public
 * @docgen
 * @privateRemarks
 * `extra-large`: 28px
 *   `large`: 24px
 *   `medium`: 20px
 *   `small`: 16px
 *   `extra-small`: 12px
 */

const IconButton = /* #__PURE__*/markAsDisabledableComp(/*#__PURE__*/forwardRef((props, ref) => {
  const {
    style = {},
    size
  } = props;
  let usedStyle = {
    ...style
  };
  let usedSize = size;
  if (typeof size === 'number') {
    usedStyle = {
      ...usedStyle,
      fontSize: `${props.size}px`
    };
    usedSize = undefined;
  }
  return /*#__PURE__*/React.createElement(ButtonBase, _extends({}, props, {
    size: usedSize,
    style: usedStyle,
    type: "icon",
    ref: ref
  }));
}));
if (process.env.NODE_ENV !== "production") {
  IconButton.displayName = 'IconButton';
}
export { IconButton };
//# sourceMappingURL=IconButton.js.map
