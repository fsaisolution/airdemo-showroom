import * as React from 'react';
import type { PopoverProps } from '..';
import { Popover } from '..';
import type { ButtonProps } from '../../button';
import { Button } from '../../button';

export interface PopperTestDemoBasicProps extends PopoverProps {
  referenceProps?: ButtonProps;
}

export default ({ referenceProps = {}, ...props }: PopperTestDemoBasicProps) => (
  <Popover {...props}>
    <Button {...referenceProps}>Reference</Button>
  </Popover>
);
