import React from 'react';
import { Select } from '../../select';
import { usePerformanceLogger } from '../../_stories/common/PerformanceContent';
import { Popover } from '..';
import { Button } from '../../button';

export default () => {
  usePerformanceLogger('PopoverMemoryTest');
  const complexNode = (
    <>
      {Array(100)
        .fill(0)
        .map(() => (
          <Select />
        ))}
    </>
  );
  return (
    <>
      <div>
        <Popover trigger="click" content={complexNode} destroyOnHide={false}>
          <Button>button</Button>
        </Popover>
      </div>
      {complexNode}
    </>
  );
};
