// autogen by ../../../../scripts/generate-stories.ts

import type { StoryFn as Story } from '@storybook/react';
import C4InfiniteScroll from '../demo/4.infinite-scroll';
import CLayoutIcon from '../demo/layout-icon';
import CPopupMemoryLeak from '../demo/popup-memory-leak';
if (!(C4InfiniteScroll as Story).storyName) {
  (C4InfiniteScroll as Story).storyName = '4 infinite scroll';
}
if (!(CLayoutIcon as Story).storyName) {
  (CLayoutIcon as Story).storyName = 'Layout icon';
}
if (!(CPopupMemoryLeak as Story).storyName) {
  (CPopupMemoryLeak as Story).storyName = 'Popup memory leak';
}

export default {
  title: 'Components/Popover/截图测试',
  tags: ['autodocs'],
};

export { C4InfiniteScroll, CLayoutIcon, CPopupMemoryLeak };
