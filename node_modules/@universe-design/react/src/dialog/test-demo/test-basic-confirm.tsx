/**
 * 用于测试 dialog 在各种情形下 inset 计算是否正常
 */
import * as React from 'react';
import type { ConfirmProps } from '../Confirm';
import { Confirm } from '../Confirm';

export interface DialogTestDemoBasicConfirmProps extends ConfirmProps {
  bodyHeight?: number | string;
}

export default ({ bodyHeight = 0, ...props }: DialogTestDemoBasicConfirmProps) => (
  <Confirm {...props}>
    <div
      style={{
        height: bodyHeight,
        background: 'var(--primary-fill-default)',
      }}
    />
  </Confirm>
);
