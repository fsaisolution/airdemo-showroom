import stylex from '@universe-design/stylex';
import { cssFlexBase, sxBorderRadius } from '../stylex';

/**
 * tokens
 */
const DialogIconSize = 24;
const DialogIconTitleSpacing = 16;

export const sxDialogIconWrapper = stylex.create({
  marginRight: DialogIconTitleSpacing,
});

export const sxDialogIcon = stylex.create({
  fontSize: DialogIconSize,
  lineHeight: 0,
  alignSelf: 'center',
});

export const sxModalContent = {
  root: stylex.compose(
    cssFlexBase,
    sxBorderRadius.md,
    stylex.create({
      position: 'relative',
      flexDirection: 'column',
      width: '100%',
    }),
  ),
  scrollable: stylex.create({
    maxHeight: '100%',
  }),
  stretch: stylex.create({
    height: '100%',
  }),
};
