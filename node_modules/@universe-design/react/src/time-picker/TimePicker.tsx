import type React from 'react';
import type moment from 'moment';
import generateConfig from '../shared/components/picker/generate/moment';
import generatePicker from '../shared/components/picker/generatePicker';

export const { TimePicker } = generatePicker<moment.Moment>(generateConfig);
/**
 * @docgen
 * @public
 */
export type TimePickerProps = React.ComponentProps<typeof TimePicker>;
