import * as React from 'react';
import type { Meta } from '@storybook/react';
import { Tag, Button } from '../..';

export default {
  title: 'Components/Tag/临时示例',
  component: Tag,
} satisfies Meta;

export const Issue1145 = () => {
  const [v, setV] = React.useState(false);
  return (
    <>
      <div>在 iPad safari 上可以复现，tag 大小会变，加上 -webkit-text-size-adjust: none 以后就不变了</div>
      <Button onClick={() => setV(!v)}>contentEditable: {v}</Button>
      <div style={{ border: `1px solid ${v ? 'red' : 'transparent'}` }} contentEditable={v}>
        <Tag size="extra-small">{123}</Tag>
      </div>
    </>
  );
};
