/* eslint-disable max-len */
import * as React from 'react';
import { Input } from '@universe-design/react';

const { TextArea } = Input;

export default () => <TextArea style={{ width: 300 }} placeholder="Basic usage" />;

export const screenshot = {
  skip: true,
};
