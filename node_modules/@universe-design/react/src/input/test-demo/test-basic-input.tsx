/* eslint-disable @typescript-eslint/ban-ts-comment */
import * as React from 'react';
import type { InputProps } from '@universe-design/react';
import { Input } from '@universe-design/react';
import { handleClick } from '../test-utils/handleClick';

export default (props: InputProps) => <Input placeholder="123" onClick={handleClick} {...props} />;
