/* eslint-disable max-len */
import * as React from 'react';
import type { InputProps } from '@universe-design/react';
import { Radio, Input } from '@universe-design/react';
import { handleClick } from '../test-utils/handleClick';

export default (props: InputProps) => (
  <Radio>
    <Input placeholder="123" onClick={handleClick} {...props} />
  </Radio>
);

export const screenshot = {
  skip: true,
};
