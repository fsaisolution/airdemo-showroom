import React, { useRef } from 'react';
import { BackTop } from '..';

export default {
  title: 'Components/BackTop/临时示例',
  component: BackTop,
};

export const DifferentStates = () => {
  const ref = useRef<HTMLDivElement>(null);
  return (
    <div ref={ref} style={{ height: 'calc(100vh - 2em)', overflowY: 'auto' }}>
      <div style={{ backgroundColor: '#80808026', height: '200vh' }} />
      <BackTop target={() => ref.current as HTMLElement} />
    </div>
  );
};
