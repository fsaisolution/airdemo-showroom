import React from 'react';
import { Layout } from '../index';
import type { CollapseType } from '../Sider';
import { Menu } from '../../menu';
import { Breadcrumb } from '../../breadcrumb';
import MailOutlined from '@universe-design/icons-react/MailOutlined';
import AppstoreOutlined from '@universe-design/icons-react/AppstoreOutlined';
import SettingOutlined from '@universe-design/icons-react/SettingOutlined';

const { SubMenu } = Menu;
const { Header, Content, Footer, Sider } = Layout;

export default {
  title: 'Components/Layout/临时示例', // 注意：这里需要修改成自己对应 Component name。
  component: Layout,
};

export const TestTooltip = () => (
  <Sider collapsible>
    <Menu mode="inline">
      <Menu.Item key="1">Light</Menu.Item>
    </Menu>
  </Sider>
);

export const AllLayout = () => {
  // const [collapsed, setCollapsed] = useState<boolean>(false);
  const onCollapse = (state: boolean, _type: CollapseType) => console.log(state, _type);
  return (
    <div>
      <Layout>
        <Layout.Sider
          breakpoint="lg"
          collapsedWidth="0"
          onCollapse={onCollapse}
          onBreakpoint={broken => {
            console.log(broken);
          }}
        >
          <Menu mode="inline" defaultSelectedKeys={['4']}>
            <Menu.Item key="1" icon={<MailOutlined />}>
              Nav 1
            </Menu.Item>
            <Menu.Item key="2" icon={<AppstoreOutlined />}>
              Nav 2
            </Menu.Item>
            <Menu.Item key="3" icon={<SettingOutlined />}>
              Nav 3
            </Menu.Item>
            <Menu.Item key="4" icon={<MailOutlined />}>
              Nav 4
            </Menu.Item>
          </Menu>
        </Layout.Sider>
        <Layout>
          <Layout.Header>
            <div>Header</div>
          </Layout.Header>
          <Layout.Content>
            <div style={{ height: 1000 }}>Content</div>
          </Layout.Content>
          <Layout.Footer>
            <div>Footer</div>
          </Layout.Footer>
        </Layout>
      </Layout>
    </div>
  );
};

export const Slider = () => {
  const [, setCollapsed] = React.useState<boolean>(false);
  return (
    <Layout style={{ minHeight: 720, overflow: 'hidden' }}>
      <Sider collapsible onCollapse={setCollapsed}>
        <div className="logo" />
        <Menu defaultSelectedKeys={['1']} mode="inline">
          <Menu.Item key="1" icon={<MailOutlined />}>
            Option 1
          </Menu.Item>
          <Menu.Item key="2" icon={<MailOutlined />}>
            Option 2
          </Menu.Item>
          <SubMenu key="sub1" icon={<AppstoreOutlined />} title="User">
            <Menu.Item key="3">Tom</Menu.Item>
            <Menu.Item key="4">Bill</Menu.Item>
            <Menu.Item key="5">Alex</Menu.Item>
          </SubMenu>
          <SubMenu key="sub2" icon={<SettingOutlined />} title="Team">
            <Menu.Item key="6">Team 1</Menu.Item>
            <Menu.Item key="8">Team 2</Menu.Item>
          </SubMenu>
          <Menu.Item key="9" icon={<SettingOutlined />}>
            Setting
          </Menu.Item>
        </Menu>
      </Sider>
      <Layout className="site-layout">
        <Header className="site-layout-background" style={{ padding: 0 }} />
        <Content style={{ margin: '0 16px' }}>
          <Breadcrumb style={{ margin: '16px 0' }}>
            <Breadcrumb.Item>User</Breadcrumb.Item>
            <Breadcrumb.Item>Bill</Breadcrumb.Item>
          </Breadcrumb>
          <div className="site-layout-background" style={{ padding: 24, minHeight: 360 }}>
            Bill is a cat.
          </div>
        </Content>
        <Footer style={{ textAlign: 'center' }}>Created by Universe Design</Footer>
      </Layout>
    </Layout>
  );
};
