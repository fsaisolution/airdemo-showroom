import React, { forwardRef } from 'react';
import clsx from 'clsx';
import type { BaseProps } from './Layout';
import { useCssScope } from '../config-provider/hooks';

import './style';

const Content = forwardRef<HTMLElement, BaseProps>((props, ref) => {
  const { className, children, ...restProps } = props;
  const cssScope = useCssScope('layout__content');
  const cls = clsx(
    {
      [cssScope()]: true,
    },
    className,
  );

  return (
    <main ref={ref} className={cls} {...restProps}>
      {children}
    </main>
  );
});

export default Content;
