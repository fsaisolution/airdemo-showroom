import React, { forwardRef } from 'react';
import clsx from 'clsx';
import type { BaseProps } from './Layout';
import { useCssScope } from '../config-provider/hooks';

import './style';

const Footer = forwardRef<HTMLElement, BaseProps>((props, ref) => {
  const { className, children, ...restProps } = props;
  const cssScope = useCssScope('layout__footer');
  const cls = clsx(
    {
      [cssScope()]: true,
    },
    className,
  );

  return (
    <footer ref={ref} className={cls} {...restProps}>
      {children}
    </footer>
  );
});

export default Footer;
