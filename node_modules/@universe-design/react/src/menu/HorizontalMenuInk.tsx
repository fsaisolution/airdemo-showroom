import React from 'react';
import { MenuContext } from './MenuContext';

export const HorizontalMenuInk = () => {
  const { mode, prefixCls } = React.useContext(MenuContext);
  return mode === 'horizontal' ? <div className={`${prefixCls}-item__ink`} /> : null;
};
