// autogen by ../../../../scripts/generate-stories.ts

import type { StoryFn as Story } from '@storybook/react';
import CAllHideCheckmark from '../demo/all.hideCheckmark';
import CAll from '../demo/all';
import CTextOverflow from '../demo/text-overflow';
if (!(CAllHideCheckmark as Story).storyName) {
  (CAllHideCheckmark as Story).storyName = 'All hide checkmark';
}
if (!(CAll as Story).storyName) {
  (CAll as Story).storyName = 'All';
}
if (!(CTextOverflow as Story).storyName) {
  (CTextOverflow as Story).storyName = 'Text overflow';
}

export default {
  title: 'Components/Dropdown/截图测试',
  tags: ['autodocs'],
};

export { CAllHideCheckmark, CAll, CTextOverflow };
