import * as React from 'react';
import type { MenuProps } from '../menu';

// DropdownMenuContext
export const DropdownMenuContext = React.createContext<Pick<MenuProps, 'className' | 'onClick'> | null>(null);
