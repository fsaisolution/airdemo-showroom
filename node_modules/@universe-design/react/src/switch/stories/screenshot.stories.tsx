// autogen by ../../../../scripts/generate-stories.ts

import type { StoryFn as Story } from '@storybook/react';
import CAll from '../demo/all';
if (!(CAll as Story).storyName) {
  (CAll as Story).storyName = 'All';
}

export default {
  title: 'Components/Switch/截图测试',
  tags: ['autodocs'],
};

export { CAll };
