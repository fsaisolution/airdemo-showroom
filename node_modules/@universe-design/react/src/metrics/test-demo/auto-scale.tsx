import * as React from 'react';
import { Metrics } from '../Metrics';

export default (props: { autoScale: boolean }) => (
  <div style={{ width: '200px' }}>
    <Metrics
      mainTitle="标题"
      mainValue={{
        children: '¥100000000000000000000万',
        autoScaleContent: props.autoScale,
      }}
    />
  </div>
);
