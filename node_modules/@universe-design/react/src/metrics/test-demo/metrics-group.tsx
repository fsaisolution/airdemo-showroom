import * as React from 'react';
import { Metrics } from '../Metrics';
import { Card } from '../../card';
import { MetricsGroup } from '../MetricsGroup';

export default (props: { title: string; label: string[] }) => (
  <MetricsGroup>
    <div style={{ width: '900px' }}>
      <Card style={{ width: '300px', display: 'inline-block' }}>
        <Metrics
          mainTitle="单行标题"
          mainValue="¥100万"
          secondary={{
            data: [{ label: '单行数据', value: '10%', type: 'increase' }],
          }}
        />
      </Card>
      <Card style={{ width: '300px', display: 'inline-block' }}>
        <Metrics
          mainTitle={props.title}
          mainValue="¥200万"
          secondary={{
            data: props.label.map(item => ({
              type: 'increase',
              label: item,
              value: '17%',
              color: 'danger',
            })),
          }}
        />
      </Card>
    </div>
  </MetricsGroup>
);
