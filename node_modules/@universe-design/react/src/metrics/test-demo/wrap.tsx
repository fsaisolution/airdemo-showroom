import * as React from 'react';
import { Metrics } from '../Metrics';

export default (props: { label: string[] }) => (
  <div
    style={{
      width: '300px',
    }}
  >
    <Metrics
      mainTitle="销售金额"
      mainInfoIcon={{
        tooltip: {
          title: '次要指标自动换行',
        },
      }}
      mainValue="¥2,343.17万"
      secondary={{
        data: props.label.map(item => ({
          type: 'increase',
          label: item,
          value: '17%',
          color: 'danger',
        })),
      }}
    />
  </div>
);
