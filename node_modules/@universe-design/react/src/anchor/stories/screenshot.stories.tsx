// autogen by ../../../../scripts/generate-stories.ts

import type { StoryFn as Story } from '@storybook/react';
import CActiveLink from '../demo/activeLink';
import CFixed from '../demo/fixed';
import CLongTextScreenshot from '../demo/long-text.screenshot';
if (!(CActiveLink as Story).storyName) {
  (CActiveLink as Story).storyName = 'Active link';
}
if (!(CFixed as Story).storyName) {
  (CFixed as Story).storyName = 'Fixed';
}
if (!(CLongTextScreenshot as Story).storyName) {
  (CLongTextScreenshot as Story).storyName = 'Long text screenshot';
}

export default {
  title: 'Components/Anchor/截图测试',
  tags: ['autodocs'],
};

export { CActiveLink, CFixed, CLongTextScreenshot };
