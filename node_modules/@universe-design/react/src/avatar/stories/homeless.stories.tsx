import * as React from 'react';
import type { Meta } from '@storybook/react';

import { Avatar } from '../..';
import { PerformanceContent } from '../../_stories/common/PerformanceContent';

const AvatarGroup = Avatar.Group;

export default {
  title: 'Components/Avatar/临时示例',
  component: Avatar,
  subcomponents: { AvatarGroup },
} satisfies Meta;

export const Performance = () => (
  <>
    <PerformanceContent
      componentName="Squircle avatar"
      count={1000}
      itemRenderer={i => (
        <Avatar size="large" shape="squircle">
          {/* abc */}
        </Avatar>
      )}
    />
    <PerformanceContent
      componentName="Square avatar"
      count={1000}
      itemRenderer={i => (
        <Avatar size="large" shape="square">
          {/* abc */}
        </Avatar>
      )}
    />
  </>
);
