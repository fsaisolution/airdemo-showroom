---
title: 头像 Avatar
desc: 使用圆形或方形头像来代表个人、群体、公司及产品，支持以图片、图标或字符形式展示。
---

# 头像 Avatar

使用圆形或方形头像来代表个人、群体、公司及产品，支持以图片、图标或字符形式展示。

%%Content%%

## API

#include(AvatarProps;AvatarGroupProps)
Tip：你可以设置 `icon` 或 `children` 作为图片加载失败的默认降级行为，优先级为 `icon` > `children`。
