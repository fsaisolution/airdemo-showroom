import stylex from '@universe-design/stylex';
import { cssFontMedium } from './typography-font-weight-medium.css';

export const cssTypographyTitle3 = stylex.compose(
  cssFontMedium,
  stylex.create({
    fontSize: 18,
    // lineHeight: 1.5556,
    lineHeight: '28px',
  }),
);
cssTypographyTitle3.alias = 'ud-typography-title-3';
