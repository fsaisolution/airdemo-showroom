import stylex from '@universe-design/stylex';
import { cssFontMedium } from './typography-font-weight-medium.css';

export const cssTypographyTitle4 = stylex.compose(
  cssFontMedium,
  stylex.create({
    fontSize: 16,
    // lineHeight: 1.5,
    lineHeight: '24px',
  }),
);
cssTypographyTitle4.alias = 'ud-typography-title-4';
