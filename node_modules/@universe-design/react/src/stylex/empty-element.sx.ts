import stylex from '@universe-design/stylex';

export const sxEmptyElement = stylex.create({
  display: 'inline-block',
  width: 0,
  overflow: 'hidden',
  visibility: 'hidden',
});
sxEmptyElement.alias = 'ud-empty-element';
