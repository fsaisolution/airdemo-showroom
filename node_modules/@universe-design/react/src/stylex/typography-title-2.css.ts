import stylex from '@universe-design/stylex';
import { cssFontMedium } from './typography-font-weight-medium.css';

export const cssTypographyTitle2 = stylex.compose(
  cssFontMedium,
  stylex.create({
    fontSize: 20,
    // lineHeight: 1.5,
    lineHeight: '30px',
  }),
);
cssTypographyTitle2.alias = 'ud-typography-title-2';
