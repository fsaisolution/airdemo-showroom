import stylex from '@universe-design/stylex';
import { cssComponentBase } from './component-base.css';

export const cssFlexBase = stylex.compose(
  cssComponentBase,
  stylex.create({
    display: 'flex',
  }),
);
