// autogen by ../../../../scripts/generate-stories.ts

import type { StoryFn as Story } from '@storybook/react';
import CButtonOverflowScreenshot from '../demo/button-overflow.screenshot';
import COptionTypeAll from '../demo/option-type-all';
import COverLength from '../demo/over-length';
import CZAllStatusScreenshot from '../demo/z.all-status.screenshot';
if (!(CButtonOverflowScreenshot as Story).storyName) {
  (CButtonOverflowScreenshot as Story).storyName = 'Button overflow screenshot';
}
if (!(COptionTypeAll as Story).storyName) {
  (COptionTypeAll as Story).storyName = 'Option type all';
}
if (!(COverLength as Story).storyName) {
  (COverLength as Story).storyName = 'Over length';
}
if (!(CZAllStatusScreenshot as Story).storyName) {
  (CZAllStatusScreenshot as Story).storyName = 'Z all status screenshot';
}

export default {
  title: 'Components/Radio/截图测试',
  tags: ['autodocs'],
};

export { CButtonOverflowScreenshot, COptionTypeAll, COverLength, CZAllStatusScreenshot };
