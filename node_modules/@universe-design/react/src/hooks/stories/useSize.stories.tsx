import * as React from 'react';
import { useLogger } from '@universe-design/react-utilities';
import { useSize } from '../useSize';

export default {
  title: 'Hooks/useSize',
};

export { default as BoxSizing } from '../demo/1.useSize.box-sizing';

export const Basic = () => {
  const [ref, size] = useSize<HTMLDivElement>();
  useLogger('Basic', size);
  return <div ref={ref} />;
};
