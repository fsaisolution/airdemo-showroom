import { Text, ExpandableText } from '@universe-design/react';
import type { ExpandableTextProps } from '@universe-design/react';
import * as React from 'react';
import type { TextProps } from '../../text';

export interface ExpandableTextTestDemoBasicProps extends ExpandableTextProps, Pick<TextProps, 'variant'> {}

export default ({ variant, ...props }: ExpandableTextTestDemoBasicProps) => (
  <Text {...{ variant }}>
    <ExpandableText {...props} />
  </Text>
);
