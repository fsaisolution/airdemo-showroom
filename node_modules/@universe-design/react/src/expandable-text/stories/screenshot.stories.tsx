// autogen by ../../../../scripts/generate-stories.ts

import type { StoryFn as Story } from '@storybook/react';
import CForm from '../demo/form';
import CMixedLayoutScreenshot from '../demo/mixed-layout.screenshot';
if (!(CForm as Story).storyName) {
  (CForm as Story).storyName = 'Form';
}
if (!(CMixedLayoutScreenshot as Story).storyName) {
  (CMixedLayoutScreenshot as Story).storyName = 'Mixed layout screenshot';
}

export default {
  title: 'Components/ExpandableText/截图测试',
  tags: ['autodocs'],
};

export { CForm, CMixedLayoutScreenshot };
