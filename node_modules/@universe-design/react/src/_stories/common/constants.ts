import { css } from '@emotion/css';
import type { BadgeProps } from '../..';

export const positions: BadgeProps['position'][] = ['topLeft', 'bottomLeft', 'topRight', 'bottomRight'];

export const popoverCss = css({
  flex: 'auto',
  boxSizing: 'border-box',
  minWidth: 0,
  minHeight: 0,
  overflow: 'auto',
  background: 'var(--bg-float)',
  borderRadius: '8px',
  padding: '12px 16px',
  textOverflow: 'ellipsis',
  border: '1px solid var(--line-border-card)',
  boxShadow: 'var(--shadow-s4-down)',
  fontSize: 14,
  lineHeight: '22px',
  maxWidth: 420,
});
