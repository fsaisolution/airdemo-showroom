import { defaultConfig } from './constants';
import { createStore } from '@universe-design/react-provider';
import type { ConfigProviderContextType } from './interface';

export const configStore = /* #__PURE__*/ createStore<ConfigProviderContextType>(defaultConfig);
