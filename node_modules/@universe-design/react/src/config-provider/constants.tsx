import type { ConfigProviderContextType } from './interface';
import enUS from '../shared/locales/en-US';
import { isClient } from '@universe-design/react-utilities';
import { defaultPopupPortalContainer } from '@universe-design/react-provider';

export const defaultConfig: ConfigProviderContextType = {
  rootPrefixCls: 'ud',
  locale: enUS,
  direction: 'ltr',
  getPopupContainer: defaultPopupPortalContainer,
  getTargetContainer: () => (isClient() ? window : ({} as HTMLElement)),
  toast: { top: 32 },
  theme: 'light',
  componentSize: 'medium',
};
