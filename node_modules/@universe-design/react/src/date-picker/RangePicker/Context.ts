import { createContextHook } from '@universe-design/react-utilities';
import type { RangePickerReturnType } from './useRangePicker';

export type RangePickerContextType = RangePickerReturnType;

export const { Provider: RangePickerContextProvider, useContext: useRangePickerContext } =
  createContextHook<RangePickerContextType>('RangePickerContext', {
    strict: true,
  });
