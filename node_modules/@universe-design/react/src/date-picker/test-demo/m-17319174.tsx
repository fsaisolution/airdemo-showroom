import * as React from 'react';
import { ConfigProvider, FloatingUIPopperSidecar, RangePicker } from '@universe-design/react';
import moment from 'moment';

export default () => (
  <ConfigProvider PopperSidecar={FloatingUIPopperSidecar}>
    <RangePicker defaultOpen defaultValue={[moment('2023-01-11'), moment('2023-02-11')]} />
  </ConfigProvider>
);
