// autogen by ../../../../scripts/generate-stories.ts

import type { StoryFn as Story } from '@storybook/react';
import CLittlePercentage from '../demo/littlePercentage';
if (!(CLittlePercentage as Story).storyName) {
  (CLittlePercentage as Story).storyName = 'Little percentage';
}

export default {
  title: 'Components/Progress/截图测试',
  tags: ['autodocs'],
};

export { CLittlePercentage };
