import stylex from '@universe-design/stylex';

export const sxCascadeBox = stylex.create({
  flexDirection: 'row',
  padding: 0,
  width: 'auto',
  overflow: 'hidden',
});

export const sxListBox = stylex.create({
  display: 'flex',
  flexDirection: 'column',
  boxSizing: 'border-box',
  padding: '2px 0',
});
