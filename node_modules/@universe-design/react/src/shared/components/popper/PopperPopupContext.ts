import type { Placement } from './types';
import { createContextHook } from '@universe-design/react-utilities';

export const { Provider: PopperPopupContextProvider, useContext: usePopperPopupContext } = createContextHook<{
  placement: Placement | null;
}>('PopperPopupContext', {
  strict: true,
});
