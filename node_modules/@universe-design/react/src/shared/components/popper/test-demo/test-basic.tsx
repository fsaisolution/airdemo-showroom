/* eslint-disable max-len */
import * as React from 'react';
import type { DemoPopoverProps } from '../test-utils/components';
import { Popover } from '../test-utils/components';

export default (props: DemoPopoverProps) => (
  <div className="container" style={{ overflowY: 'auto', height: '100vh' }}>
    <div style={{ height: 300 }} />
    <Popover trigger="click" {...props} />
    <div style={{ height: 1200 }} />
  </div>
);
