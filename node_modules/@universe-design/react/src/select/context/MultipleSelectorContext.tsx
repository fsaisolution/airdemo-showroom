import type * as React from 'react';
import { createContextHook } from '@universe-design/react-utilities';

const { Provider: MultipleSelectorContextProvider, useContext: useMultipleSelectorContext } = createContextHook<{
  maxHeight: React.CSSProperties['maxHeight'];
}>('MultipleSelectorContext', {
  strict: true,
});

export { MultipleSelectorContextProvider, useMultipleSelectorContext };
