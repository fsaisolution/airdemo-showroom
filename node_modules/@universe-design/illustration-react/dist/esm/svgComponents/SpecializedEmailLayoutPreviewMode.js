import * as React from 'react';
import SpecializedEmailLayoutPreviewModeLight from './SpecializedEmailLayoutPreviewModeLight.js';
import SpecializedEmailLayoutPreviewModeDark from './SpecializedEmailLayoutPreviewModeDark.js';
import { useIllustrationTheme } from '../ThemeProvider.js';

// autogen by ../../scripts/transform.ts
function SpecializedEmailLayoutPreviewMode(props) {
    const mode = useIllustrationTheme();
    if (mode === 'light') {
        return React.createElement(SpecializedEmailLayoutPreviewModeLight, { ...props });
    }
    else {
        return React.createElement(SpecializedEmailLayoutPreviewModeDark, { ...props });
    }
}

export { SpecializedEmailLayoutPreviewMode as default };
