import * as React from 'react';
import VcEmptyPositiveNoMeetingsLight from './VcEmptyPositiveNoMeetingsLight.js';
import VcEmptyPositiveNoMeetingsDark from './VcEmptyPositiveNoMeetingsDark.js';
import { useIllustrationTheme } from '../ThemeProvider.js';

// autogen by ../../scripts/transform.ts
function VcEmptyPositiveNoMeetings(props) {
    const mode = useIllustrationTheme();
    if (mode === 'light') {
        return React.createElement(VcEmptyPositiveNoMeetingsLight, { ...props });
    }
    else {
        return React.createElement(VcEmptyPositiveNoMeetingsDark, { ...props });
    }
}

export { VcEmptyPositiveNoMeetings as default };
