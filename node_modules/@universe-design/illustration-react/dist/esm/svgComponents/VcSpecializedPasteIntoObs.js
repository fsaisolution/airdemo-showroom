import * as React from 'react';
import VcSpecializedPasteIntoObsLight from './VcSpecializedPasteIntoObsLight.js';
import VcSpecializedPasteIntoObsDark from './VcSpecializedPasteIntoObsDark.js';
import { useIllustrationTheme } from '../ThemeProvider.js';

// autogen by ../../scripts/transform.ts
function VcSpecializedPasteIntoObs(props) {
    const mode = useIllustrationTheme();
    if (mode === 'light') {
        return React.createElement(VcSpecializedPasteIntoObsLight, { ...props });
    }
    else {
        return React.createElement(VcSpecializedPasteIntoObsDark, { ...props });
    }
}

export { VcSpecializedPasteIntoObs as default };
