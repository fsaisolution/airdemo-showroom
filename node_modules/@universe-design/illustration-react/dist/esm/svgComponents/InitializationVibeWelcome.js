import * as React from 'react';
import InitializationVibeWelcomeLight from './InitializationVibeWelcomeLight.js';
import InitializationVibeWelcomeDark from './InitializationVibeWelcomeDark.js';
import { useIllustrationTheme } from '../ThemeProvider.js';

// autogen by ../../scripts/transform.ts
function InitializationVibeWelcome(props) {
    const mode = useIllustrationTheme();
    if (mode === 'light') {
        return React.createElement(InitializationVibeWelcomeLight, { ...props });
    }
    else {
        return React.createElement(InitializationVibeWelcomeDark, { ...props });
    }
}

export { InitializationVibeWelcome as default };
