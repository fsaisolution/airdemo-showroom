import * as React from 'react';
import InitializationFunctionCalendarLight from './InitializationFunctionCalendarLight.js';
import InitializationFunctionCalendarDark from './InitializationFunctionCalendarDark.js';
import { useIllustrationTheme } from '../ThemeProvider.js';

// autogen by ../../scripts/transform.ts
function InitializationFunctionCalendar(props) {
    const mode = useIllustrationTheme();
    if (mode === 'light') {
        return React.createElement(InitializationFunctionCalendarLight, { ...props });
    }
    else {
        return React.createElement(InitializationFunctionCalendarDark, { ...props });
    }
}

export { InitializationFunctionCalendar as default };
