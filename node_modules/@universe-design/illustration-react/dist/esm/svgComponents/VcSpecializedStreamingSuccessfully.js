import * as React from 'react';
import VcSpecializedStreamingSuccessfullyLight from './VcSpecializedStreamingSuccessfullyLight.js';
import VcSpecializedStreamingSuccessfullyDark from './VcSpecializedStreamingSuccessfullyDark.js';
import { useIllustrationTheme } from '../ThemeProvider.js';

// autogen by ../../scripts/transform.ts
function VcSpecializedStreamingSuccessfully(props) {
    const mode = useIllustrationTheme();
    if (mode === 'light') {
        return React.createElement(VcSpecializedStreamingSuccessfullyLight, { ...props });
    }
    else {
        return React.createElement(VcSpecializedStreamingSuccessfullyDark, { ...props });
    }
}

export { VcSpecializedStreamingSuccessfully as default };
