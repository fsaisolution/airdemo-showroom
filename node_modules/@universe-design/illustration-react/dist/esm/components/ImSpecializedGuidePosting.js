import * as React from 'react';
import ImSpecializedGuidePostingLight from './ImSpecializedGuidePostingLight.js';
import ImSpecializedGuidePostingDark from './ImSpecializedGuidePostingDark.js';
import { useIllustrationTheme } from '../ThemeProvider.js';

// autogen by ../../scripts/transform.ts
function ImSpecializedGuidePosting(props) {
    const mode = useIllustrationTheme();
    if (mode === 'light') {
        return React.createElement(ImSpecializedGuidePostingLight, { ...props });
    }
    else {
        return React.createElement(ImSpecializedGuidePostingDark, { ...props });
    }
}

export { ImSpecializedGuidePosting as default };
