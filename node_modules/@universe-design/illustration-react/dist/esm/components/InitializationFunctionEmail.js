import * as React from 'react';
import InitializationFunctionEmailLight from './InitializationFunctionEmailLight.js';
import InitializationFunctionEmailDark from './InitializationFunctionEmailDark.js';
import { useIllustrationTheme } from '../ThemeProvider.js';

// autogen by ../../scripts/transform.ts
function InitializationFunctionEmail(props) {
    const mode = useIllustrationTheme();
    if (mode === 'light') {
        return React.createElement(InitializationFunctionEmailLight, { ...props });
    }
    else {
        return React.createElement(InitializationFunctionEmailDark, { ...props });
    }
}

export { InitializationFunctionEmail as default };
