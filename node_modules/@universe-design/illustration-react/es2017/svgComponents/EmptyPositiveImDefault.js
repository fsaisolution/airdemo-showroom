import * as React from 'react';
import EmptyPositiveImDefaultLight from './EmptyPositiveImDefaultLight.js';
import EmptyPositiveImDefaultDark from './EmptyPositiveImDefaultDark.js';
import { useIllustrationTheme } from '../ThemeProvider.js';

// autogen by ../../scripts/transform.ts
function EmptyPositiveImDefault(props) {
    const mode = useIllustrationTheme();
    if (mode === 'light') {
        return React.createElement(EmptyPositiveImDefaultLight, Object.assign({}, props));
    }
    else {
        return React.createElement(EmptyPositiveImDefaultDark, Object.assign({}, props));
    }
}

export { EmptyPositiveImDefault as default };
