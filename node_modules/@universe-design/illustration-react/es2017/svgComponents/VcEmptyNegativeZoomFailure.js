import * as React from 'react';
import VcEmptyNegativeZoomFailureLight from './VcEmptyNegativeZoomFailureLight.js';
import VcEmptyNegativeZoomFailureDark from './VcEmptyNegativeZoomFailureDark.js';
import { useIllustrationTheme } from '../ThemeProvider.js';

// autogen by ../../scripts/transform.ts
function VcEmptyNegativeZoomFailure(props) {
    const mode = useIllustrationTheme();
    if (mode === 'light') {
        return React.createElement(VcEmptyNegativeZoomFailureLight, Object.assign({}, props));
    }
    else {
        return React.createElement(VcEmptyNegativeZoomFailureDark, Object.assign({}, props));
    }
}

export { VcEmptyNegativeZoomFailure as default };
