import * as React from 'react';
import EmptyPositiveCommonDefaultLight from './EmptyPositiveCommonDefaultLight.js';
import EmptyPositiveCommonDefaultDark from './EmptyPositiveCommonDefaultDark.js';
import { useIllustrationTheme } from '../ThemeProvider.js';

// autogen by ../../scripts/transform.ts
function EmptyPositiveCommonDefault(props) {
    const mode = useIllustrationTheme();
    if (mode === 'light') {
        return React.createElement(EmptyPositiveCommonDefaultLight, Object.assign({}, props));
    }
    else {
        return React.createElement(EmptyPositiveCommonDefaultDark, Object.assign({}, props));
    }
}

export { EmptyPositiveCommonDefault as default };
