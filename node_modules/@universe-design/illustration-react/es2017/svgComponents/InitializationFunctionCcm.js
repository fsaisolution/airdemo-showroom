import * as React from 'react';
import InitializationFunctionCcmLight from './InitializationFunctionCcmLight.js';
import InitializationFunctionCcmDark from './InitializationFunctionCcmDark.js';
import { useIllustrationTheme } from '../ThemeProvider.js';

// autogen by ../../scripts/transform.ts
function InitializationFunctionCcm(props) {
    const mode = useIllustrationTheme();
    if (mode === 'light') {
        return React.createElement(InitializationFunctionCcmLight, Object.assign({}, props));
    }
    else {
        return React.createElement(InitializationFunctionCcmDark, Object.assign({}, props));
    }
}

export { InitializationFunctionCcm as default };
