import * as React from 'react';
import LoadingLight from './EmptyPositiveLoadingLight.js';
import LoadingLight$1 from './EmptyPositiveLoadingDark.js';
import { useIllustrationTheme } from '../../ThemeProvider.js';

function EmptyPositiveLoading(props) {
    const mode = useIllustrationTheme();
    if (mode === 'light') {
        return React.createElement(LoadingLight, Object.assign({}, props));
    }
    else {
        return React.createElement(LoadingLight$1, Object.assign({}, props));
    }
}

export { EmptyPositiveLoading as default };
