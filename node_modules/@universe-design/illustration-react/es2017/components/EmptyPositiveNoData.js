import * as React from 'react';
import EmptyPositiveNoDataLight from './EmptyPositiveNoDataLight.js';
import EmptyPositiveNoDataDark from './EmptyPositiveNoDataDark.js';
import { useIllustrationTheme } from '../ThemeProvider.js';

// autogen by ../../scripts/transform.ts
function EmptyPositiveNoData(props) {
    const mode = useIllustrationTheme();
    if (mode === 'light') {
        return React.createElement(EmptyPositiveNoDataLight, Object.assign({}, props));
    }
    else {
        return React.createElement(EmptyPositiveNoDataDark, Object.assign({}, props));
    }
}

export { EmptyPositiveNoData as default };
