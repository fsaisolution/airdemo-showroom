import * as React from 'react';
import VcEmptyPositiveInteractionLight from './VcEmptyPositiveInteractionLight.js';
import VcEmptyPositiveInteractionDark from './VcEmptyPositiveInteractionDark.js';
import { useIllustrationTheme } from '../ThemeProvider.js';

// autogen by ../../scripts/transform.ts
function VcEmptyPositiveInteraction(props) {
    const mode = useIllustrationTheme();
    if (mode === 'light') {
        return React.createElement(VcEmptyPositiveInteractionLight, Object.assign({}, props));
    }
    else {
        return React.createElement(VcEmptyPositiveInteractionDark, Object.assign({}, props));
    }
}

export { VcEmptyPositiveInteraction as default };
