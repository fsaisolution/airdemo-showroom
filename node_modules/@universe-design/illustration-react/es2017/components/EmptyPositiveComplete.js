import * as React from 'react';
import EmptyPositiveCompleteLight from './EmptyPositiveCompleteLight.js';
import EmptyPositiveCompleteDark from './EmptyPositiveCompleteDark.js';
import { useIllustrationTheme } from '../ThemeProvider.js';

// autogen by ../../scripts/transform.ts
function EmptyPositiveComplete(props) {
    const mode = useIllustrationTheme();
    if (mode === 'light') {
        return React.createElement(EmptyPositiveCompleteLight, Object.assign({}, props));
    }
    else {
        return React.createElement(EmptyPositiveCompleteDark, Object.assign({}, props));
    }
}

export { EmptyPositiveComplete as default };
