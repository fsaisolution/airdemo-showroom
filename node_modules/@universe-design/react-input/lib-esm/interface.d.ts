/**
 * Input 和 TextArea 计数与最大长度相关属性。
 * @public
 */
export interface InputCounterProps {
    /**
     * 当仅使用此属性时，会使用原生的 maxLength 属性
     */
    maxLength?: number;
    /**
     *  超出长度之后，是否截断
     *  @defaultValue false
     */
    truncateExcessWords?: boolean;
    /**
     * 自定义如何截断，与 counter 搭配使用，当需要截断时，会调用此方法
     */
    slicer?: (str: string, length: number) => string;
    /**
     * 是否展示计数器。当设置时，会使用 [UD的计数规则](https://bytedance.feishu.cn/docx/doxcnox8gbqOZ5zZ1U2kg27inOT)
     * @defaultValue false
     */
    showCount?: boolean;
    /**
     * 自定义计数规则
     */
    counter?: (str: string) => number;
}
/**
 * Input 和 TextArea 值变更回调函数
 * @public
 */
export type ChangeHandler<T extends HTMLTextAreaElement | HTMLInputElement> = (ref: T, event: React.ChangeEvent<T> | React.CompositionEvent<T> | React.MouseEvent<HTMLSpanElement, MouseEvent>, value: string, eventType: 'inputChange' | 'composition' | 'clear') => void;
//# sourceMappingURL=interface.d.ts.map