/**
 * 最大长度的问题
 * 1. 默认使用按照码点计数的规则
 * 2. 支持用户传入自定义长度计算方法
 * 2. 支持用户传入自定义截断方法
 */
import type { InputCounterProps } from '../interface';
export declare function useMaxLength(args: InputCounterProps): {
    valueFormatter: (str?: string) => string;
    nativeMaxLength: number | undefined;
    counter: (str: string) => number;
    maxLength: number | undefined;
};
//# sourceMappingURL=useMaxLength.d.ts.map