import { useEffectOnce } from '@universe-design/react-utilities';
import { useEffect } from 'react';

/**
 * 监听Input 和 TextArea 焦点变更
 * @public
 */
export const useFocus = _ref => {
  let {
    inputRef,
    onFocusChange,
    autoFocus
  } = _ref;
  // TODO 补充一下这样做的原因
  useEffect(() => {
    const input = inputRef.current;
    if (!input) {
      return;
    }
    const onBlur = function (e) {
      onFocusChange === null || onFocusChange === void 0 ? void 0 : onFocusChange(false, e);
    };
    const onFocus = function (e) {
      onFocusChange === null || onFocusChange === void 0 ? void 0 : onFocusChange(true, e);
    };
    input.addEventListener('focus', onFocus);
    input.addEventListener('blur', onBlur);
    return () => {
      input.removeEventListener('focus', onFocus);
      input.removeEventListener('blur', onBlur);
    };
  }, [inputRef, onFocusChange]);

  /**
   * 为了给 autoFocus 的input 加上高亮边，并保证有且只有一个高亮
   * 注意： autoFocus 属性不能给到 input 上面, 否则不会触发 onFocusChange
   */
  useEffectOnce(() => {
    if (autoFocus) {
      var _inputRef$current;
      (_inputRef$current = inputRef.current) === null || _inputRef$current === void 0 ? void 0 : _inputRef$current.focus();
    }
  });
};
//# sourceMappingURL=useFocus.js.map
