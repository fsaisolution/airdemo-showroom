import React from 'react';
import createCache from '@emotion/cache';
const defaultEmotionCache = createCache({
  key: 'universe-design'
});
const UDEmotionCacheContext = /*#__PURE__*/React.createContext(defaultEmotionCache);
/**
 * @public
 */
export const UDStylesheetContainerProvider = props => {
  const {
    container,
    children
  } = props;
  const outerOrDefault = React.useContext(UDEmotionCacheContext);
  const cache = React.useMemo(() => {
    if (container) {
      return createCache({
        key: 'universe-design-custom-container',
        container
      });
    }
    return outerOrDefault;
  }, [container, outerOrDefault]);
  return /*#__PURE__*/React.createElement(UDEmotionCacheContext.Provider, {
    value: cache
  }, children);
};

/**
 * @public
 */
export const useEmotionCache = () => React.useContext(UDEmotionCacheContext);
//# sourceMappingURL=UDStyleSheetContainer.js.map
