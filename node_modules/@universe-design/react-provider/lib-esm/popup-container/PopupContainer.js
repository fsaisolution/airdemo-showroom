import React from 'react';
import { isClient } from '../isClient';

/**
 * @public
 * 弹出框（下拉选择，文字提示，菜单等等）渲染父节点，默认渲染到 `body` 上。
 * @defaultValue `() => document.body`
 */

/**
 * @public
 * @privateRemarks TODO 其实这个地方不应该判断 isClient 的，但是 rc-table 里面有个在 render 里直接调用 getTargetContainer 的逻辑导致这边在 ssr 的时候有问题
 */
export const defaultPopupPortalContainer = () =>
// eslint-disable-next-line @typescript-eslint/consistent-type-assertions
isClient() ? document.body : {};

/**
 * @public
 */
export const PopupPortalContainerContext = /*#__PURE__*/React.createContext(null);

/**
 * @public
 */
export const PopupPortalContainerProvider = PopupPortalContainerContext.Provider;
/**
 * @public
 */
export const usePopupPortalContainer = () => {
  var _React$useContext;
  return (_React$useContext = React.useContext(PopupPortalContainerContext)) !== null && _React$useContext !== void 0 ? _React$useContext : defaultPopupPortalContainer;
};
//# sourceMappingURL=PopupContainer.js.map
