import { createContextHook } from './createContextHook';
const {
  Provider,
  useContext
} = /* #__PURE__*/createContextHook('DomainEventTargetContext', {
  defaultValue: null,
  strict: false
});

/**
 * `useDomainEventTargetContext` is a hook to get the nearest parent event target. You can dispatch events using the event target returned.
 *
 * @alpha
 */
const useDomainEventTargetContext = useContext;

/**
 * `DomainEventTargetContextProvider` is a provider to provide an event target to children.
 *
 * @alpha
 */
const DomainEventTargetContextProvider = Provider;
export { DomainEventTargetContextProvider, useDomainEventTargetContext };
//# sourceMappingURL=domain-events-context.js.map
