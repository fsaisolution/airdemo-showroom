import React from 'react';
import { render, act } from '@testing-library/react';
import { createGlobalConfig } from './createGlobalConfig';
describe('createGlobalStore', () => {
  test('setGlobal', () => {
    const {
      useConfig,
      setGlobal
    } = createGlobalConfig('test', 'a');
    const onRendered = jest.fn();
    const wrapper = render(/*#__PURE__*/React.createElement(_ref => {
      let {
        onRendered
      } = _ref;
      const config = useConfig();
      onRendered(config);
      return /*#__PURE__*/React.createElement(React.Fragment, null, config);
    }, {
      onRendered: onRendered
    }));
    expect(onRendered).toHaveBeenCalledTimes(1);
    expect(onRendered).toHaveBeenLastCalledWith('a');
    act(() => {
      setGlobal('a');
    });
    expect(onRendered).toHaveBeenCalledTimes(1);
    expect(onRendered).toHaveBeenLastCalledWith('a');
    act(() => {
      setGlobal('b');
    });
    expect(onRendered).toHaveBeenCalledTimes(2);
    expect(onRendered).toHaveBeenLastCalledWith('b');
    wrapper.unmount();
  });
  test('GlobalConfigProvider', () => {
    const {
      useConfig,
      GlobalConfigProvider
    } = createGlobalConfig('test', 'a');
    const onRendered = jest.fn();
    const wrapper = render(/*#__PURE__*/React.createElement(_ref2 => {
      let {
        onRendered
      } = _ref2;
      const config = useConfig();
      onRendered(config);
      return /*#__PURE__*/React.createElement(React.Fragment, null, config);
    }, {
      onRendered: onRendered
    }));
    expect(onRendered).toHaveBeenCalledTimes(1);
    expect(onRendered).toHaveBeenLastCalledWith('a');
    const wrapper2 = render(/*#__PURE__*/React.createElement(GlobalConfigProvider, {
      value: "a"
    }));
    expect(onRendered).toHaveBeenCalledTimes(1);
    expect(onRendered).toHaveBeenLastCalledWith('a');
    wrapper2.rerender(/*#__PURE__*/React.createElement(GlobalConfigProvider, {
      value: "b"
    }));
    expect(onRendered).toHaveBeenCalledTimes(2);
    expect(onRendered).toHaveBeenLastCalledWith('b');
    wrapper.unmount();
    wrapper2.unmount();
  });
  test('ConfigProvider', () => {
    const {
      useConfig,
      ConfigProvider
    } = createGlobalConfig('test', 'a');
    const Component = _ref3 => {
      let {
        onRendered
      } = _ref3;
      const config = useConfig();
      onRendered(config);
      return /*#__PURE__*/React.createElement(React.Fragment, null, config);
    };
    const onRendered = jest.fn();
    const wrapper = render(/*#__PURE__*/React.createElement(ConfigProvider, {
      value: "a"
    }, /*#__PURE__*/React.createElement(Component, {
      onRendered: onRendered
    })));
    expect(onRendered).toHaveBeenCalledTimes(1);
    expect(onRendered).toHaveBeenLastCalledWith('a');
    wrapper.rerender(/*#__PURE__*/React.createElement(ConfigProvider, {
      value: "b"
    }, /*#__PURE__*/React.createElement(Component, {
      onRendered: onRendered
    })));
    expect(onRendered).toHaveBeenCalledTimes(2);
    expect(onRendered).toHaveBeenLastCalledWith('b');
    wrapper.unmount();
  });
  test('ContextValue > GlobalValue', () => {
    const {
      useConfig,
      ConfigProvider,
      setGlobal
    } = createGlobalConfig('test', 'a');
    const onRendered = jest.fn();
    const wrapper = render(/*#__PURE__*/React.createElement(ConfigProvider, {
      value: "a"
    }, /*#__PURE__*/React.createElement(_ref4 => {
      let {
        onRendered
      } = _ref4;
      const config = useConfig();
      onRendered(config);
      return /*#__PURE__*/React.createElement(React.Fragment, null, config);
    }, {
      onRendered: onRendered
    })));
    expect(onRendered).toHaveBeenCalledTimes(1);
    expect(onRendered).toHaveBeenLastCalledWith('a');
    act(() => {
      setGlobal('b');
    });
    expect(onRendered).toHaveBeenCalledTimes(1);
    expect(onRendered).toHaveBeenLastCalledWith('a');
    wrapper.unmount();
  });
  test('PropValue > ContextValue', () => {
    const {
      useConfig,
      ConfigProvider
    } = createGlobalConfig('test', 'a');
    const onRendered = jest.fn();
    const componentNode = /*#__PURE__*/React.createElement(_ref5 => {
      let {
        config: configProp,
        onRendered
      } = _ref5;
      const config = useConfig(configProp);
      onRendered(config);
      return /*#__PURE__*/React.createElement(React.Fragment, null, config);
    }, {
      config: "b",
      onRendered: onRendered
    });
    const wrapper = render(/*#__PURE__*/React.createElement(ConfigProvider, {
      value: "a"
    }, componentNode));
    expect(onRendered).toHaveBeenCalledTimes(1);
    expect(onRendered).toHaveBeenLastCalledWith('b');
    wrapper.rerender(/*#__PURE__*/React.createElement(ConfigProvider, {
      value: "c"
    }, componentNode));
    // 这里再次 render 是因为 contextValue 变了，但是实际上 render 值还是用的 propValue
    expect(onRendered).toHaveBeenCalledTimes(2);
    expect(onRendered).toHaveBeenLastCalledWith('b');
    wrapper.unmount();
  });
});
//# sourceMappingURL=createGlobalStore.test.js.map
