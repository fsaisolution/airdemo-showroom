"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useLocaleName = exports.setGlobalLocaleName = exports.getGlobalLocaleName = exports.LocaleNameProvider = exports.LOCALE_NAMES = void 0;
var _createGlobalConfig = require("./createGlobalConfig");
/**
 * 所有支持的区域名称数组（按字母顺序排列）
 *
 * @public
 */
const LOCALE_NAMES = exports.LOCALE_NAMES = ['de-DE', 'en-US', 'es-ES', 'fr-FR',
// 'hi-IN', 后续不再支持印地语了
'id-ID', 'it-IT', 'ja-JP', 'ko-KR', 'pt-BR', 'ru-RU', 'th-TH', 'vi-VN', 'zh-CN', 'zh-HK', 'zh-TW', 'ms-MY'];

/**
 * 所有区域名称的 literal union
 *
 * @public
 */

const {
  ConfigProvider,
  useConfig,
  getGlobal,
  setGlobal
} = (0, _createGlobalConfig.createGlobalConfig)('locale-name', 'en-US');

/**
 * 提供应用本地化名称的上下文配置
 *
 * @remarks
 * 该组件用于包裹需要本地化名称支持的组件树，
 * 可通过`value`属性设置当前区域名称
 *
 * @public
 */
const LocaleNameProvider = exports.LocaleNameProvider = ConfigProvider;

/**
 * 获取当前区域名称的 react hook
 *
 * @returns 返回当前区域名称
 * @example
 * const locale = useLocaleName();
 *
 * @public
 */
const useLocaleName = exports.useLocaleName = useConfig;

/**
 * 设置全局区域名称
 *
 * @param locale - 要设置的全局区域名称
 * @remarks
 * 该设置会影响所有未使用 `LocaleNameProvider` 包裹的子元素
 *
 * @public
 */
const setGlobalLocaleName = exports.setGlobalLocaleName = setGlobal;

/**
 * 获取全局区域名称
 *
 * @public
 */
const getGlobalLocaleName = exports.getGlobalLocaleName = getGlobal;
//# sourceMappingURL=locale_name.js.map
