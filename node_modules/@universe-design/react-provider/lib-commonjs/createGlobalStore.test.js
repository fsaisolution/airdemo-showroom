"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault").default;
var _react = _interopRequireDefault(require("react"));
var _react2 = require("@testing-library/react");
var _createGlobalConfig = require("./createGlobalConfig");
describe('createGlobalStore', () => {
  test('setGlobal', () => {
    const {
      useConfig,
      setGlobal
    } = (0, _createGlobalConfig.createGlobalConfig)('test', 'a');
    const onRendered = jest.fn();
    const wrapper = (0, _react2.render)(/*#__PURE__*/_react.default.createElement(_ref => {
      let {
        onRendered
      } = _ref;
      const config = useConfig();
      onRendered(config);
      return /*#__PURE__*/_react.default.createElement(_react.default.Fragment, null, config);
    }, {
      onRendered: onRendered
    }));
    expect(onRendered).toHaveBeenCalledTimes(1);
    expect(onRendered).toHaveBeenLastCalledWith('a');
    (0, _react2.act)(() => {
      setGlobal('a');
    });
    expect(onRendered).toHaveBeenCalledTimes(1);
    expect(onRendered).toHaveBeenLastCalledWith('a');
    (0, _react2.act)(() => {
      setGlobal('b');
    });
    expect(onRendered).toHaveBeenCalledTimes(2);
    expect(onRendered).toHaveBeenLastCalledWith('b');
    wrapper.unmount();
  });
  test('GlobalConfigProvider', () => {
    const {
      useConfig,
      GlobalConfigProvider
    } = (0, _createGlobalConfig.createGlobalConfig)('test', 'a');
    const onRendered = jest.fn();
    const wrapper = (0, _react2.render)(/*#__PURE__*/_react.default.createElement(_ref2 => {
      let {
        onRendered
      } = _ref2;
      const config = useConfig();
      onRendered(config);
      return /*#__PURE__*/_react.default.createElement(_react.default.Fragment, null, config);
    }, {
      onRendered: onRendered
    }));
    expect(onRendered).toHaveBeenCalledTimes(1);
    expect(onRendered).toHaveBeenLastCalledWith('a');
    const wrapper2 = (0, _react2.render)(/*#__PURE__*/_react.default.createElement(GlobalConfigProvider, {
      value: "a"
    }));
    expect(onRendered).toHaveBeenCalledTimes(1);
    expect(onRendered).toHaveBeenLastCalledWith('a');
    wrapper2.rerender(/*#__PURE__*/_react.default.createElement(GlobalConfigProvider, {
      value: "b"
    }));
    expect(onRendered).toHaveBeenCalledTimes(2);
    expect(onRendered).toHaveBeenLastCalledWith('b');
    wrapper.unmount();
    wrapper2.unmount();
  });
  test('ConfigProvider', () => {
    const {
      useConfig,
      ConfigProvider
    } = (0, _createGlobalConfig.createGlobalConfig)('test', 'a');
    const Component = _ref3 => {
      let {
        onRendered
      } = _ref3;
      const config = useConfig();
      onRendered(config);
      return /*#__PURE__*/_react.default.createElement(_react.default.Fragment, null, config);
    };
    const onRendered = jest.fn();
    const wrapper = (0, _react2.render)(/*#__PURE__*/_react.default.createElement(ConfigProvider, {
      value: "a"
    }, /*#__PURE__*/_react.default.createElement(Component, {
      onRendered: onRendered
    })));
    expect(onRendered).toHaveBeenCalledTimes(1);
    expect(onRendered).toHaveBeenLastCalledWith('a');
    wrapper.rerender(/*#__PURE__*/_react.default.createElement(ConfigProvider, {
      value: "b"
    }, /*#__PURE__*/_react.default.createElement(Component, {
      onRendered: onRendered
    })));
    expect(onRendered).toHaveBeenCalledTimes(2);
    expect(onRendered).toHaveBeenLastCalledWith('b');
    wrapper.unmount();
  });
  test('ContextValue > GlobalValue', () => {
    const {
      useConfig,
      ConfigProvider,
      setGlobal
    } = (0, _createGlobalConfig.createGlobalConfig)('test', 'a');
    const onRendered = jest.fn();
    const wrapper = (0, _react2.render)(/*#__PURE__*/_react.default.createElement(ConfigProvider, {
      value: "a"
    }, /*#__PURE__*/_react.default.createElement(_ref4 => {
      let {
        onRendered
      } = _ref4;
      const config = useConfig();
      onRendered(config);
      return /*#__PURE__*/_react.default.createElement(_react.default.Fragment, null, config);
    }, {
      onRendered: onRendered
    })));
    expect(onRendered).toHaveBeenCalledTimes(1);
    expect(onRendered).toHaveBeenLastCalledWith('a');
    (0, _react2.act)(() => {
      setGlobal('b');
    });
    expect(onRendered).toHaveBeenCalledTimes(1);
    expect(onRendered).toHaveBeenLastCalledWith('a');
    wrapper.unmount();
  });
  test('PropValue > ContextValue', () => {
    const {
      useConfig,
      ConfigProvider
    } = (0, _createGlobalConfig.createGlobalConfig)('test', 'a');
    const onRendered = jest.fn();
    const componentNode = /*#__PURE__*/_react.default.createElement(_ref5 => {
      let {
        config: configProp,
        onRendered
      } = _ref5;
      const config = useConfig(configProp);
      onRendered(config);
      return /*#__PURE__*/_react.default.createElement(_react.default.Fragment, null, config);
    }, {
      config: "b",
      onRendered: onRendered
    });
    const wrapper = (0, _react2.render)(/*#__PURE__*/_react.default.createElement(ConfigProvider, {
      value: "a"
    }, componentNode));
    expect(onRendered).toHaveBeenCalledTimes(1);
    expect(onRendered).toHaveBeenLastCalledWith('b');
    wrapper.rerender(/*#__PURE__*/_react.default.createElement(ConfigProvider, {
      value: "c"
    }, componentNode));
    // 这里再次 render 是因为 contextValue 变了，但是实际上 render 值还是用的 propValue
    expect(onRendered).toHaveBeenCalledTimes(2);
    expect(onRendered).toHaveBeenLastCalledWith('b');
    wrapper.unmount();
  });
});
//# sourceMappingURL=createGlobalStore.test.js.map
