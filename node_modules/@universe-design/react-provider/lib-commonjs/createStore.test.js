"use strict";

var _createStore = require("./createStore");
describe('createStore', () => {
  test('basic', () => {
    const store = (0, _createStore.createStore)('a');
    const cb = jest.fn();
    const disposer = store.subscribe(cb);
    expect(cb).toHaveBeenCalledTimes(0);
    store.set('b');
    expect(store.get()).toBe('b');
    expect(cb).toHaveBeenCalledTimes(1);
    expect(cb).toHaveBeenLastCalledWith('b');
    disposer();
    store.set('c');
    expect(store.get()).toBe('c');
    expect(cb).toHaveBeenCalledTimes(1);
    expect(cb).toHaveBeenLastCalledWith('b');
  });
  test('same callback will be subscribed once', () => {
    const store = (0, _createStore.createStore)('a');
    const cb = jest.fn();
    const disposer1 = store.subscribe(cb);
    const disposer2 = store.subscribe(cb);
    store.set('b');
    expect(cb).toHaveBeenCalledTimes(1);
    expect(cb).toHaveBeenLastCalledWith('b');
    disposer2();
    store.set('c');
    expect(cb).toHaveBeenCalledTimes(2);
    expect(cb).toHaveBeenLastCalledWith('c');
    disposer1();
    store.set('d');
    expect(cb).toHaveBeenCalledTimes(2);
    expect(cb).toHaveBeenLastCalledWith('c');
  });
  test('equal value will not call callback', () => {
    const store = (0, _createStore.createStore)('a');
    const cb = jest.fn();
    const disposer = store.subscribe(cb);
    store.set('a');
    expect(cb).toHaveBeenCalledTimes(0);
    disposer();
  });
});
//# sourceMappingURL=createStore.test.js.map
