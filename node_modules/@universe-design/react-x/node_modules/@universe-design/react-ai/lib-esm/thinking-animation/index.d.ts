import * as React from 'react';
/**
 * @public
 *
 * AI 默认思考动画组件属性
 */
export interface AIThinkAnimationProps extends React.ComponentPropsWithoutRef<'div'> {
    /**
     * @public
     *
     * 是否正在 thinking (动画执行)
     */
    thinking: boolean;
}
/**
 * @public
 *
 * AI 默认思考动画组件
 */
export declare const AIThinkingAnimation: (props: AIThinkAnimationProps) => React.JSX.Element;
//# sourceMappingURL=index.d.ts.map