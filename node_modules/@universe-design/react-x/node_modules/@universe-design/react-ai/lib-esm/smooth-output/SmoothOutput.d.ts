export type OutputCallback<T> = (str: T[]) => void;
/**
 * @public
 */
export interface Observer<T> {
    /**
     * 向外输出最新数据的回调
     */
    next?: (value: T) => void;
    /**
     * 输出完毕的事件回调
     */
    complete?: () => void;
}
/**
 * @public
 * 类 `SmoothOutput` 的类型
 */
export interface ISmoothOutput<T = string> {
    /**
     * 向 smoother 传递数据
     */
    next: (data: T[]) => void;
    /**
     * 通知 smoother 数据已经输入完毕
     */
    complete: () => void;
    /**
     * 订阅最新输出数据, 并返回 unsubscribe 方法
     */
    subscribe: (observer: Observer<T[]>) => () => void;
    /**
     * 取消所有对输出的订阅，并且不再接收新的数据
     */
    close: () => void;
}
/**
 * @public
 * 流式输入
 */
export declare class SmoothOutput<T = string> implements ISmoothOutput<T> {
    private _cachedData;
    private _outputData;
    private _outputIndex;
    private _pipeTimer?;
    private _timeOfReceivedFirstData?;
    private _speed;
    private _subject;
    private _receivedCompleted;
    constructor();
    complete: () => void;
    getSnapshot: () => T[];
    next: (data: T[]) => void;
    close: () => void;
    subscribe: (observer: Observer<T[]>) => () => void;
    /**
     * 计算输出速率算法
     **/
    private pipeToLastToken;
}
//# sourceMappingURL=SmoothOutput.d.ts.map