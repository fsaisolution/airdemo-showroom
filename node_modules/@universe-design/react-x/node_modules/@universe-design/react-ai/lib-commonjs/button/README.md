# AI 按钮 AI Button

AI 按钮组件。

## 类型

AIButton 的类型分为 2 种： `outlined` 和 `filled`，默认为 `outlined`。
<code src="./demo/type.tsx">

## 颜色

AIButton 的颜色分为 2 种： `default` 和 `primary`，默认为 `default`。
<code src="./demo/color.tsx">

## 其他属性

AIButton 的其他属性。
<code src="./demo/all.tsx">

## API

#include(react-ai.aibuttonprops.md)
