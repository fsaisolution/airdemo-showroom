import { render } from '@testing-library/react';
import React from 'react';
import { AuroraBlock } from '../index';
describe('Test suites', () => {
  test('basic usage', () => {
    const renderResult = render(/*#__PURE__*/React.createElement(AuroraBlock, {
      noAnimation: true
    }));
    expect(renderResult.baseElement).toMatchSnapshot();
  });
  test('customize fill color', () => {
    const renderResult = render(/*#__PURE__*/React.createElement(AuroraBlock, {
      mainEllipse: {
        color: 'green'
      },
      reflectEllipse: {
        color: 'blue'
      },
      subEllipse: {
        color: 'red'
      },
      noAnimation: true
    }));
    expect(renderResult.baseElement).toMatchSnapshot();
  });
});
//# sourceMappingURL=index.test.js.map
