import * as React from 'react';
import type { GetRowKey, Key, GetComponentProps, GroupConfig } from '../interface';
export interface BodyProps<RecordType> {
    data: readonly RecordType[];
    getRowKey: GetRowKey<RecordType>;
    measureColumnWidth: boolean;
    expandedKeys: Set<Key>;
    onRow: GetComponentProps<RecordType>;
    rowExpandable: (record: RecordType) => boolean;
    emptyNode: React.ReactNode;
    childrenColumnName: string;
    groupsMap: Map<Key, Key[]>;
    groupRowRenderer: GroupConfig<RecordType>['groupRowRenderer'];
    expandedGroupKeySet: Set<Key>;
    triggerExpandGroup: (groupKey: Key) => void;
    onGroupRow: GroupConfig<RecordType>['onGroupRow'];
    getRecordByKey: (key: Key) => RecordType;
}
declare function Body<RecordType>({ data, getRowKey, measureColumnWidth, expandedKeys, onRow, rowExpandable, emptyNode, childrenColumnName, groupsMap, groupRowRenderer, expandedGroupKeySet, triggerExpandGroup, onGroupRow, getRecordByKey, }: BodyProps<RecordType>): JSX.Element;
declare const MemoBody: React.MemoExoticComponent<typeof Body>;
export default MemoBody;
