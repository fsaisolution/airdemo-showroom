import type { GetComponent } from '../interface';
import type { FixedInfo } from '../utils/fixUtil';
export interface TableContextProps {
    prefixCls: string;
    getComponent: GetComponent;
    scrollbarColumnWidth: number;
    direction: 'ltr' | 'rtl';
    fixedInfoList: readonly FixedInfo[];
    isSticky: boolean;
    /**
     * 是否左侧固定
     */
    pingedLeft: boolean;
    /**
     * 是否右侧固定
     */
    pingedRight: boolean;
}
declare const TableContext: import("use-context-selector").Context<TableContextProps>;
export default TableContext;
export declare const useTableContextSelector: <Selected>(selector: (value: TableContextProps) => Selected) => Selected;
