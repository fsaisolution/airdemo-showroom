import type { Key } from 'react';
import type { GetRowKey, GroupConfig } from '../interface';
export default function useGroups<RecordType>({ groupBy, dataSource, getRowKey, defaultExpandedGroupKeys, defaultExpandAllGroups, }: {
    groupBy: GroupConfig<RecordType>['groupBy'];
    dataSource: readonly RecordType[];
    getRowKey: GetRowKey<RecordType>;
    defaultExpandedGroupKeys: GroupConfig<RecordType>['defaultExpandedGroupKeys'];
    defaultExpandAllGroups: GroupConfig<RecordType>['defaultExpandAllGroups'];
}): {
    groupsMap: Map<Key, Key[]>;
    triggerExpandGroup: (groupKey: Key) => void;
    expandedGroupKeySet: Set<Key>;
};
