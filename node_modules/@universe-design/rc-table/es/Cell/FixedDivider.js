import { useTableContextSelector } from '../context/TableContext';
import * as React from 'react';
import classNames from 'classnames';
export var CellFixedDivider = function CellFixedDivider(_ref) {
  var position = _ref.position;
  var prefixCls = useTableContextSelector(function (ctx) {
    return ctx.prefixCls;
  });
  var tablePingedLeft = useTableContextSelector(function (ctx) {
    return ctx.pingedLeft;
  });
  var tablePingedRight = useTableContextSelector(function (ctx) {
    return ctx.pingedRight;
  });
  var visible = tablePingedLeft && position === 'left' || tablePingedRight && position === 'right';
  return /*#__PURE__*/React.createElement("div", {
    className: classNames(["".concat(prefixCls, "__divider"), "".concat(prefixCls, "__divider--").concat(position), visible && "".concat(prefixCls, "__divider--visible")])
  });
};