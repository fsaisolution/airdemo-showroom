export declare abstract class BaseLogger {
    abstract debug(input: string): void;
    abstract info(input: string): void;
    abstract warn(input: string): void;
    abstract error(input: string): void;
    abstract trace(input: string): void;
}
/**
 * 工厂函数，用于创建一个带 logId 的 BytedServiceLogger 实例
 * @param logId 日志 ID
 * @returns BaseLogger 的实例
 */
export declare const generateBytedServiceLoggerWithLogId: ({ logId, }: {
    logId: string;
}) => BaseLogger;
/**
 * 工厂函数，用于创建一个 ConsoleErrorLogger 实例
 * @returns BaseLogger 的实例
 */
export declare const generateConsoleErrorLogger: () => BaseLogger;
//# sourceMappingURL=logger.d.ts.map