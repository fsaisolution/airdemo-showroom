import { AfterCallHook, BeforeCallHook, BytedMcpClientContext, HttpConnection, Tool } from './types.js';
export declare class BytedMcpClient {
    private servers;
    private connections;
    constructor(connections: Record<string, HttpConnection>);
    private validateConnections;
    private connectServer;
    loadAllTools: ({ ctx, }: {
        ctx: BytedMcpClientContext;
    }) => Promise<Tool[]>;
    listLoadedTools({ ctx }: {
        ctx: BytedMcpClientContext;
    }): Tool[];
    callTool(toolName: string, args: unknown): Promise<unknown>;
    registerBeforeCallHook({ serverName, beforeCallHook, ctx, }: {
        serverName: string;
        beforeCallHook: BeforeCallHook;
        ctx: BytedMcpClientContext;
    }): void;
    clearBeforeCallHook({ serverName, ctx, }: {
        serverName: string;
        ctx: BytedMcpClientContext;
    }): void;
    registerAfterCallHook({ serverName, afterCallHook, ctx, }: {
        serverName: string;
        afterCallHook: AfterCallHook;
        ctx: BytedMcpClientContext;
    }): void;
    private findTool;
    connectAll({ ctx, }: {
        ctx: BytedMcpClientContext;
    }): Promise<void>;
    close({ ctx }: {
        ctx: BytedMcpClientContext;
    }): void;
}
//# sourceMappingURL=mcpClient.d.ts.map