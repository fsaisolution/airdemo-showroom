import { RequestOptions, IncomingHttpHeaders } from 'http';
export declare const createRequest: (address: string) => (path: string, options: RequestOptions) => Promise<{
    statusCode: number;
    body: string;
    headers: IncomingHttpHeaders;
}>;
