import { LogAgent } from './logagent';
import { Message, ILogger, LogAgentOptionsType } from './typings';
export declare const sendViaHTTPProtocol: (host: string, port: number) => (this: LogAgent, messages: Message[]) => Promise<void>;
export declare const sendViaUnixDomainSocket: (socketPath: string, logger: ILogger) => (this: LogAgent, messages: Message[]) => Promise<void>;
export declare const getGateway: () => string;
/**
 * getMessageSender()返回发送日志的方法。
 *
 * 如果当前环境支持logagent的unix domain socket，则logagent会优先利用这种方式发送数据；否则
 * 会通过http发送数据。
 */
export declare function getMessageSender(options: LogAgentOptionsType & {
    logger: ILogger;
}): (this: LogAgent, messages: Message[]) => Promise<void>;
export declare const truncate: (data: Uint8Array, len: number) => Uint8Array;
