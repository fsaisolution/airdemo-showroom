/**
 * message RequestPayload {
 *   message Tags {
 *     string key = 1;
 *     string value = 2;
 *   }
 *
 *   bytes msg = 1;
 *   repeated Tags tags = 2;
 * }
 */
/**
 * 目前先手写实现，后续再考虑根据情况切换为生成代码
 */
export declare const encode: ({ msg, tags, }: {
    msg: Uint8Array;
    tags: Array<{
        key: string;
        value: string;
    }>;
}) => Uint8Array;
/**
 * 目前先手写实现，后续再考虑根据情况切换为生成代码
 */
export declare const decode: (data: Uint8Array) => {
    msg: Uint8Array;
    tags: {
        key: string;
        value: string;
    }[];
};
export declare const quickEncode: (msg: Uint8Array, tags: Array<{
    key: string;
    value: string;
}>) => Uint8Array;
