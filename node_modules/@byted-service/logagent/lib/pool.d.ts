import { TCPPool, DynamicPool } from '@byted-service/netpool';
import { ILogger } from './typings';
export declare const idCreator: (suffix?: string | undefined) => string;
export interface PoolOptions {
    socketPath: string;
    logger: ILogger;
}
export declare const poolCache: {
    cache: Map<string, Map<ILogger, TCPPool>>;
    get(options: PoolOptions): TCPPool;
    values(): TCPPool[];
    delete(pool: TCPPool): void;
};
export declare const pool: DynamicPool<TCPPool, import("@byted-service/netpool").TCPSocket, PoolOptions>;
