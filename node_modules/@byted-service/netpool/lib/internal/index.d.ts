import { Pool, PoolOptions as InternelPoolOptions, Stage, Operation, TimeoutOptions } from './pool';
export * from './errors';
export { Stage, Operation, TimeoutOptions };
export interface DebugOptions {
    idPrefix?: string;
    acquire?: boolean;
    release?: boolean;
    create?: boolean;
    use?: boolean;
    destroy?: boolean;
    reaping?: boolean;
    poolDestroy?: boolean;
    runningStatsInterval?: number;
}
export interface Resource {
    id?: string;
    destroyReason?: string;
}
export interface PoolOptions<T extends Resource> extends InternelPoolOptions<T> {
    /**
     * 是否开启 Debug 模式，默认 false
     */
    debug?: boolean | DebugOptions;
}
export declare const createPool: <T extends Resource>(options: PoolOptions<T>) => Pool<T>;
