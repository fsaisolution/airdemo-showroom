"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const dayjs_1 = tslib_1.__importDefault(require("dayjs"));
const utc_1 = tslib_1.__importDefault(require("dayjs/plugin/utc"));
const timezone_1 = tslib_1.__importDefault(require("dayjs/plugin/timezone"));
dayjs_1.default.extend(utc_1.default);
dayjs_1.default.extend(timezone_1.default);
if (process.env.LOGGER_TIME_ZONE) {
    const timeZone = process.env.LOGGER_TIME_ZONE;
    try {
        new Date().toLocaleString('en-US', {
            timeZone,
        });
        dayjs_1.default.tz.setDefault(timeZone);
    }
    catch (err) {
        console.warn('Failed to set timezone to "%s". %s', timeZone, err);
    }
}
