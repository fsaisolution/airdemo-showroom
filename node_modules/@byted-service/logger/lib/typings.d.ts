/** @deprecated use Logger instead */
export interface ILogger {
    fatal(message: string, ...splat: any[]): void;
    error(message: string, ...splat: any[]): void;
    warn(message: string, ...splat: any[]): void;
    info(message: string, ...splat: any[]): void;
    debug(message: string, ...splat: any[]): void;
    trace(message: string, ...splat: any[]): void;
}
export type Level = 'fatal' | 'error' | 'warn' | 'info' | 'debug' | 'trace';
export interface Formatter {
    format(msg: string, options: LogOptions): string;
    clone?(): Formatter;
}
export interface Stream {
    write(msg: string, options: LogOptions): void;
    flush?(): void;
    flushSync?(): void;
    clone?(): Stream;
}
export interface LoggerOptions {
    /**
     * 日志输出级别，默认为 `debug`
     */
    level?: Level;
    /**
     * 输出时未设置级别日志的默认日志级别，默认 `info`
     */
    defaultLevel?: Level;
    /**
     * 日志格式化器
     */
    formatter?: Formatter['format'] | Formatter;
    /**
     * 日志输出流
     */
    streams?: Array<{
        level?: Level;
        stream: Stream;
    }>;
    /**
     * 日志刷新间隔，默认为 `15s`
     */
    flushInterval?: number;
}
export interface LogOptions {
    /**
     * 日志级别，默认为 `options.defaultLevel`
     */
    level?: Level;
    /**
     * 日志内容
     */
    message?: string;
    /**
     * 日志格式化参数
     */
    splat?: any[];
    [key: string]: any;
}
export interface CloneOptions extends LoggerOptions {
}
export interface LoggerOptionsType extends Omit<LoggerOptions, 'formatter'> {
    /**
     * `console` 日志级别，默认为 `options.level`
     */
    consoleLogLevel?: Level;
    /**
     * 文件日志级别，默认为 `options.level`
     */
    fileLogLevel?: Level;
    /**
     * `logagent` 日志级别，默认为 `options.level`
     */
    agentLogLevel?: Level;
    /**
     * `metrics` 打点级别，默认为 `warn`
     */
    metricsLevel?: Level;
    /**
     * 是否开启 `console` 日志，默认为 `!env.isTest() && !env.isProd()` ( 仅在本地开发环境下为 `true`，开启 `console` 日志 )
     */
    enableConsoleLog?: boolean;
    /**
     * 是否开启文件日志，默认为 `env.isTest() || env.isProd()` ( 仅在本地开发环境下为 `false`，关闭文件日志 )
     */
    enableFileLog?: boolean;
    /**
     * 是否开启 `logagent` 日志，默认为 `env.isTest() || env.isProd()` ( 仅在本地开发环境下为 `false`，关闭 `logagent` 日志 )
     */
    enableAgentLog?: boolean;
    /**
     * 是否开启 `metrics` 打点，默认为 `true`
     */
    enableMetrics?: boolean;
    /**
     * 项目 `psm`，默认为 `env.getPSM()`
     */
    psm?: string;
    /**
     * 日志默认前缀
     */
    prefix?: string;
    /**
     * 全局日志 `tags`
     */
    tags?: {
        [key: string]: any;
    };
    /**
     * 日志 `tagKeys`
     */
    tagKeys?: string[];
    /**
     * 堆栈深度，用于获取调用日志位置
     */
    callSiteFrameIndex?: number;
    /**
     * 是否开启 `security mark`，默认 `false`，在 `TTP` 环境下默认 `true`，开启后会将 `key=value` 转换为 `{{key=value}}`
     *
     * https://bytedance.feishu.cn/wiki/wikcn9yu4qGfXObjt40GN8ni9Lu
     */
    enableSecMark?: boolean;
    /**
     * 是否跳过打印日志时，不获取打印日志的代码文件名字和行数，用 -:- 替代
     * 错误日志（error,fatal）会忽略这个选项，始终带上 location
     * 支持通过 process.env.LOGGER_SKIP_LOCATION
     * 默认值为 false
     */
    skipLocation?: boolean;
}
export interface LoggerArgsType extends LogOptions {
    /**
     * [x-tt-logid](https://bytedance.feishu.cn/wiki/wikcn2L5x9IgQ4iLEdHofSMPVQc#qUpuqi)
     */
    logId?: string;
    /**
     * 日志调用堆栈位置
     */
    location?: string;
    /**
     * 日志 `tags`
     */
    tags?: {
        [key: string]: any;
    };
    /**
     * 日志 `tagKeys`
     */
    tagKeys?: string[];
}
export interface LoggerCloneOptionsType extends Omit<CloneOptions, 'formatter'> {
    /**
     * 项目 `psm`
     */
    psm?: string;
    /**
     * 日志默认前缀
     */
    prefix?: string;
    /**
     * 全局日志 `tags`
     */
    tags?: {
        [key: string]: any;
    };
    /**
     * 需要作为 `tag` 的 `key` 值
     */
    tagKeys?: string[];
    /**
     * 堆栈深度，用于获取调用日志位置
     */
    callSiteFrameIndex?: number;
}
export interface InputData {
    /**
     * 日志级别
     */
    level: Level;
    /**
     * 日志内容
     */
    message: string;
    /**
     * 日志格式化参数
     */
    splat: any[];
    /**
     * [x-tt-logid](https://bytedance.feishu.cn/wiki/wikcn2L5x9IgQ4iLEdHofSMPVQc#qUpuqi)
     */
    logId: string;
    /**
     * 日志调用堆栈位置
     */
    location: string;
    /**
     * 日志 `tags`
     */
    tags?: {
        [key: string]: any;
    };
    /**
     * 日志 `tagKeys`
     */
    tagKeys?: string[];
    /**
     * 日志版本
     */
    version: string;
    /**
     * 日志时间戳
     */
    ts: number;
    /**
     * 日志时间 `YYYY-MM-DD HH:mm:ss,SSS`
     */
    datetime: string;
    psm: string;
    cluster: string;
    idc: string;
    env: string;
    deployStage: string;
}
export type LoggerHook = (log: InputData) => InputData;
