import Metrics, { MetricsOptionsType } from '@byted-service/metrics';
import { InputData, LoggerOptionsType } from '../typings';
declare module '../typings' {
    interface LoggerOptionsType {
        /**
         * `metrics` 构造参数，或 `metrics` 实例
         */
        metrics?: Metrics | MetricsOptionsType;
    }
}
export declare const createMetrics: (options?: LoggerOptionsType) => {
    metrics: Metrics;
    write(_: string, inputData: InputData): void;
    flush(): void;
};
