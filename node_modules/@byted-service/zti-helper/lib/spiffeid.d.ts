import { TrustDomain } from './types';
export declare class SpiffeidID {
    td: TrustDomain;
    path: string;
    constructor(id: TrustDomain, path: string);
    string(): string;
    isZero(): boolean;
    static fromString(str?: string): SpiffeidID;
}
export interface Svid {
    id: SpiffeidID;
    audience: string[];
    expiry: number;
    claims: Record<string, any>;
    token: string;
}
