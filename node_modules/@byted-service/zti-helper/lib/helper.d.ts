import { LegacyIdentity } from './types';
import Logger from '@byted-service/logger';
export declare const symbolForZtiJWTHelperInstance: unique symbol;
export declare class ZtiJWTHelper {
    private ztiAgent;
    private sf;
    private tokenFromPath;
    private updateTokenFromPathTime;
    private logger;
    constructor(options?: {
        logger?: Logger;
    });
    getJwtSVID(forceUpdate?: boolean): Promise<string>;
    decodeGDPRorJwtSVID(tokenString: string): LegacyIdentity;
    decodeJwtSVID(tokenString: string): import("./zeroTrustIdentity").ZeroTrustIdentity;
    close(): void;
    private _pollOnce;
    private _createZtiAgent;
    private _fetchTokenFromPath;
    private _fetchJWTSVID;
}
