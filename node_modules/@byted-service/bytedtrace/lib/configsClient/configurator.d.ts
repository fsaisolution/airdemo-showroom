import * as types from './types';
import { Logger } from '@byted-service/logger';
export declare class Configurator implements types.IConfigurator {
    static create(serviceName: string, cluster: string, framework: string, logger: Logger): Configurator;
    private functions;
    private logger;
    private globalConfiger;
    private cbConfiger;
    private samplerConfiger;
    private event;
    syncInit(): Promise<void>;
    asyncInit(): void;
    pullGlobalTracerConfig(force?: boolean): Promise<any>;
    pullBreakerConfig(force?: boolean): Promise<any>;
    pullSamplerConfig(force?: boolean): Promise<any>;
    /**
     * 如果有变化需要触发注册的函数
     * The registered functions should be invoked if config has changed
     * @param key
     * @param f
     */
    register(key: string, f: types.voidFun): void;
    close(): void;
}
