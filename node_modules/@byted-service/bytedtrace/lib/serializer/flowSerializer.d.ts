/// <reference types="node" />
import * as gTypes from '../types';
import * as types from './types';
import { Transaction } from '../transaction';
export declare class FlowSerializer {
    options: types.FlowSerializerOption;
    dataSize: number;
    private serializer;
    constructor(options: types.FlowSerializerOption, env: gTypes.Environment);
    append(t: Transaction): void;
    size(): number;
    feed(data: any, type: types.KeyValueType): void;
    getSerializedData(): Buffer | undefined;
}
