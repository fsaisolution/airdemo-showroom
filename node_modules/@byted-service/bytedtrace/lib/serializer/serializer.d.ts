/// <reference types="node" />
import * as types from './types';
import { ISerializer } from './types';
export declare class Serializer extends ISerializer {
    constructor(serializerType?: string, opt?: types.SOption);
    setHeader(opt: types.HeaderOptionFlow): void;
    fixHeader(opt: types.HeaderOptionFlow): any;
    setCommonHeaders(headers: types.KeyValue[]): void;
    genCommonHeader(force: boolean): void;
    /**
     * 填充 header 共 24 Byte
     * @param force
     * @returns
     */
    genHeader(force: boolean): void;
    feed(dataPack: types.DataPack): void;
    serialize(): Buffer | undefined;
    clear(): void;
    reset(): void;
    private getTempBuffer;
}
