/// <reference types="node" />
import * as types from './types';
/**
 * 目前 sdk 侧还没有使用压缩，可以先忽略
 * 暂时先 ignore
 */
export declare const None = 0;
export declare const noneCompressor: types.Compressor;
export declare class Compressors {
    compressors: types.CompressorRecord;
    constructor(compressors?: types.CompressorRecord);
    registerCompressor(compressorType: string, compressor: types.Compressor): void;
    compress(compressorType: number, data: Buffer): Buffer;
    deCompress(compressorType: string, data: string): string;
}
export interface GlobalCompressors {
    compressors: Compressors;
}
export declare const globalCompressor: GlobalCompressors;
export declare const registerCompressor: (compressorType: string, compressor: types.Compressor) => void;
export declare const getCompressors: () => Compressors;
