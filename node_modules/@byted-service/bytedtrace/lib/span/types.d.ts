import { SpanContext } from './spanContext';
export interface LogRecordField {
    key: string;
    fieldType: number;
    numericVal: number;
    stringVal: string;
    interfaceVal: any;
}
export interface LogRecord {
    timestamp: number;
    fields: LogRecordField[];
}
export declare enum SamplingTraceType {
    /**
     * SamplingTrace only happen in local txn
     * 0b0010
     */
    localTraceType = 1,
    /**
     * SamplingTrace happen in local txn and transfer to backward
     * 0b0101
     */
    postTraceType = 2,
    /**
     * SamplingTrace happen in local txn and transfer to forward
     * 0b0011
     */
    debugTraceType = 3,
    /**
     * SamplingTrace happen in local txn and transfer to backward and forward
     * 0b0111
     */
    bothSideTraceType = 4
}
export declare const samplingMethodMap: {};
export interface SamplingTraceOptions {
    samplingTraceType: number;
    rootSpanContext?: SpanContext;
}
export interface ReservedParam {
    statusCode: number;
    remoteService: string;
    remoteMethod: string;
    remoteCluster: string;
    remoteAddrStr: string;
    remoteDc: string;
    serviceType: string;
    isError: boolean;
    businessStatusCode: number;
    sendSize: number;
    recvSize: number;
    component: string;
}
