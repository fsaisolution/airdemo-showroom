import { ExtInfo } from './extInfo';
export declare class BatchSpanInfo extends ExtInfo {
    constructor(disableTotalMetrics?: boolean);
    appendExtraSpanTags(tags: Array<[string, any]>): [string, any][];
}
