import * as types from './types';
import * as bytetracePb from './gen/bytedtrace_pb';
import { Span, SpanContext } from './span';
import { BytedTracer, TraceId } from './tracer';
export declare class Transaction {
    traceID: TraceId;
    logId?: string;
    transactionId: number;
    method: string;
    samplingRate: number;
    debugInfo: string;
    rootSpanId: string;
    serverSpans: Span[];
    clientSpans: Span[];
    customSpans: Span[];
    tracer: BytedTracer;
    rootSpan: Span;
    tags: types.TagKVs;
    baggage: Map<string, string>;
    pTraceType: number;
    samplingType: number;
    /**
     * 调用 setFinish 后，hasFinish 的值被设置为 1
     * 之后调用 isFinished 返回 true
     */
    hasFinish: number;
    private flags;
    constructor(tracer: BytedTracer);
    setRootSpan(span: Span): void;
    setSamplingInfo(samplerType: number, samplingRate: number): void;
    isFinished(): boolean;
    reset(): void;
    getSpanCount(): number;
    reportSpan(span: Span): void;
    appendSpan(span: Span): boolean;
    doAppendSpan(span: Span): void;
    setTagKv(key: string, value: any): void;
    setUniqueTagKv(key: string, value: any): void;
    setBaggageItem(key: string, value: string): void;
    /**
     * 目前暂时只用到了 serializeBinary
     * @param protoType
     * @returns
     */
    toBs(protoType?: types.ProtoType): Uint8Array;
    genTraceID(): bytetracePb.TraceID;
    initSampled(operationName: string, lowerBound: boolean): boolean;
    initRootSpanContext(ctx: SpanContext, rate: number, operationName: string, needLimit: boolean): void;
    isBothSideTrace(): boolean;
    isDebugTrace(): boolean;
    enableDebugTrace(operationName: string, code: number, recordDebugTraceType: boolean): void;
    setDebugTrace(code: number, recordDebugTraceType: boolean): void;
    setPostTrace(ctx?: SpanContext): boolean;
    enablePostTrace(operationName: string, ctx?: SpanContext): boolean;
    setLocalSamplingTrace(): void;
    sampled(): boolean;
    isSampled(): boolean;
    isSamplingPropagate(): boolean;
    isForwardPropagate(): boolean;
    isLocalSampled(): boolean;
    isPostTrace(): boolean;
    shouldReported(): boolean;
    getFlags(): number;
    toPb(): bytetracePb.TransactionProto;
    private passTxnSampling;
    private setFinished;
    private copy;
    private registerDegrade;
}
