/// <reference types="node" />
import * as remote from './remote';
import { EventEmitter } from 'events';
import { AsyncCache } from './asyncCache';
import { Logger } from '@byted-service/logger';
export declare class Configer<T> {
    protected cache: AsyncCache;
    protected event: EventEmitter;
    protected defaultConfig: T;
    protected namespace: string;
    protected fetcher: remote.Fetcher<T>;
    constructor(namespace: string, event: EventEmitter, logger: Logger);
    asyncSetDefaultConfig(): Promise<void>;
    syncSetDefaultConfig(): Promise<void>;
    get(force?: boolean): Promise<any>;
    dump(): {};
    protected isSame(oldValue: any, newValue: any): boolean;
    protected notify(newValue: any): void;
    private fetch;
}
