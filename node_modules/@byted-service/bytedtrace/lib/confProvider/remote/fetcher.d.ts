import { KVStore } from './kvstore';
export declare class Fetcher<T> {
    configer: any;
    private cachedConfig;
    private namespace;
    constructor(namespace: string, kvStore: KVStore, defaultConfig: T);
    fetch(): Promise<T>;
    private getPromiseByNamespace;
}
