/// <reference types="node" />
import * as gTypes from '../types';
import * as status from '../status';
import { Logger } from '@byted-service/logger';
export declare class AgentClient {
    lastConnectTime: number;
    statusCounter: status.StatusCounter;
    options: gTypes.ClientOptions & {
        netType: string;
    };
    byteLogHeader: Buffer;
    logger: Logger;
    private socket;
    private singleflight;
    constructor(statusCounter: status.StatusCounter, option?: gTypes.ClientOptions);
    send(data: Buffer): Promise<void>;
    getByteLogHeaderWithSize(size: number): Buffer;
    connect(): Promise<void>;
    sendFull(data: any): Promise<void>;
    close(): void;
    private getByteLogSetName;
    private createByteLogHeader;
    private fixCleintOptions;
}
