/// <reference types="node" />
/**
 * 上报 transaction
 */
import * as status from '../status';
import * as gTypes from '../types';
import { AgentProvider } from './provider';
import { FlowSerializer } from '../serializer';
import { Transaction } from '../transaction';
import { FlowSerializerOption } from '../serializer/types';
export declare class AgentReporter {
    static fixSerializerOption(options?: FlowSerializerOption): FlowSerializerOption;
    static getDefaultAgentReporterOption(options?: gTypes.AgentReporterOption): gTypes.AgentReporterOption;
    options: gTypes.AgentReporterOption;
    provider: AgentProvider;
    status: gTypes.ReporterStatus;
    flowSerializer: FlowSerializer;
    queue: Transaction[];
    lastFlushTimeMs: number;
    statusCounter: status.StatusCounter;
    flushIntervalId: NodeJS.Timeout;
    env: gTypes.Environment;
    constructor(options: gTypes.AgentReporterOption, env: gTypes.Environment, statusCounter: status.StatusCounter);
    /**
     * 如果 queue 中长度大于 queueLen 怎丢弃
     * @param txn
     */
    report(txn: Transaction): void;
    close(): void;
    run(): void;
    flushWithInterval(): void;
    flush(): void;
    append(txn: Transaction): void;
}
