# CHANGELOG

[![bnpm](https://web-bnpm.byted.org/badge/v/@byted-service/bytedtrace.svg?style=flat-square)](https://web-bnpm.byted.org/package/@byted-service/bytedtrace) [![download](https://2y8ngapf.fn.bytedance.net/d?p=@byted-service/bytedtrace)](https://web-bnpm.byted.org/package/@byted-service/bytedtrace)

[简体中文](https://code.byted.org/nodejs/byted-service/blob/master/packages/bytedtrace/README.md) | [CHANGELOG](https://code.byted.org/nodejs/byted-service/blob/master/packages/bytedtrace/CHANGELOG.md)

## 1.8.6

fix: emitRater tag 不正确的问题

## 1.8.5

chore: add `disableReportSpan` option to `bytedtrace`

## 1.8.4

chore: export `SpanType` 类型

## 1.8.3

refactor: metrics v2 client 创建形式 

## 1.8.2

chore: version bump

## 1.8.1

chore: version bump

## 1.8.0

feat: 替换 metrics v2 版本

## 1.7.15

chore: update tslib version

## 1.7.14

fix(bytedtrace): 移除 node-ip 依赖，规避 [CVE-2023-42282](https://nvd.nist.gov/vuln/detail/CVE-2023-42282) 及 [CVE-2024-29415](https://nvd.nist.gov/vuln/detail/CVE-2024-29415)

## 1.7.13

fix(bytedtrace): 修复 receive/send.bytes 打点类型问题

## 1.7.12

fix(bytedtrace)：修复 internal.op.rate 打点有问题

## 1.7.11

fix(bytedtrace)：修复 debug trace 不生效问题

## 1.7.10

fix(bytedtrace)：修复 client span metrics 缺少 request.env 问题

## 1.7.9

fix(bytedtrace)：修复 getCompressors 由于继承的属性 getFilterXxx 误当做 compressor 导致的异常

## 1.7.8

fix(bytedtrace): 修复 appendSpanMetricTags 方法多次调用 tag 未去重问题

## 1.7.7

chore(bytedtrace)：回滚升级依赖 @byted-service/metrics@2.0.0

## 1.7.6

chore(bytedtrace): 升级依赖 @byted-service/metrics@2.0.0

## 1.7.5

fix(bytedtrace): 修复 tcc-general 配置信息变更时，事件 EVENT_CONFIG_CHANGE 未触发问题

## 1.7.4

fix(bytedtrace): 当发生错误时，应该上报 \_to_addr，disableReportRemoteAddrWhenErr 应该取反

## 1.7.3

fix(bytedtrace): 修复 low-bound 采样策略 setInterval 计时问题

## 1.7.2

fix(bytedtrace): 修复 \_to_method 与\_method 一样问题

## 1.7.1

fix(bytedtrace): 修复 / 导致的 tcc-general key not found 问题

## 1.7.0

feat(bytedtrace): 增加 appendSpanMetricTags 方法，用户可自定义 metrics tag

## 1.6.9

fix(bytedtrace): 增加 \_http_route 和\_http_route 两个 tag

## 1.6.8

chore(bytedtrace): 增加 \_http_route 和\_http_route 两个 tag

## 1.6.7

feat(bytedtrace): 增加 \_tce_physical_cluster 与\_tce_node_level 两个 tag

## 1.6.6

fix(bytedtrace): add default value for metrics common tags

## 1.6.5

chore(bytedtrace): 增加 engines 字段

## 1.6.4

fix(bytedtrace): noop span 初始化 extinfo 属性

## 1.6.3

chore(bytedtrace): noop span 增加 extinfo 属性

## 1.6.2

feat(bytedtrace): 调整日志与对外 api 文档

## 1.6.1

fix(bytedtrace): 修改 logger 默认参数

## 1.6.0

feat(bytedtrace): 优化执行性能

## 1.5.2

fix(bytedtrace): report ohter span after server span is finished

## 1.5.1

fix(bytedtrace): disable tcc warning log

## 1.4.3

perf(bytedtrace): 优化 extract 和 inject 方法

## 1.4.2

fix(bytedtrace): 处理 emitMetricsXXX 方法中 tags 不传报错问题

## 1.4.1

fix(bytedtrace): 修改 accesslog 和 calllog 的默认值

## 1.4.0

feat(bytedtrace): 增加修改 server/client/custom span logger 接口禁止打印 tcc-gen log

## 1.3.2

chore(bytedtrace): 上报 framework & enableSendRecvBytesMetrics 默认为 true

## 1.3.1

feat(bytedtrace): 升级 @byted-service/consul，移除废弃 api

## 1.3.0

feat(bytedtrace): 支持 batch span 和 consumer span

## 1.2.4

feat(bytedtrace): 升级 @byted-service/env，移除废弃 api

## 1.2.3

chore(bytedtrace): 在非生产环境下禁止打印 tcc log

## 1.2.2

chore(bytedtrace): format 代码 & jsdoc 参数添加说明

## 1.2.1

fix(bytedtrace): 修复 Span 是 NoopSpan 时出现的问题

## 1.2.0

feat(bytedtrace): 增加 getStatus 接口返回 sdk 状态

## 1.1.1

chore(bytedtrace): unref 补充发版

## 1.1.0

feat(bytedtrace): 增加 lower-bound 采样策略

## 1.0.4

fix(bytedtrace): 去除 agent client 监听到 end 事件时打印 warn log

## 1.0.4

fix(bytedtrace): 修复 metrics 缺失 tags 和 rateCounter 少了后缀的 bug

## 1.0.3

fix(bytedtrace): 调用 defaultBytedTracer 后没有及时拉取 tcc 配置的 bug

fix(bytedtrace): 修复拉取 tcc 失败后不再上报数据的问题

## 1.0.2

fix(bytedtrace): 默认 spanLogger 和 clientSpanLogger 为同一个 Logger

fix(bytedtrace): bytedInject 静默处理 error

fix(bytedtrace): 移除 opentracing 依赖

fix(bytedtrace): 移除 @byted-service/logid 依赖

chore(bytedtrace): 采用 @byted-service/fnv 计算 logIdHash

## 1.0.1

feat(bytedtrace): 升级 @byted-service/logger 至 2.x

## 1.0.0

feat(bytedtrace): bytedtrace 基本功能
