/// <reference types="node" />
import { URL } from 'url';
import { FetchOptions, FetchResponse, RequestHandle } from './typings';
export declare const protocols: Map<string, RequestHandle>;
export declare function fetch(url: string | URL, options?: FetchOptions): Promise<FetchResponse>;
export declare namespace fetch {
    var setDefaultTimeout: (timeout: number) => number;
    var getDefaultTimeout: () => number;
    var register: (protocol: string, handle: RequestHandle) => void;
}
