import { UDPPool, DynamicPool } from '@byted-service/netpool';
import { ILogger } from './typings';
export declare const idCreator: (suffix?: string | undefined) => string;
export interface PoolOptions {
    logger: ILogger;
    host: string;
}
export declare const poolCache: {
    cache: Map<ILogger, Map<string, UDPPool>>;
    get(options: PoolOptions): UDPPool;
    values(): UDPPool[];
    delete(pool: UDPPool): void;
};
export declare const pool: DynamicPool<UDPPool, import("@byted-service/netpool").UDPSocket, PoolOptions>;
