"use strict";
// Refer to httpguts golang module
Object.defineProperty(exports, "__esModule", { value: true });
exports.validHeaderFieldValue = exports.validHeaderFieldName = void 0;
const isToken = (char) => {
    const validCharsets = "!#$%&'*+-.0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ^_`abcdefghijklmnopqrstuvwxyz|~";
    return validCharsets.includes(char);
};
const isLWS = (char) => {
    return char === ' ' || char === '\t';
};
const isCTL = (char) => {
    const code = char.charCodeAt(0);
    return code < 32 || code === 127;
};
const validHeaderFieldName = (v) => {
    if (!v) {
        return false;
    }
    for (const char of v) {
        if (!isToken(char)) {
            return false;
        }
    }
    return true;
};
exports.validHeaderFieldName = validHeaderFieldName;
const validHeaderFieldValue = (v) => {
    for (const char of v) {
        if (isCTL(char) && !isLWS(char)) {
            return false;
        }
    }
    return true;
};
exports.validHeaderFieldValue = validHeaderFieldValue;
