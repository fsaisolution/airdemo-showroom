"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.saveMetaInfoToMap = exports.setMetaInfoFromMap = exports.hasMetaInfo = void 0;
const kv_1 = require("./kv");
const info_1 = require("./info");
const consts_1 = require("./consts");
/**
 * HasMetaInfo detects whether the given context contains metainfo.
 */
function hasMetaInfo(ctx) {
    return (0, kv_1.getKV)(ctx) !== null;
}
exports.hasMetaInfo = hasMetaInfo;
const lenPTU = consts_1.PrefixTransientUpstream.length;
const lenPT = consts_1.PrefixTransient.length;
const lenPP = consts_1.PrefixPersistent.length;
/**
 * SetMetaInfoFromMap retrieves metainfo key-value pairs from the given map and sets then into the context.
 * Only those keys with prefixes defined in this module would be used.
 */
function setMetaInfoFromMap(ctx, m) {
    if (!ctx || !m) {
        return ctx;
    }
    const ks = Object.keys(m);
    for (let i = 0; i < ks.length; i++) {
        const k = ks[i];
        const v = m[k];
        if (k.length === 0 || v.length === 0) {
            continue;
        }
        let t = consts_1.invalidType;
        let nk = k;
        switch (true) {
            case k.startsWith(consts_1.PrefixTransientUpstream):
                if (k.length > lenPTU) {
                    t = consts_1.transientUpstreamType;
                    nk = k.slice(lenPTU);
                }
                break;
            case k.startsWith(consts_1.PrefixTransient):
                if (k.length > lenPT) {
                    t = consts_1.transientType;
                    nk = k.slice(lenPT);
                }
                break;
            case k.startsWith(consts_1.PrefixPersistent):
                if (k.length > lenPP) {
                    t = consts_1.persistentType;
                    nk = k.slice(lenPP);
                }
                break;
        }
        if (t !== consts_1.invalidType) {
            (0, kv_1.addKV)(ctx, t, nk, v);
        }
    }
    return ctx;
}
exports.setMetaInfoFromMap = setMetaInfoFromMap;
/**
 * SaveMetaInfoToMap set key-value pairs from ctx to m while filtering out transient-upstream data.
 */
function saveMetaInfoToMap(ctx, m) {
    if (!ctx || !m) {
        return;
    }
    ctx = (0, info_1.transferForward)(ctx);
    const allValues = (0, info_1.getAllValues)(ctx);
    const allValuesKeys = Object.keys(allValues);
    for (let i = 0; i < allValuesKeys.length; i++) {
        const k = allValuesKeys[i];
        const v = allValues[k];
        m[consts_1.PrefixTransient + k] = v;
    }
    const allPersistentValues = (0, info_1.getAllPersistentValues)(ctx);
    const allPersistentValuesKeys = Object.keys(allPersistentValues);
    for (let i = 0; i < allPersistentValuesKeys.length; i++) {
        const k = allPersistentValuesKeys[i];
        const v = allPersistentValues[k];
        m[consts_1.PrefixPersistent + k] = v;
    }
}
exports.saveMetaInfoToMap = saveMetaInfoToMap;
