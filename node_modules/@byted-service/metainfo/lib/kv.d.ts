import { Pair, Context } from './typings';
export declare function getKV(ctx: Context): Pair | null;
export declare function addKV(ctx: Context, infoType: number, key: string, val: string): Context;
export declare function getV(ctx: Context, mode: number, key: string): string;
export declare function getAll(ctx: Context, mode: number): {
    [key: string]: string;
};
/**
 * copyLink creates a new link copying the original one with each node processed by the given `modifier`.
 * If modifier returns nil, the kv pair will be discard.
 * Modifier must always return a valid pair object's pointer or nil.
 */
export declare function copyLink(ctx: Context, modifier: (mode: number, k: string, v: string) => Pair | null): Context;
