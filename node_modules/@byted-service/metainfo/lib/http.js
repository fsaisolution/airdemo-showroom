"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.toHTTPHeader = exports.fromHTTPHeader = exports.CGIVariableToHTTPHeader = exports.HTTPHeaderToCGIVariable = void 0;
const consts_1 = require("./consts");
const kv_1 = require("./kv");
const info_1 = require("./info");
const httplex_1 = require("./internal/httplex");
/**
 * HTTPHeaderToCGIVariable performs an CGI variable conversion.
 * For example, an HTTP header key `abc-def` will result in `ABC_DEF`.
 */
function HTTPHeaderToCGIVariable(key) {
    return key.toUpperCase().replace(/-/g, '_');
}
exports.HTTPHeaderToCGIVariable = HTTPHeaderToCGIVariable;
/**
 * CGIVariableToHTTPHeader converts a CGI variable into an HTTP header key.
 * For example, `ABC_DEF` will be converted to `abc-def`.
 */
function CGIVariableToHTTPHeader(key) {
    return key.toLowerCase().replace(/_/g, '-');
}
exports.CGIVariableToHTTPHeader = CGIVariableToHTTPHeader;
const lenHPP = consts_1.HTTPPrefixPersistent.length;
const lenHPT = consts_1.HTTPPrefixTransient.length;
/**
 * FromHTTPHeader reads metainfo from a given HTTP header and sets them into the context.
 * Note that this function does not call TransferForward inside.
 */
function fromHTTPHeader(ctx, h) {
    if (!ctx || !h) {
        return ctx;
    }
    const keys = Object.keys(h);
    for (let i = 0; i < keys.length; i++) {
        const k = keys[i].toLowerCase();
        const v = h[keys[i]];
        const ln = k.length;
        if (ln > lenHPT && k.startsWith(consts_1.HTTPPrefixTransient)) {
            (0, kv_1.addKV)(ctx, consts_1.transientType, HTTPHeaderToCGIVariable(k.slice(lenHPT)), v);
        }
        else if (ln > lenHPP && k.startsWith(consts_1.HTTPPrefixPersistent)) {
            (0, kv_1.addKV)(ctx, consts_1.persistentType, HTTPHeaderToCGIVariable(k.slice(lenHPP)), v);
        }
    }
    return ctx;
}
exports.fromHTTPHeader = fromHTTPHeader;
/**
 * ToHTTPHeader writes all metainfo into the given HTTP header.
 * Note that this function does not call TransferForward inside.
 */
function toHTTPHeader(ctx, h) {
    if (!ctx || !h) {
        return ctx;
    }
    const allValues = (0, info_1.getAllValues)(ctx);
    const allValuesKeys = Object.keys(allValues);
    for (let i = 0; i < allValuesKeys.length; i++) {
        const k = allValuesKeys[i];
        const v = allValues[k];
        if ((0, httplex_1.validHeaderFieldName)(k) && (0, httplex_1.validHeaderFieldValue)(v)) {
            h[consts_1.HTTPPrefixTransient + CGIVariableToHTTPHeader(k)] = v;
        }
    }
    const allPersistentValues = (0, info_1.getAllPersistentValues)(ctx);
    const allPersistentValuesKeys = Object.keys(allPersistentValues);
    for (let i = 0; i < allPersistentValuesKeys.length; i++) {
        const k = allPersistentValuesKeys[i];
        const v = allPersistentValues[k];
        if ((0, httplex_1.validHeaderFieldName)(k) && (0, httplex_1.validHeaderFieldValue)(v)) {
            h[consts_1.HTTPPrefixPersistent + CGIVariableToHTTPHeader(k)] = v;
        }
    }
    return ctx;
}
exports.toHTTPHeader = toHTTPHeader;
