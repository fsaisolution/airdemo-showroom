"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Singleflight = void 0;
const tslib_1 = require("tslib");
const lru_cache_1 = tslib_1.__importDefault(require("lru-cache"));
class Singleflight {
    constructor(options = {}) {
        this.options = Object.assign({}, options);
        this.keyMap = new lru_cache_1.default(this.options.cacheOptions);
    }
    do(key, fn) {
        const { keyMap } = this;
        let promise = keyMap.get(key);
        if (promise) {
            return promise;
        }
        promise = fn()
            .then(ret => {
            if (keyMap.get(key) === promise) {
                keyMap.del(key);
            }
            return Promise.resolve(ret);
        })
            .catch(err => {
            if (keyMap.get(key) === promise) {
                keyMap.del(key);
            }
            return Promise.reject(err);
        });
        keyMap.set(key, promise);
        return promise;
    }
}
exports.Singleflight = Singleflight;
