import { Unit, Timeline, TimerOptions, CallOptions } from './typings';
export declare class Timer {
    unit: Unit;
    timeout: number | null;
    private startTime;
    private stopTime;
    private nowTime;
    private startTimestamp;
    private stopTimestamp;
    private timelineMap;
    private _timelines;
    constructor(options?: TimerOptions | number | null);
    start(): this;
    stop(unit?: Unit): number;
    cost(unit?: Unit): number;
    remain(unit?: Unit, timeout?: number): number;
    call<T>(fn: (timeout: number | undefined, index: number) => Promise<T>, options?: CallOptions): Promise<T>;
    child(options?: TimerOptions | number | null): Timer;
    hasTimeline(key: string): boolean;
    delTimeline(key: string): Timeline | undefined;
    timeline(key: string, unit?: Unit): number;
    timelines(unit?: Unit): {
        key: string | symbol;
        cost: number;
        start: number;
        stop: number;
        unit: "s" | "second" | "ms" | "millisecond" | "μs" | "microsecond" | "ns" | "nanosecond";
        stoped: boolean;
    }[];
    /** @deprecated */
    unref(): this;
}
