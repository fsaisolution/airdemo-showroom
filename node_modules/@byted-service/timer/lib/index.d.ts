import { Timer } from './timer';
import { TimerOptions } from './typings';
export * from './timer';
export * from './consts';
export * from './typings';
export declare const createTimer: (options?: TimerOptions | number | null) => Timer;
/** @deprecated timer will not reuse by default, so is not need to release */
export declare const releaseTimer: (timer: Timer) => void;
