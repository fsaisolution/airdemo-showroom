"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getLogger = exports.NoopLogger = void 0;
const logger_1 = require("@byted-service/logger");
const defaultLogger = new logger_1.Logger();
class NoopLogger {
    log(args) {
        // do nothing
    }
    fatal(message, ...splat) {
        // do nothing
    }
    error(message, ...splat) {
        // do nothing
    }
    warn(message, ...splat) {
        // do nothing
    }
    info(message, ...splat) {
        // do nothing
    }
    debug(message, ...splat) {
        // do nothing
    }
    trace(message, ...splat) {
        // do nothing
    }
}
exports.NoopLogger = NoopLogger;
const noopLogger = new NoopLogger();
const getLogger = (disableLog = false) => {
    if (disableLog) {
        return noopLogger;
    }
    return defaultLogger;
};
exports.getLogger = getLogger;
