"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.textFormatter = exports.jsonFormatter = void 0;
function jsonFormatter(data) {
    return JSON.stringify(data);
}
exports.jsonFormatter = jsonFormatter;
function textFormatter(data) {
    const tags = Object.keys(data.tags).reduce((prev, cur) => {
        return prev + `${cur}=${data.tags[cur]} `;
    }, '');
    const message = `${data.level.toUpperCase()} ${data.datetime} ${data.version} ${data.location} ${data.hostIP} ${tags}${data.msg || data.message || '-'}\n`;
    return message;
}
exports.textFormatter = textFormatter;
