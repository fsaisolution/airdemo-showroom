/**
 * 用 Map 来动态索引序列
 */
import { Collector, CollectorGetOption, CollectorForEachCB } from './collector';
import { SlowCursor as Cursor } from './cursor';
import { GCOption } from '../type';
export type CursorPool = Map<string, Cursor>;
export type TagPool = Map<string, CursorPool>;
export declare class MapCollector extends Collector {
    private tagPool;
    private maxCursorSize;
    get(opt: CollectorGetOption): Cursor | null;
    gc(opt: GCOption): void;
    each(cb: CollectorForEachCB): void;
}
