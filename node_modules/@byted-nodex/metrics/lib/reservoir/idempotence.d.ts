/**
 * Gauge / Store
 * 实际值是采样周期中最后打上来的值。
 */
import { Value } from '../measure';
import { Reservoir } from './reservoir';
export declare class Idempotence extends Reservoir {
    realMerge(val: Value): boolean;
    values(): {
        suffix: string;
        type: import("@byted-nodex/metrics-codec").MetricType;
        values: number[];
    }[] | null;
    init(): void;
    reset(): void;
}
