/**
 * Counter
 * 会在指定 tagkv 集合积攒的值与打上来的值相加作为新值
 * 该值在 SDK 启动后一直累加，若 SDK 重启 或者 序列过期，则累计值清除。
 */
import { Value } from '../measure';
import { Reservoir } from './reservoir';
export declare class Addition extends Reservoir {
    lastValue: number;
    setLastValue?: boolean;
    get active(): boolean;
    realMerge(val: Value): boolean;
    values(): {
        suffix: string;
        type: import("@byted-nodex/metrics-codec").MetricType;
        values: number[];
        lastValues: number[] | undefined;
    }[];
    skip(): void;
    init(): void;
    reset(): void;
}
