/**
 * Rate Counter
 * 将采样周期内打上来的值累加 / 采样周期的秒数，算出变化率；每个周期内从 0 开始累加。
 */
import { Value } from '../measure';
import { Reservoir } from './reservoir';
export declare class Ratio extends Reservoir {
    get active(): boolean;
    realMerge(val: Value): boolean;
    values(): {
        suffix: string;
        type: import("@byted-nodex/metrics-codec").MetricType;
        values: number[];
    }[];
    init(): void;
    reset(): void;
}
