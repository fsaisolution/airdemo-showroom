"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.newMetric = newMetric;
const client_1 = require("./client");
const consts_1 = require("./consts");
/**
 * 默认的 newMetric 方法，给到基础库使用，省去每个基础库都重新 new 个 client
 * 注意！！该方法不能调用过多，创建的 metric 会被缓存（ 调用 metric.close 可以释放 ）
 */
function newMetric(metricName, options) {
    let client;
    const g = global;
    if (!(client = g[consts_1.DefaultClientSymbol])) {
        client = g[consts_1.DefaultClientSymbol] = new client_1.Client();
    }
    return client.newMetric(metricName, options);
}
