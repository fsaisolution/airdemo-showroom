import { Writer } from './';
export declare class NoopWriter implements Writer {
    ready: boolean;
    closed: boolean;
    close(): Promise<void>;
    init(): Promise<void>;
    write(): Promise<void>;
}
