import pooling from 'generic-pool';
import { Writer, WriterConstructor, WriterOptions } from './';
import { ILogger } from '../type';
export interface WriterPoolOptions<T extends WriterOptions = WriterOptions> extends pooling.Options {
    logger: ILogger;
    writer: WriterConstructor;
    writerOptions?: T;
}
export declare class WriterPool<T extends WriterOptions = WriterOptions> {
    logger: ILogger;
    pool: pooling.Pool<Writer>;
    options: WriterPoolOptions<T>;
    constructor(options: WriterPoolOptions<T>);
    private _create;
    private _destroy;
    destroy(w: Writer): Promise<void>;
    acquire(): Promise<Writer>;
    close(): Promise<void>;
    release(w: Writer): Promise<void>;
}
