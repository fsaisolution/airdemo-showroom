import { WriterPool } from './pool';
export interface Writer {
    ready: boolean;
    closed: boolean;
    init(): Promise<void>;
    write(buf: Buffer): Promise<void>;
    close(): Promise<void>;
}
export interface WriterConstructor {
    new (pool: WriterPool, opt?: any): Writer;
}
export type WriterOptions = Record<string, any>;
export * from './pool';
export * from './seqpacket';
export * from './noop';
