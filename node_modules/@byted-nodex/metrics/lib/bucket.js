"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.linearBuckets = linearBuckets;
exports.exponentialBuckets = exponentialBuckets;
const assert_1 = __importDefault(require("assert"));
/**
 * LinearBuckets creates 'count' buckets, each 'width' wide, where the lowest
 * bucket has an upper bound of 'start'. The final +Inf bucket is not counted
 * and not included in the returned slice. The returned slice is meant to be
 * used for the Buckets field of HistogramOpts.
 */
function linearBuckets(start, width, count) {
    (0, assert_1.default)(count > 0, 'LinearBuckets needs a positive count value');
    const buckets = [];
    for (let i = 0; i < count; i++) {
        buckets[i] = start;
        start += width;
    }
    return buckets;
}
/**
 * ExponentialBuckets creates 'count' buckets, where the lowest bucket has an
 * upper bound of 'start' and each following bucket's upper bound is 'factor'
 * times the previous bucket's upper bound. The final +Inf bucket is not counted
 * and not included in the returned slice. The returned slice is meant to be
 * used for the Buckets field of HistogramOpts.
 */
function exponentialBuckets(start, factor, count) {
    (0, assert_1.default)(count > 0, 'ExponentialBuckets needs a positive count value');
    (0, assert_1.default)(start > 0, 'ExponentialBuckets needs a positive start value');
    (0, assert_1.default)(factor > 1, 'ExponentialBuckets needs a factor greater than 1');
    const buckets = [];
    for (let i = 0; i < count; i++) {
        buckets[i] = start;
        start *= factor;
    }
    return buckets;
}
