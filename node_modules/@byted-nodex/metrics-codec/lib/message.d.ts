/// <reference types="node" />
import { MessageHeader, Metric, MetricType, Measurement, int32, Field } from './typings';
export interface MessageOptions {
    tenant?: string;
    account?: string;
    project?: string;
    sdkVersion?: string;
    dc?: string;
    host?: string;
}
export declare class Message {
    private dc;
    private host;
    header: MessageHeader;
    metrics: Metric[];
    constructor(options?: MessageOptions);
    get byteLength(): number;
    addMetric(metricType: MetricType, metricName: string, fields: Field[], measurements: Measurement[]): this;
    encode(): Buffer | undefined;
    reset(): void;
    setTimestamp(timestamp: bigint): void;
    setFlags(flags: int32): void;
}
