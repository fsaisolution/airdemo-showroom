# @byted-nodex/metrics-codec

Metrics V2 SDK 的 Codec 库。

> **注意**，该 SDK 仅为 @byted-nodex/metrics 提供能力，不要单独使用

## 安装

```bash
$ npm i @byted-nodex/metrics-codec --save
```

## 使用

```ts
import { Message } from '@byted-nodex/message-codec';

const message = new Message();
message.setTimestamp(1674017086n);
message.setFlags(0x3);

// 添加 metric
message.addMetric(
  MetricType.MultiFields,
  'metric.name',
  [
    { name: 'fields1', type: FieldType.Float64 },
    { name: 'field2', type: FieldType.Float64 },
  ],
  [
    {
      tags: [
        {
          key: 'tag0',
          value: 'value0',
        },
        {
          key: 'tag1',
          value: 'value1',
        },
        {
          key: 'tag2',
          value: 'value2',
        },
        {
          key: 'tag3',
          value: 'value3',
        },
      ],
      values: [1.0, 2.0],
    },
  ],
);

// 编码成 buffer
const buffer = message.encode();
```
