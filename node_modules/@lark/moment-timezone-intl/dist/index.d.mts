import moment from 'moment';
export { default } from 'moment';

declare class Zone {
    readonly name: string;
    constructor(name: string);
    abbr(mom: moment.Moment): string | undefined;
    utcOffset(mom: moment.Moment): number | undefined;
    utcOffset(mom: Date | number): number;
    parse(localTS: moment.Moment | Date | number): number;
}

declare module 'moment' {
    interface Moment {
        tz(): string | undefined;
        tz(timezone: string, keepLocalTime?: boolean): moment.Moment;
    }
    let defaultZone: Zone | null;
    const tz: MomentTimezone;
}
interface MomentTimezone {
    (): moment.Moment;
    (timezone: string): moment.Moment;
    (date: number, timezone: string): moment.Moment;
    (date: number[], timezone: string): moment.Moment;
    (date: string, timezone: string): moment.Moment;
    (date: string, format: moment.MomentFormatSpecification, timezone: string): moment.Moment;
    (date: string, format: moment.MomentFormatSpecification, strict: boolean, timezone: string): moment.Moment;
    (date: string, format: moment.MomentFormatSpecification, language: string, timezone: string): moment.Moment;
    (date: string, format: moment.MomentFormatSpecification, language: string, strict: boolean, timezone: string): moment.Moment;
    (date: Date, timezone: string): moment.Moment;
    (date: moment.Moment, timezone: string): moment.Moment;
    (date: any, timezone: string): moment.Moment;
    zone(timezone: string): Zone | null;
    names(): string[];
    guess(ignoreCache?: boolean): string;
    needsOffset(mom: moment.Moment): boolean;
    setDefault(timezone?: string): typeof moment;
}
