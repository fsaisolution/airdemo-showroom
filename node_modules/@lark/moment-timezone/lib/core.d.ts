/// <reference types="moment" />
import moment from '@lark/moment-timezone-polyfill';
import { IStartDayOfWeek } from './constants';
export declare const supportLocales: string[];
/**
 * @description 初始化moment
 * @param { locale, localeConfig, is12Hour, timezone }
 * locale: locale的key，项目不维护
 * localeConfig: import('moment-timezone').LocaleSpecification类型，业务方可以从locales中选取适配的语言做导入
 * is12Hour: 是否是12小时制
 * timezone: 时区id，如Asia/Shanghai等
 */
export declare function initMoment({ locale, localeConfig, is12Hour, timezone, }: {
    locale: string;
    localeConfig: moment.LocaleSpecification | moment.Locale;
    is12Hour: boolean;
    timezone?: string;
}): void;
/**
 * @description 更新是否是12h制
 * @param is12Hour
 */
export declare function updateTimeFormatHour(is12Hour: boolean): void;
export declare function is12HourFormat(): boolean;
/**
 * @description 更新一周的开始时间
 * @param startDayOfWeek
 */
export declare function updateStartDayOfWeek(startDayOfWeek: IStartDayOfWeek): void;
/**
 * @description 获得一周的开始时间
 */
export declare function getStartDayOfWeek(): number;
/**
 * @see 与原逻辑不一样，不再维护localtimezone的全局变量
 * @description 设置moment默认时区
 * @param timezone
 */
export declare function updateLocalTimezone(timezone: string): void;
/**
 * @see 与原逻辑不一样，不再维护localtimezone的全局变量
 * @description 没有调用initMoment的时候，返回的就是undefinded
 */
export declare function getLocalTimezone(): string;
