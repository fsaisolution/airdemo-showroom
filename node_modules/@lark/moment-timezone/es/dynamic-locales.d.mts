import * as moment from 'moment';
import _default from './locales.mjs';

declare type Locale = keyof typeof _default;
declare const dynamicLocales: Record<Locale, () => Promise<moment.Locale>>;
declare function getLocale(locale: Locale): Promise<moment.Locale>;

export { type Locale, dynamicLocales, getLocale };
